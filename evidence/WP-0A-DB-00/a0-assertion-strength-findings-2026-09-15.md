# The assertion-strength findings on 081 and 090 — which the closure batches already answer, and which stand

Run: `/claude/a0_atlas` (Author). Date: 2026-09-15. Six findings from the 2026-09-15 role runs are of one kind: an apply-time or static assertion that reads the **shape** of a predicate (a word is present, a count is right) rather than its **meaning**, so a reversal that keeps the word passes. They were owed to A0 as one forward batch. Reading them against what has merged since, most no longer need one.

| Finding | What it said | Where it stands on `main` today |
|---|---|---|
| Q0-081 **F4 / P7** — the deferred-FK absence assertion (`081:401-413`) can be gutted by appending a term that no constraint matches | true of the text | **moot**: batch 111 wrote the key, and 111's own apply-time block asserts its presence, columns, validation and uniqueness on the column. An absence assertion nobody can trigger is now a historical fact about 081, not a live control. |
| Q0-081 **F4 / P2** — narrowing `every_role` in 081 to `['authenticated']` silences its service-privilege assertions | true of the text | **covered**: batch 083's apply-time claim 3 asks the same question of all five service roles on `content_targets`, in a file 081's edits cannot reach. |
| C0-090 **M1** — the "no service role holds anything" premise asserted for two roles, not five | true of 090 | **covered**: batch 092's apply-time claim 3 asks it of all five on the three approval tables. |
| C0-090 **M2** — the request narrowing's chain is checked by word presence; a reversal keeping `content_items` and dropping the page `case` passes both layers | true | **stands, with a live case behind it**: `pinned-editor-a-cannot-see-the-approval-request-of-a-sibling-target-item` fails the reversal on a database (C0 says so), and Q0-081 F2 (#138) recorded why a text rule cannot see this: the item's own policy answers first. The apply-time text check is defence in depth and is now described as such in the corrected whys. A stronger apply-time form — comparing `pg_get_expr` to the exact expected predicate — is brittle against harmless rewrites and is not proposed. |
| C0-090 **M3** — the `expired` assertion proves the absence of a word | true | **stands, defended elsewhere**: the static rule's exact-literal match on the decide policy's WITH CHECK and `owner-a-cannot-expire-an-approval-request` both catch the reversal C0 ran; the sentence at `090:954-956` overstates which layer defends it and cannot be edited. Recorded here rather than rewritten. |
| C0-081 **M2** — "no neighbouring vocabulary value appears anywhere in the file" is asserted by a rule that does not assert it; **M4** — the §8-row derivation considers two rows where there are three | true | **stand**: both are about sentences in 081's text and its static rules' messages; neither is a control. A later increment that touches 081's static rules should correct the messages; nothing is owed to a migration. |

So: two moot, two covered by 083/092, two standing as text with their controls elsewhere. No forward migration is written for this class tonight, and this file is the record of why — the batch that was owed would have re-asserted, more strongly, four things that a later batch had already asserted or removed.
