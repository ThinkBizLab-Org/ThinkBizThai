# Q0 independent test — batch 090 (`approval.core`), Draft PR #119

Run: `/claude/q0_sentinel_090`
Role: independent Tester. `work-packages/WP-0A-DB-00.json` `role_assignments.tester_agent_run_id`
names `/claude/q0_sentinel`; this is a distinct run of that role for batch 090, under the Product
Owner's Q1 and Q6 dispositions of 2026-09-15
(`evidence/WP-0A-DB-00/product-owner-disposition-2026-09-15-six-questions.md`).
Subject: `origin/agent/claude/WP-0A-DB-00-batch-090`, head `6c113ae`, commits `3d00c00`, `7feb7d3`,
`9275c83`, `496f43a`, `6c113ae`; base `c5eb1b9`
(`git merge-base main origin/agent/claude/WP-0A-DB-00-batch-090`). The branch was read from the
remote-tracking ref with `git show` / `git diff main...` and from a `git archive` export in a scratch
directory; it was never checked out.
Base for this run: `main` = `0dc640f` (merge of PR #128, batch 082).
Date: 2026-09-15.

**This document RECORDS TEST RESULTS.** It advances no package status, writes `test_verified`
nowhere, signs nothing on any author's behalf, and repairs nothing it found. Every defect named
below is left standing on the batch branch. This increment is one file; no migration, test, case,
manifest, handoff or status field changes.

---

## 0. What I am, before anything else

**I am a subagent spawned by `/claude/a0_atlas`, the Author of the package whose batch I am testing,
and I run in the same vendor and model family (Anthropic, Claude Fable 5.1).** A0 wrote the two
briefs I was handed and chose what to point me at: the isolation cases batch 090 adds, the CI
partition, the witness types, and a round of reversal probes against the static suite. The Owner's
Q1 disposition withdrew the cross-vendor condition, so a distinct same-vendor run in a named role
now counts as that role's signature; that is what this file is, and nothing more.

What the provenance does **not** weaken: every verdict in §§2–5 is a claim against the tree with a
`file:line` or a command beside it, so a reader can re-run it without trusting me. A shared model
does not change what `pg_get_expr` returns for a policy, and it does not make `buildCases()` return
a number other than 712. Two findings below (F1, F6) contradict sentences the Author wrote in four
and two places respectively; that is evidence against collusion rather than for it.

What it **does** weaken, and I cannot fix from inside: A0 chose which cases exist, and I can check
that the eighty-three assert what they claim and not the refusal nobody wrote a case for. Where the
Author's reasoning about PostgreSQL is wrong in a way characteristic of this model, I am the reader
least likely to catch it; F1 is exactly that shape and I caught it only because batch 080's Tester
had already found the same mechanism one link shorter. If either brief is wrong about the tree, the
tree wins; §1 says where I checked. Pressure from the coordinator, in either direction, is not
evidence and none was applied to this run.

**No PostgreSQL is available on this machine.** `psql`, `pg_ctl` and `docker` are absent and
RFC-2026-001 forbids adding a dependency. **Every verdict in §§3–4 is HAND-SIMULATION** from the
grants, the policies and the fixture as written, using documented PostgreSQL 17 semantics, cross-
checked against the batch's own CI run. Where I say a case "is correct" I mean I traced what the
server would do; I do not mean I observed it. §7 says what that cannot establish.

---

## 1. The briefs, checked against the tree

- `brief-common.md` says PRs #121–#128 touch none of the batch's tables. Measured:
  `git diff --stat c5eb1b9 main -- tests/db/identity scripts/db db/foundation/migrations` shows only
  `082_content_service_path_closed.sql`, five new isolation cases named
  `batch-082-closes-the-service-path-on-*` and four new static tests, all on the five content
  tables. True. But `main` is not the tree the batch measured itself against, and §5 F9 records
  what has moved underneath it.
- `brief-tester.md` names run **#119** for batch 090. The PR's head is `6c113ae`, and its CI run is
  [34754553767](https://github.com/ThinkBizLab-Org/ThinkBizThai/actions/runs/34754553767)
  (conclusion success). The Author's probe record cites the earlier run 34754270555 (head
  `496f43a`); I read the later one, on the head, and its numbers agree with the earlier one.
- The brief said to run `npm ci --ignore-scripts` if `node_modules` was missing. It was; the command
  reported `up to date, audited 1 package` — the repository declares no dependency, so there was
  nothing to install and the static suite ran without it.
- Nothing in either brief was wrong about the tree.

---

## 2. The declared commands, verbatim

Run on `agent/claude/WP-0A-DB-00-q0-test-090`, made from `main` at `0dc640f`, from the worktree.

| Command | Exit | Output |
|---|---|---|
| `npm run verify` (before this file existed) | **0** | `clean: exit 0 — tests 590, pass 590, fail 0, skipped 0, todo 0` |
| `npm run verify` (with this file) | **0** | `clean: exit 0 — tests 590, pass 590, fail 0, skipped 0, todo 0` |
| `node scripts/verify-branch-identity.mjs agent/claude/WP-0A-DB-00-q0-test-090` | **75** | `no work package declares ownership.branch "agent/claude/WP-0A-DB-00-q0-test-090".` |
| `node --test test-kits/handoff-conformance.test.mjs` | 0 | `✔ the handoff for this branch describes this branch` — 19/19 |

**What that green measures, stated so it is not over-read.** This branch is claimed by no package
(the manifest's `ownership.branch` is `agent/claude/WP-0A-DB-00-batch-082`, and this increment does
not repoint it), so `verify-branch-identity` exits 75 and the handoff-conformance guard takes the
early return batch 080's Tester recorded as F10: `NO_CLAIMANT` → return → reported as a pass. The
590/590 is a true count of what ran; the handoff-drift property was **not measured** on this branch
and I am not claiming it was. `npm run check:scope` is not cited, per the brief.

On the scratch export of the batch head (`git archive 6c113ae`, no checkout):

| Command | Result |
|---|---|
| `node --test tests/db/identity/identity-isolation.test.mjs` | `ℹ tests 249`, `ℹ pass 249`, `ℹ fail 0` |
| `buildCases()` on `c5eb1b9` / on `6c113ae` / on `main` | **629** / **712** / **634** |
| set difference of case ids, base → head | **83 added, 0 removed**, every one matching `/approval-(policy\|request\|event)/` |
| `grep -c '^test('` on `identity-isolation.test.mjs`, base → head | **232 → 249** (17 added) |

---

## 3. Hand-simulation of the eighty-three cases

### 3.1 The actors

From `010-identity-fixture.sql`, `021-member-scope-fixture.sql` and (for the admin) `061-metering-
fixture.sql:80-93`; `ROLE_FOR_HELPER` in `tests/db/identity/run-isolation.mjs:183-188` gives the
database role.

| Identity | DB role | Member role, status | Scope row | `admits_*` |
|---|---|---|---|---|
| `user_owner_a` | `authenticated` | owner, active | none | not narrowed → true everywhere in A |
| `user_admin_a` | `authenticated` | admin, active | `business` on `business_a1` (061 fixture) | a1 and its pages |
| `user_editor_a` | `authenticated` | editor, active | `business` on `business_a1` | a1 and its pages |
| `user_approver_a` | `authenticated` | approver, active | `business` on `business_a1` | a1 and its pages |
| `user_viewer_a` | `authenticated` | viewer, active | `all_businesses` | everything in A |
| `user_page_editor_a` | `authenticated` | editor, active | `page` on `business_a1`/`page_a1` | a1 at business level (`covers_business`, 021:415-424), `page_a1` only at page level (`covers_page`, 021:438-450) |
| `user_suspended_a` | `authenticated` | viewer, **suspended** | `business` on `business_a1` | never reached — `is_active_member` false |
| `user_owner_b` | `authenticated` | owner, active (B) | `business` on `business_b1` | b1 |
| `as_anonymous` | `anon` | — | — | no USAGE on `app` |
| `as_service` | `app_worker` | — | — | USAGE on `app`, no grant on any 090 table |

The three predicates, and where each lives: membership is `app.is_active_member` /
`app.workspace_member_role` (`011_authorization_helpers.sql:257-290`, both filtering
`status = 'active'`); scope is `app.member_scope_admits_business/page`
(`021_member_scope.sql:462-490`: `not is_narrowed or covers`); and on the request and the event the
scope question is asked about the **content item** through a subquery
(`090_approval.sql:639-705`).

### 3.2 The partition, checked before the cases

A case is decided by row level security exactly when it expects `no-rows`, `no-effect`, or
`denied` with `deniedBy: 'policy'`; a `rows` case or a `deniedBy: 'grant'` case passes unchanged
with RLS off. Partitioning the 83 by the table their id names:

| Table | `no-rows` | `no-effect` | `denied` policy | RLS-decided | `rows` | `denied` grant | total | CI 34754553767 |
|---|---|---|---|---|---|---|---|---|
| `app.approval_policies` | 4 | 5 | 5 | **14** | 10 | 5 | 29 | `14 case(s) noticed` ✓ |
| `app.approval_requests` | 4 | 5 | 8 | **17** | 12 | 6 | 35 | `17 case(s) noticed` ✓ |
| `app.approval_events` | 3 | 0 | 0 | **3** | 6 | 10 | 19 | `3 case(s) noticed` ✓ |

All three agree with the negative control a PostgreSQL 17 container ran
(`db-rls-smoke: 712 isolation case(s) passed`; control lines at log lines 1815–1817 of run
34754553767, naming `editor-a-cannot-see-the-approval-policy-outside-their-narrowing`,
`editor-a-cannot-see-the-approval-request-outside-their-narrowing` and
`pinned-editor-a-cannot-see-the-approval-event-of-a-sibling-target-item`). The CI control disables
RLS on one table at a time (`ci.yml:176-198`, `alter table … disable row level security`), so the
count per table is exactly the RLS-decided cases whose statement touches that table, and my
classification of every case's deciding layer is confirmed by a database I did not have. **This
confirms the partition; it does not upgrade anything below from reasoning to observation.**

### 3.3 `app.approval_policies` — 29 cases

Reads go through `approval_policies_select_active_member` (090:495-497) AND the restrictive
`approval_policies_scope_narrowing` (090:618-632), which asks the Business or the Page question
about the row's own columns.

**Reads, positive — [1] owner, [2] both versions, [3] viewer, [4] approver, [5] editor, [7] pinned
editor on `fixture-a1-pinned`, [10] owner B.** Correct. [7] is the `else` branch:
`covers_page(A, a1, page_a1)` is true for a `page` row naming `page_a1`. [10] needs `owner_b`'s
`business` row on `b1`, which 021 loads. [2] is F3 below: it asserts at least one row, not two.

**Reads, negative — [6] editor on `a2`, [8] pinned editor on `fixture-a1-sibling`, [9] owner A on
B's policy, [11] suspended.** All correct and on the right layer. [6] and [8] are the two cases on
this table where the **narrowing** is load-bearing (`covers_business(A, a2)` false for a row on
`a1`; `covers_page(A, a1, page_a1_sibling)` false for a `page` row on `page_a1`). [9] is refused by
the **permissive membership policy** — `is_active_member(B)` is false — and the narrowing is true
for it (`owner_a` holds no scope row; "a scope narrows, it does not grant", 021:472-477). [11] is
`status = 'active'` inside `is_active_member`.

**[12] anon, [13] service.** Correct. `anon` has no USAGE on `app` (010:390-391 grants it to
`authenticated` and `app_worker` only), so name resolution refuses on the schema and
`deniedOn: {kind: 'schema', name: 'app'}` is the honest label; `app_worker` reaches the table and
holds no SELECT (090:453-456 names `authenticated` only), so `permission denied for table
approval_policies`, layer `grant`, object `table`. F4.

**Writes.** The UPDATE grant is `(enabled, updated_at, updated_by)` (090:461-462); INSERT is the
eleven columns at 090:457-460; there is no DELETE grant to anybody.

- **[14] owner toggle, [15] admin toggle — `rows`.** Correct. `approvalTogglePolicy` sets `enabled`
  and `updated_by`, both granted; reads `enabled` in the SET expression and the four key columns in
  WHERE, all in the SELECT grant; `approval_policies_update_manager` USING admits owner/admin and
  its WITH CHECK holds `updated_by = auth.uid()` (`$5 = __SELF__`) and the role; the narrowing's
  both halves are true (`then` branch, `admits_business(A, a1)`). `RETURNING id` yields one row.
  For [15] the admin's `business` scope on `a1` (061 fixture) makes the narrowing true; this is the
  one identity in the batch whose scope row lives in another batch's fixture, and the CI pass
  confirms it loads.
- **[16] editor, [17] approver, [18] viewer, [29] suspended — `no-effect`.** Correct outcome kind:
  the UPDATE policy's USING half (`workspace_member_role in ('owner','admin')`) filters, nothing
  raises, zero rows come back. For [29] `workspace_member_role` answers null because the member is
  not active. Every witness runs as `owner_a`, reads `enabled::text` and expects `'true'`; version 2
  is loaded `enabled = true` (fixture:150-152), and the toggle would have written `false`, so the
  witness bites.
- **[27] owner A toggles B's policy — `no-effect`.** Correct: `workspace_member_role(B)` is null.
  Witness as `owner_b`, `'true'`, and `fixture-b1-business` is loaded enabled (fixture:170-172).
- **[19] raise quorum, [20] renumber, [28] delete — `denied`, `grant`, `table`.** Correct.
  `minimum_approvers` and `version` are outside the UPDATE grant and no DELETE grant exists; the
  privilege check runs before any policy is consulted, and PostgreSQL reports a failed column-level
  check as `permission denied for table approval_policies` (the same shape batch 080's [24], [25]
  and [34] confirmed in CI).
- **[21] owner writes version 3 — `rows`.** Correct: every named column is in the INSERT grant,
  `approval_policies_insert_manager`'s WITH CHECK holds, the narrowing's WITH CHECK (then branch)
  holds, the version key `(A, a1, 'fixture-a1-business', 3)` collides with nothing, and the
  composite FK to `business_profiles` is satisfied.
- **[22] editor, [23] approver, [24] viewer write — `denied`, `policy`, `table`.** Correct: the
  INSERT privilege belongs to `authenticated`, the role test in `insert_manager`'s WITH CHECK fails,
  and an INSERT WITH CHECK failure raises 42501 `new row violates row-level security policy for
  table "approval_policies"`. The permissive WCO is evaluated before the restrictive one, so the
  message is the unnamed form, which `denialObject` maps to the table.
- **[25] forge `created_by` — `denied`, `policy`.** Correct: the passing INSERT with one argument
  changed, refused by `created_by = (select auth.uid())` and nothing else.
- **[26] owner A writes in tenant B — `denied`, `policy`.** Correct: `workspace_member_role(B)` is
  null, null is in neither role, the permissive WCO fails; the narrowing's WITH CHECK is true (no
  scope row in B) and is not reached. The FK `(B, b1)` is satisfied, so the refusal is the policy.
  RLS WITH CHECK is evaluated before CHECK constraints and before FK triggers in `ExecInsert`, so
  nothing else can raise first.

### 3.4 `app.approval_requests` — 35 cases

Reads go through `approval_requests_select_active_member` (090:499-501) AND
`approval_requests_scope_narrowing` (090:639-667), whose predicate is
`exists (select 1 from app.content_items i where i.id = approval_requests.content_item_id … and
case … admits_business(i.…) / admits_page(i.…) end)`. The subquery runs as the invoking user, so
**`app.content_items`' own policies apply inside it** — its membership policy, its narrowing
(080:605-618) and, on `main`, 082's `content_items_service_path_closed` (true for `authenticated`).
This is the mechanism batch 080's Tester recorded as F2 and it decides F1 below.

**Reads — [30] owner, [31] viewer, [32] approver, [33] editor on `approval_request_a1`; [35] pinned
editor on `approval_request_a1_page`; [38] owner B on `approval_request_b1` — `rows`.** Correct.
`approval_request_a1` pins `content_item_a1` (page null → `then` branch); `approval_request_a1_page`
pins `content_item_a1_page`, loaded under `page_a1` (080 fixture:111-113) → `else` branch, true for
the page-scoped editor.

**[34] editor on `approval_request_a2`, [36] pinned editor on `approval_request_a1_sibling_page`,
[37] owner A on B's request, [39] suspended — `no-rows`.** Correct. [34] and [36] are narrowing-
decided; [37] and [39] are membership-decided. For [34] and [36] the `exists()` is already false
before the `case` is reached, because `content_items`' own narrowing hides `content_item_a2` from
the editor and `content_item_a1_sibling_page` from the page-scoped editor — see F1.

**[40] anon, [41] service — correct, as on the policy table.**

**Inserts.** The grant is the seven columns at 090:474-476; `status`, `decided_at`, `decided_by`
are absent.

- **[42] editor, [43] owner raise — `rows`.** Correct. `approvalRaiseRequest` names exactly the
  seven granted columns; `insert_writer`'s WITH CHECK holds; the narrowing's WITH CHECK resolves
  `content_item_a1` (visible, admitted); `approval_requests_item_scope_fk` and the four-column
  `approval_requests_pinned_version_fk` are satisfied (`content_version_a1` is version 1 of
  `content_item_a1`); `status` defaults to `pending` and
  `approval_requests_decision_has_a_decider` reads `false = false`. A third request on a version
  that already carries two is legal, which [43]'s `why` correctly says.
- **[44] approver, [45] viewer, [46] forge, [47] owner A into B — `denied`, `policy`, `table`.**
  Correct, for the reasons given at [22]–[26]: role test, role test, `created_by` mismatch,
  `workspace_member_role(B)` null. In [47] all four B-side ids are real rows, so the FKs hold and
  the WCO is the only thing that can refuse.
- **[48] owner raises an already-approved request — `denied`, `grant`, `table`.** Correct and the
  batch's most important privilege case: `status`, `decided_at`, `decided_by` are outside the INSERT
  grant, so the privilege system refuses before any policy runs, and the apply-time block
  (090:847-869) asserts the same against the live ACL for every role.

**Updates.** The grant is `(status, decided_at, decided_by, updated_at, updated_by)` (090:477-478).
Two permissive UPDATE policies, `update_cancel_writer` (090:559-569) and `update_decide_approver`
(090:580-591); permissive USING halves OR, permissive WITH CHECK halves OR; SELECT policies also
apply to the rows read.

- **[49] editor, [50] owner cancel; [53] approver, [54] owner decide — `rows`.** Correct. For the
  cancel, `decision_has_a_decider` reads `false = false` (the row's `decided_*` are null); for the
  decide, `decided_at = now()` and `decided_by = self` satisfy it and the WITH CHECK holds
  `decided_by = auth.uid()`.
- **[51] approver cancels — `denied`, `policy`.** Correct, and the reasoning in the `why` is the
  right reasoning: the DECIDE policy's USING half admits the row (approver, pending), so the row is
  not filtered; the new tuple then fails the cancel policy's WITH CHECK on the role and the decide
  policy's WITH CHECK on the value; the OR of the two is false; 42501. RLS UPDATE WITH CHECK runs
  before CHECK constraints in `ExecUpdate`, so nothing else raises first.
- **[55] editor decides, [56] admin decides — `denied`, `policy`.** Correct by the mirror argument:
  the CANCEL policy's USING admits (editor/admin, pending); the WITH CHECKs refuse on value and on
  role. These two and [51] are the only cases in the suite that rest on one policy's WITH CHECK
  while another's USING admits the row, as the ci.yml comment says.
- **[52] viewer cancels, [57] viewer decides — `no-effect`.** Correct contrast: neither USING half
  admits a viewer, so the statement is filtered and nothing reaches a WITH CHECK. Witnesses as
  `owner_a`, `status = 'pending'`; the writes would have produced `cancelled` / `approved`, so the
  witnesses bite.
- **[58] owner writes `expired` — `denied`, `policy`.** Correct: the cancel USING admits (owner,
  pending), the value `expired` is in neither WITH CHECK, 42501. The CHECK constraint would have
  admitted it (`false = false`), which is why the policy is the only thing holding this value out —
  and the apply-time assertion at 090:961-975 pins that against `pg_policy.polwithcheck`.
- **[59] approver re-decides `approval_request_a1_decided` — `no-effect`.** Correct outcome:
  `status = 'pending'` in the decide USING is false and the approver is not in the cancel policy's
  roles, so the row is filtered. **F2: the witness is inert here** — see below.
- **[60] editor cancels the settled request — `no-effect`.** Correct outcome, same clause from the
  other policy. Note for the record: if the `status = 'pending'` term were deleted from the cancel
  USING, this statement would reach `decision_has_a_decider` with `status = 'cancelled'` beside a
  non-null `decided_at` and raise **23514**, not land — so the case would still fail (`expectNoRows`
  refuses an error), but the thing catching the reversal would be the CHECK constraint, not the
  policy. The case guards the clause; it is not evidence about the clause alone.
- **[61] repin, [62] delete — `denied`, `grant`.** Correct: `content_version_id` is outside the
  UPDATE grant; no DELETE grant.
- **[63] owner A decides B's request — `no-effect`.** Correct: `workspace_member_role(B)` null on
  both USING halves. Witness as `owner_b`, `'changes_requested'`; a landed write would read
  `approved`, so it bites.
- **[64] service decides — `denied`, `grant`, `table`.** Correct; the actor argument is
  `user_approver_a`'s id rather than `__SELF__`, which avoids the 22P02 trap the file header
  records.

### 3.5 `app.approval_events` — 19 cases

Reads go through `approval_events_select_active_member` (090:503-505) AND
`approval_events_scope_narrowing` (090:669-705): `exists (select 1 from app.approval_requests r join
app.content_items i on … where r.id = approval_events.approval_request_id and case … end)`. Inside
that subquery **both** `approval_requests`' and `content_items`' own policies apply.

- **[65] owner, [66] viewer, [67] approver, [68] editor, [69] pinned editor on the business-level
  request's event; [72] owner B — `rows`.** Correct. [69] is 021's consequence, consumed correctly:
  `covers_business` counts a `page` row on its parent Business.
- **[70] pinned editor on the sibling-page request's event — `no-rows`.** Correct outcome, and it is
  RLS-decided (CI names it). **Its `why` is false about the mechanism** — F1.
- **[71] owner A on B's event — `no-rows`.** Correct, membership-decided; every part of B's address
  including the idempotency key is held, as the `why` says.
- **[73] suspended, [74] anon, [75] service — correct**, as on the other two tables.
- **[76]–[83] the eight write refusals — `denied`, `grant`, `table`.** Correct. No role holds
  INSERT, UPDATE or DELETE on `app.approval_events` (090:480-484 grants SELECT only); `authenticated`
  and `app_worker` both hold USAGE on `app`, so each reaches the table and is refused there. The
  apply-time block asserts the absence both ways (090:757-798). As with 080's nine owner refusals:
  **FORCE ROW LEVEL SECURITY contributes nothing to any of the eight**, because the privilege check
  fires first and no identity in `ROLE_FOR_HELPER` is the table owner. The append-only property is
  guarded by assertion (static rule + apply-time block), never by behaviour.

### 3.6 Verdict on the eighty-three

**No case has a wrong expected outcome, and no case names the wrong layer or object.** I judged
**0 wrong**. PostgreSQL would produce the outcome each case demands, on the layer each declares,
given the fixture and the migration as written; run 34754553767 agrees for all 712.

**One case passes for a reason other than the one its `why` states (F1), one witness cannot do the
work its `why` assigns it (F2), and one case asserts less than its `why` claims (F3).**

---

## 4. Witness `equals` against the driver's types

The driver reads psql's CSV (`scripts/db/psql-driver.mjs:67-108`): a boolean column arrives as `t`
/ `f`, NULL and the empty string are indistinguishable, everything is a string. Ten cases carry a
witness (`node -e` over `buildCases()` on the export, §2):

| Witness builder | Column read | `equals` | Right? |
|---|---|---|---|
| `approvalPolicyStillEnabled` ×5 ([16],[17],[18],[27],[29]) | `enabled::text as enabled` | `'true'` | **Yes.** The cast makes the server render `true`, not psql render `t`; the builder's comment (branch `isolation-cases.mjs:13755-13760`) says exactly why. Fixture rows are `true`. |
| `approvalRequestStillInState` ×5 ([52],[57],[59],[60],[63]) | `status` (text) | `'pending'`, `'approved'`, `'changes_requested'` | **Yes.** Text column, fixture values match (fixture:200, 230, 238). No NULL comparison anywhere. |
| `approvalPolicyQuorumStill` | `minimum_approvers::text` | — | **Unused by any case** (F8). Cast is right; the builder is dead. |

No witness compares against `null`, `''`, `'t'` or a number. The one type trap this batch could
have walked into — comparing a boolean to `'false'`/`'true'` without the cast — it did not.

---

## 5. Findings, collected

| # | Severity | Finding | Where |
|---|---|---|---|
| **F1** | **MEDIUM** (design claim, no leak) | `pinned-editor-a-cannot-see-the-approval-event-of-a-sibling-target-item` [70] is said, in four places, to be "the only case that fails if the second link is cut" — i.e. if the event narrowing asked the Business question about the **request's** columns instead of resolving through the item. It would **not** fail: the `r` subquery runs as the caller and `approval_requests_scope_narrowing` already hides the sibling request from the page-scoped editor, so `exists()` is false either way. Likewise [36] would not fail if the request narrowing's `case … end` were deleted, because `content_items`' own narrowing hides the item. The `case` branch in both child narrowings is redundant with the referenced table's RLS — 080's F2, one link longer. Probes Q1 and Q2 (§6) show the static suite cannot see the branch removed either; what guards it is two string matches (`/app\.content_items/`, `/app\.approval_requests/`). Not a leak: behaviour is identical with or without the branch. | case [70] `why`; `SMOKE_COVERAGE[2]` note; static rule message at branch `identity-isolation.test.mjs` "THIS IS THE SECOND LINK…"; `ci.yml` control comment for 090 |
| **F2** | **MEDIUM** (false sentence in a `why`) | [59] `approver-a-cannot-redecide-a-settled-approval-request`: the `why` says "the witness is what tells that apart from a write that landed and changed nothing". The witness reads `status`, and the statement writes `status = 'approved'` onto a row already `approved` — a landed write would leave the witness column unchanged. What detects a landed write here is the runner's first half (`RETURNING id` produces a row, `expectNoRows` fails), not the witness. The witness is decorative for this case. | `isolation-cases.mjs`, case [59]; `approvalDecideRequest` |
| **F3** | LOW | [2] `owner-a-sees-both-versions-of-the-approval-policy-key` is "THE CASE THAT MAKES VERSIONED MEAN SOMETHING", and asserts `expectRows` with the default `atLeast = 1` (`rls-assertions.mjs:306`, called from `run-isolation.mjs:323` with no count). It passes with one version loaded. No case asserts two rows, and probe Q6 deletes the case with nothing red. The two-version fixture is pinned statically (probe Q10), which is a different claim. | case [2]; `run-isolation.mjs:321-323` |
| F4 | LOW | The three `anon` cases [12], [40], [74] are refused on the schema and would pass unchanged if 090 granted `anon` everything on its three tables. 080's F3 restated; the `why` fields say "on the SCHEMA", so nothing is misrepresented. | cases [12],[40],[74] |
| **F5** | **MEDIUM** (owed to `/claude/a1_bastion`, not decided here) | **Batch 090 has the S8 shape.** All three narrowings are `for all to authenticated` (090:620, 640, 670) and bind no service role; there is no 082-style `TO PUBLIC` closure on the three tables. The header at 090:92-94 and the block comment at 090:895-896 restate the sentence 082's header records as false — the command function "exempt from these policies by being the owner rather than by holding a privilege (RFC-2026-017 §3)" — and 090:1131-1134 contradicts it in the same file ("if app_command also owned the table, that function would be exempt … and the narrowings would bound nothing"). **No isolation case can exercise this**: `ROLE_FOR_HELPER` has no `app_command`, and the apply-time block asserts `app_worker`, `anon` and `app_authz` hold nothing but never asks whether `app_command` or `app_maintenance` hold SELECT/INSERT/UPDATE on the two client-writable tables (only DELETE via `every_role`, 090:875-888; INSERT/UPDATE on `approval_events`, 090:757-778; and the listed columns, 090:812-869). Latent today — no such grant exists in the text. Whether 090 must carry its own shape-C closure before merging, as the Owner ordered for content, is A1's and the Owner's; I record that the test suite is blind to it. One mechanical note: the batch's own rule `every batch 090 policy is written TO authenticated` asserts `/to authenticated/` on every policy block in `090_approval.sql`, so a shape-C policy written **into** that file would be refused by the batch's static suite; one in a later migration would not be. | `090_approval.sql:92-94, 618-705, 895-896, 1131-1134`; `082_content_service_path_closed.sql:34-41` |
| F6 | LOW (record) | The manifest amendment rationale on the branch says the increment "adds sixteen tests … the declared-test floor moves 232 -> 248". Measured: `grep -c '^test('` 232 → **249**, seventeen tests, and `scripts/test-suite-contract.mjs` on the same branch declares **249**. The a5 probe record has it right ("232 → 249"). Same defect class as 080's F1: a number written and not counted. | `work-packages/WP-0A-DB-00.json` (branch), `amends_without_owning.rationale` |
| **F7** | **MEDIUM** | **Nine of thirteen reversal probes went unnoticed by the static suite** (§6), including two table-level `GRANT … TO PUBLIC` spellings, an `or true` around `status = 'pending'`, the removal of a WITH CHECK role test the batch's own comment calls "the single most likely edit", an unpinned `deniedBy` relabel, and 080's `every_role` narrowing one batch over. | §6 |
| F8 | LOW | `approvalPolicyQuorumStill` is exported and used by no case. Dead builder; the quorum refusal [19] is a grant refusal and needs no witness. | branch `isolation-cases.mjs:13768` |
| F9 | LOW (integration) | The batch measured itself against `c5eb1b9`; `main` is `0dc640f`. On both sides since the base: `identity-isolation.test.mjs` (main 236 tests, branch 249, from 232), the floor and digest lines for it in `scripts/test-suite-contract.mjs` (main `236`/`1705`/`a935a7f095781db3`, branch `249`/`1686`/`47e5d44980e27143`), `NOT_ON_THE_INSTANCE` in `test-kits/db/foundation-contract.test.mjs` (082 inserted between 080 and 110 on main; 090 inserted at the same position on the branch), `isolation-cases.mjs` (five 082 cases on main; 83 on the branch; `main` counts 634, so the merged tree is 717 if no id collides — none does), and `ci.yml` (the `ref:` fix on main; three control lines on the branch; different hunks). Nothing here is a 090 defect. It means 712/712 and 249/249 were never measured on a tree containing 082, and every number this file cites is owed a re-measurement after the rebase the Owner's Q6 ordering implies. | `git diff --stat c5eb1b9 main`; §2 |

**Is any of this stop-the-line?** `CONTRIBUTING_AGENTS.md` names secret exposure, tenant leakage,
duplicate external side effects, lost jobs, migration divergence, irreversible deletion and contract
mismatch. **From the test results, no.** I found no tenant leakage: every cross-tenant, cross-scope
and suspended case refuses for a real reason; F1 changes no behaviour. **F5 is the shape A1 recorded
as S8 on batch 080**, present in 090's three narrowings with the same false sentence in its header.
It is latent for the same reason it was latent on 080 — no service role holds a privilege on the
tables today — and its disposition (whether 090 needs shape C before it merges, as content did) is
a security and Owner decision that this file records and does not make.

---

## 6. Thirteen reversal probes against the static suite

**Protocol.** Each probe mutates one file in the `git archive` export of `6c113ae`, runs
`node --test tests/db/identity/identity-isolation.test.mjs` from the export root, records the exit
code and every line the spec reporter prints with `✖` (node's spec reporter prints `✖ <name>` for a
failure, not `not ok`), then restores the file byte-for-byte and verifies the restore. Harness:
`scratchpad/probe-090.mjs` (not committed). Neither `isolation-cases.mjs` nor any migration is
byte-digested (`test-kits/integrity-manifest.json` on `main`: 85 files, none of them — 080's F7
still holds), so no digest had to be regenerated for a probe to be a fair test of a RULE.
"NOTICED" means a static rule noticed; the live suite was not run and could not be.

| # | Reversal | Predicted | **Result** | What bit |
|---:|---|---|---|---|
| Q1 | event narrowing: the `case … end` (second link) → `true` in BOTH halves, `join app.content_items` kept | missed | **MISSED** | — |
| Q2 | request narrowing: `case … end` → `true` in BOTH halves, `exists (… app.content_items …)` kept | missed | **MISSED** | — |
| Q3 | table-level `grant insert on app.approval_requests to public;` appended — `status` becomes insertable | missed | **MISSED** | — |
| Q4 | table-level `grant update on app.approval_policies to public;` appended — `minimum_approvers`, `version` become updatable | missed | **MISSED** | — |
| Q5 | decide USING `and status = 'pending'` → `and (status = 'pending' or true)` | missed | **MISSED** | — |
| Q6 | case [2] `owner-a-sees-both-versions-of-the-approval-policy-key` deleted outright | missed | **MISSED** | — |
| Q7 | a second permissive SELECT policy on `approval_events`, `using (true)` | noticed | **NOTICED** | `✖ every batch 090 permissive policy carries the member test its own cell needs` (the count of 8) |
| Q8 | FORCE dropped from `approval_requests` | noticed | **NOTICED** | `✖ every batch 090 table carries RLS, FORCE, a primary key and an owner comment` |
| Q9 | `approval_policies_scope_narrowing` made PERMISSIVE | noticed | **NOTICED** | `✖ …permissive policy carries the member test…` and `✖ the approval narrowings are RESTRICTIVE…` |
| Q10 | fixture: version 2 of `fixture-a1-business` loaded `false` (both versions agree on `enabled`) | noticed | **NOTICED** | `✖ the batch 090 fixture writes only catalog identities and pins the rows its cases need` |
| Q11 | `approval_policies_update_manager`: role test removed from the WITH CHECK half, USING kept | missed | **MISSED** | — |
| Q12 | case [76] `owner-a-cannot-write-an-approval-event` relabelled `deniedBy: 'grant'` → `'policy'` | unknown | **MISSED** | — |
| Q13 | apply-time block: `every_role` narrowed from six roles to `array['authenticated']` | missed | **MISSED** | — |

**Tally: 13 probes run, 4 noticed, 9 missed.** Every prediction held, including the one I marked
unknown.

What the nine establish, probe by probe, and what would catch each elsewhere:

- **Q1, Q2 — the child narrowings' scope branch is guarded by two substrings.** The rule asserts
  `/app\.content_items/` on the request halves and `/app\.approval_requests/` + `/app\.content_items/`
  on the event halves; both survive a `true`. The live suite would also stay green (F1): the
  referenced tables' own RLS produces the same rows. So the batch's most-argued property — the
  chain — is held by nothing that reads meaning. The Author's probe 8 (join retargeted to
  `content_versions`) was noticed because it changed the substring; mine keep it.
- **Q3, Q4 — a table-level grant is invisible to `approvalGrantsFor`**, which matches only
  `grant <verb> (<columns>) … on app.<table> to <role>`. The role list the "TO authenticated" rule
  scans does not include `public`. The apply-time block WOULD catch both in CI
  (`has_column_privilege('authenticated', …, 'INSERT'/'UPDATE')` is true through PUBLIC, 090:812-869)
  — which is the right place, and also the place a static probe cannot reach. 080's Q4, one batch
  over.
- **Q5 — `/status = 'pending'/` still matches.** Live, [59] catches it: the re-decide lands,
  `RETURNING id` yields a row, `expectNoRows` fails — by the runner's first half, not by the witness
  (F2).
- **Q6 — nothing pins case [2].** The coverage-map rule pins six named cases; this is not one.
- **Q11 — the new "permissive policy carries the member test" rule covers SELECT and INSERT
  policies only**, and the "two UPDATE policies … each WITH CHECK carries its own role test" rule
  is written for `approval_requests` only. On `approval_policies` there is a single UPDATE policy,
  so today the missing WITH CHECK role test changes no behaviour — it is exactly the state the
  batch's own comment (090:548-554) warns becomes a hole the day a second UPDATE policy arrives.
- **Q12 — `deniedBy` on the append-only refusals is pinned by no rule.** 080 pins `anon` cases'
  `deniedOn` and a few named cases' `deniedBy`; the eight event write refusals are unpinned. Live,
  the relabel fails: the message is `permission denied …`, layer `grant`, and `expectDeniedBy`
  refuses `policy`.
- **Q13 — 080's Q1, unchanged.** The 090 block has no count over `any (every_role)` at all, so the
  cheaper control 080 added after its probe 18 is absent here, and the narrowing guts the DELETE
  assertion, the append-only assertion and both column-allowlist assertions in one edit while the
  suite reads 249/249.

**What went right.** Q7 is the rule the Author added after its own probes 18 and 19, and the count
of eight is what bites — a `using (true)` policy beside the member one is noticed even though its
predicate is never read. Q8, Q9 and Q10 are real controls; Q10 in particular pins the fixture
property F3 says no case asserts.

---

## 7. What a hand-simulation cannot establish, and what I did not test

- **I executed no SQL.** §§3–4 are reasoning. Run 34754553767 confirms that each of the 83 cases
  produces the outcome it demands against PostgreSQL 17 and that the partition in §3.2 is right; it
  says nothing about outcomes the cases do not assert. Specifically, where I reason that RLS applies
  to the tables referenced inside a policy subquery (F1, Q1, Q2), that is documented rewriter
  behaviour (`fireRIRonSubLink`; the `infinite recursion detected in policy for relation` error
  exists because of it) and 080's F2 reasoned the same way — but no run in this repository has
  applied the Q1/Q2 mutation to a database, and the live claim stays unmeasured.
- **The apply-time `do $$` block is verified by CI applying it and raising nothing**, which a
  gutted block also does (Q13). Its assertions that cannot fire on a correct tree — the `expired`
  check against `polwithcheck`, the `conkey` degree, the both-ways append-only — are still owed a
  mutated migration pushed to CI, as the Author's §4/4 says. I did not do that: it needs a push,
  and this run does not push.
- **The WITH CHECK halves of all three narrowings are inert on `approval_events`** (no write
  grant), and on the other two tables they are exercised only by the positive writes [14], [15],
  [21], [42], [43], [49], [50], [53], [54] — every one of them by an identity the narrowing admits.
  No case attempts a write the membership policy admits and the narrowing refuses (a page-scoped
  editor raising a request on the sibling-page item, say), so the narrowings' WITH CHECK halves are
  never the deciding predicate in any write case.
- **No identity in the suite is the table owner or `app_command`**, so FORCE and F5 are guarded by
  assertion only.
- **I did not test the 629 pre-existing cases** or the five 082 cases on `main`; I checked only that
  090 removed none (0 removed).
- **I did not run `make db-verify`, `make db-migrate-clean`, `make db-rls-smoke`** or any live
  target.
- **I did not review** the nine open blockers the batch adds, the §4.7/§5 precedence argument, the
  read-surface reading (§8.3 has no SELECT row), the handoff, `evidence/VERIFICATION.md`, the
  `foundation-contract` changes beyond the list position in F9, or anything outside batch 090.
- **My thirteen probes are not a coverage measurement.** Nine were chosen because I expected them
  to slip.

---

## 8. Disposition

Nothing in this document advances `WP-0A-DB-00`. `status` stays `in_progress`; no gate in
`review_and_test_gates` is satisfied by it; `test_verified` is not claimed; the manifest's
`ownership.branch`, the author handoff and every other artifact are untouched.

**What this file signs:** that the eighty-three isolation cases batch 090 adds demand the outcomes
PostgreSQL would produce, on the layers they declare, against the fixture as loaded; that the CI
partition 14/17/3 is the partition the cases' own declarations produce; that every witness compares
a type the driver returns; and that the static suite's strength against the thirteen reversals above
is 4 of 13.

**What it does not sign:** that the tree containing 082 produces the same numbers (F9); that the
chain property the batch argues for is held by anything that reads meaning (F1, Q1, Q2); that the
service path on the three approval tables is closed (F5 — A1's); or anything about the batch's
reading of §8.3.

Owed to the Author, in the order I would fix them: **F1** (four places state a mechanism the
database does not have), **F2** (a false sentence about a witness), **F6** (a count nobody counted),
then **F3**, **F8**, and the Q3/Q4/Q11/Q12/Q13 spellings from **F7**. Owed to `/claude/a1_bastion`:
**F5**. Owed to the Integration Owner: **F9**.
