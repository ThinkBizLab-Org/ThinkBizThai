# Batch 041 — reversal probes, and the one they found

Run: `/claude/a2_knowledge` (A2 Knowledge), Author of batch `041`, 2026-09-06.
Branch: `agent/claude/WP-0A-DB-00-batch-041`.
Method: apply ONE reversal to a committed-shape file, run
`node --test tests/db/identity/identity-isolation.test.mjs` (and `node scripts/db/run.mjs schema-lint`
where the reversal is in the migration), record which named test refuses it, restore the file. The
script that does this is not committed; every step it takes is written out below and is
reproducible by hand.

This is the same shape batch `040` used, for the reason `040` recorded: a green suite is not
evidence until it can be made to go red on purpose, and the probes are what turn "these tests check
the batch" into "here is the edit each one refuses".

---

## What the probe round found, before the results

**A test that asserted a constant against itself.** The first version of the totality guard read:

```js
for (const guarded of ['item_business_profile_id is not null', …]) {
  assert.ok(RESOLUTION_BODY.includes(guarded), …);
}
```

`RESOLUTION_BODY` is the expected value declared five lines above it. The loop therefore compared
the constant with itself and would have passed against **any** migration, including one with no
function in it at all. It was invisible in a green run and it is exactly what the probes exist to
surface: the loop's three assertions contributed nothing, and the whole predicate rested on the
single `assert.equal` beside them.

That also exposed a **concentration**: probes P1, P2 and P3 — three independent reversals of the
predicate, each with a different failure mode — were all noticed by that one assertion. Deleting one
line would have taken all three notices with it, which is `040`'s own scar (`USING` and `WITH CHECK`
are two catalog columns, and one assertion over the whole policy body could not tell them apart)
arriving one batch later in a different shape.

**Fixed in the same change.** The section is now three tests instead of one: the whole-body pin, a
clause-by-clause test that reads the FILE (three branches, three null guards, and the eight
apply-time raises that carry the behavioural half), and the `search_path` argument with its premise.
P1–P3 are now each refused by two independent tests.

---

## Results

Every probe below was refused. `exit 1` is `node --test`'s.

| # | reversal | refused by |
|---|---|---|
| P1 | the business-level branch of the predicate is deleted (`item_page … is null` becomes `is not null`) | `the resolution predicate is pinned character for character…` **and** `the predicate asks both halves of the two-column scope…` |
| P2 | a page-level row is made to apply to every Page (`= in_page…` becomes `is not null`) | the same two |
| P3 | the totality guard is removed, so the predicate is three-valued | the same two |
| P4 | the predicate is made `SECURITY DEFINER` | `the resolution predicate names no object…` **and** `db-schema-lint` exit 1 — *"app.knowledge_scope_applies is SECURITY DEFINER without an empty search_path — §8.5"* |
| P5 | `anon` is granted `EXECUTE` on the contract | `EXECUTE is revoked from PUBLIC and granted only where batch 041 says there is a caller` |
| P6 | `041` adds a sixth inherited base-table grant (`grant select (name) on app.knowledge_items to authenticated`) | `batch 041 adds no client base-table grant, so the closed exceptions list still names five batches` |
| P7 | `041` creates the view its own registry row names (`create view app.resolved_business_knowledge … security_invoker = true`) | `batch 041 creates one function and no table, no view and no policy` |
| P8 | the apply-time truth table stops checking the sibling-Page leak (`raise exception` → `raise notice`) | `the predicate asks both halves of the two-column scope…` |
| P9 | the apply-time truth table stops checking the `NULL` cell | `the predicate asks both halves of the two-column scope…` |
| P10 | one of the four filter-not-permission cases is renamed away | `the resolution contract narrows what row level security admits and never widens it` |
| P11 | a predicate negative loses the paired case that gives it meaning | `every predicate negative is paired with a statement that differs in exactly one argument` |
| P12 | a predicate negative is quietly re-pointed at the request that WOULD return the row | `every predicate negative is paired with a statement that differs in exactly one argument` |
| P13 | a negative-control entry is attributed to batch `041` | `batch 041 owes no CI negative-control entry…` **and** `the tables batch 040 adds have their own entries in the CI negative control` |

After the last probe the tree was restored and `node --test` reported exit 0 with no failing test,
which is recorded here because a probe round that left an edit behind would be indistinguishable
from one that found nothing.

---

## What these probes CANNOT reach, stated rather than left to be assumed

**Nothing here executed SQL.** This host has no `psql`, no `postgres` and no container runtime
(checked: `which psql postgres pg_ctl docker initdb` finds none), and batch `041` is declared
`not_applied_to_this_instance` in `db/foundation/lint/catalog-snapshot.json` because its apply-time
block reads `app.knowledge_items`, which the provisioned instance does not have. So:

* **`041_knowledge_resolution.sql` has not been parsed by a Postgres.** Every claim above about the
  migration is a claim about its TEXT. CI is the proof surface: `make db-migrate-clean` applies all
  twelve batches to a `postgres:17` container on every pull request, and that is where the function
  body, the `do $$` block and the nine-call truth table are first executed.
* **The fifteen new isolation cases have not been RUN.** They are executable specification until
  `make db-rls-smoke` runs them in CI. Batch `040` learned this the expensive way — a case using
  `__SELF__` under an identity with no JWT subject inlined the text `'undefined'` and came back
  `22P02`, invisible to every static rule — so the claim here is deliberately narrow: the cases are
  well-formed, they are pinned by the static suite, and whether they PASS is CI's answer and not
  this file's.
* **P4's `schema-lint` result is a text rule, not a catalog one.** It reads the migration's own
  words. The catalog half — `prosecdef`, `provolatile`, `proconfig` read from `pg_proc` — is in the
  migration's apply-time block and runs only where the migration runs.

**No measurement of a query plan.** The migration's header argues that a `SET` clause would make the
predicate opaque to the planner's inliner, and says in terms that this is a design reason and NOT a
measurement. Nothing here measured a plan, and nothing here claims one.

---

## Counts

* Isolation cases before: **209**. After: **224** (+15).
* `tests/db/identity/identity-isolation.test.mjs` tests before: **67**. After: **80** (+13);
  assertions 449 → 515. Both floors in `scripts/test-suite-contract.mjs` are RAISED, and the name
  digest moves with them. (The thirteenth arrived after this round: the citation check in the
  addendum below, added when the header turned out to cite `:210` for a rule that is at `:217`.)
* Of the fifteen new cases, **four** are refused by row level security while the predicate admits
  the row — the cases the CI negative control for `app.knowledge_items` now additionally rests on —
  **three** are refused by the predicate and would return nothing with row level security switched
  off, **seven** are positives, and **one** is an anonymous refusal at the schema.
  The three predicate negatives are named in `.github/workflows/ci.yml` as NOT part of the control's
  basis, because a control credited with cases that pass while row level security is off is the
  defect the per-family rewrite fixed one level down.

---

## Addendum, 2026-09-07: CI turned two derived claims into measured ones

The probe round above ran on a host with no Postgres, and said so. CI has since run the branch and
two of this batch's claims stop being derivations.

Runs, all `bootstrap` on `ThinkBizLab-Org/ThinkBizThai`:

| head | run | result |
|---|---|---|
| `25a4891` | 34047402788 | success |
| `6dbea87` | 34048625836 | success |
| `a622b7a` (main, batch 040) | 34043646171 | success — the baseline the numbers below are read against |

**1. The migration parses, applies, and its truth table holds against a real Postgres.** Both branch
runs print `applied 041_knowledge_resolution.sql` after the eleven batches before it, which means the
function body compiled, the `do $$` block parsed, and every one of the nine truth-table calls plus
the ACL sweep and the two-column-scope check returned without raising. None of that could be
asserted from the authoring host, and the probe round said so.

**2. The negative-control claim was a derivation and is now a measurement.**
`.github/workflows/ci.yml` says batch 041 adds four cases that fail with row level security disabled
on `app.knowledge_items`, and that its other three negatives — the ones the PREDICATE refuses — are
NOT part of that basis. Read from the control step's own output:

```
main (batch 040):    app.knowledge_items (040): 24 case(s) noticed
this branch:         app.knowledge_items (040): 28 case(s) noticed
main (batch 040):    app.knowledge_item_versions (040): 9 case(s) noticed
this branch:         app.knowledge_item_versions (040): 9 case(s) noticed
```

**24 → 28 is exactly +4**, against +7 new `no-rows` cases matching that entry's pattern. The three
predicate negatives did not move the number, which is what the comment beside the entry claims and
is the half a reviewer would otherwise have had to take on trust. And
`app.knowledge_item_versions` is **unchanged at 9**, which is the other half: batch 041 asserts
nothing about the version table, so an entry that had grown there would have meant a case was
crediting a control it does not test.

`db-rls-smoke: 224 isolation case(s) passed` on both branch runs; 209 on main.

**What is still not measured.** The query-plan claim in the migration's `search_path` argument. It
was a design reason on 2026-09-06 and it is a design reason now.
