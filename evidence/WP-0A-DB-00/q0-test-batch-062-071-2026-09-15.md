# Q0 independent test — batches 062 and 071 (the Owner's row (b) closures), open as PR #145

Run: `/claude/q0_sentinel_062071`
Role: independent Tester, one of the same-vendor role runs Product Owner decision Q1/Q6
(`product-owner-disposition-2026-09-15-six-questions.md`) made countable.
Subject: branch `origin/agent/claude/WP-0A-DB-00-closures-062-071`, commits `f01d99e`, `c503594`,
`ded29c2` (head), branched from `main` = `1a80f43` (merge of PR #144). PR #145, not merged. Read
from the remote-tracking ref with `git show` and `git diff main...`; never checked out.
Base for this run: `main` = `1a80f43`.
Date: 2026-09-15.

**This document RECORDS TEST RESULTS.** It advances no package status, writes `test_verified`
nowhere, signs nothing on any author's behalf, and repairs nothing it found. Every defect named
below is left standing on the batch branch.

---

## 0. What I am, before anything else

**I am a subagent spawned by `/claude/a0_atlas`, the Author of this package and the author of the
two files under test, and I run in the same vendor and model family.** A0 wrote the three briefs I
was handed and chose what to point me at: RFC-2026-022 §3's table, the two tables' grants and
policies, the two cases, the CI partition, the witness types, a reversal round against the static
suite, the pre-080 role-run findings, and a scratch PostgreSQL on port 5533. Under Q1 of the
2026-09-15 disposition a distinct same-vendor run in a named role counts as that role's signature;
this run is distinct from A0. What that does and does not buy:

- **Every verdict below is a claim against the tree or against a database, with a `file:line`, a
  command, or a run id beside it**, so a reader re-runs it rather than trusting me. A shared model
  does not change what `061_metering.sql:1000-1007` grants or what `pg_policy` holds.
- **Where I contradict the Author's record, I say so and show the measurement.** F1 says the two
  headers name what is left open and not what is closed; F2 says the apply-time block can still be
  emptied with the static suite green, one round after that gap was recorded as fixed.
- **Where the briefs were wrong I followed the tree.** `brief-common.md` says "No PostgreSQL is
  available on this machine. Say so." That is false, and the subject override says so: Homebrew
  `postgresql@17` 17.11 is installed and every runtime claim below was **measured** on a scratch
  cluster I created on `127.0.0.1:5533` (§3), never on the user's server at `/tmp:5432`, which I
  did not connect to. One more thing the override did not say: a cluster directory inside the
  worktree makes `npm run verify` read `NOT clean: exit 71` (`scan-repository-secrets.mjs` reports
  986 `unscannable: undecodable-file — pg-scratch/…` findings), so the verify line in §1 was
  measured after the cluster was stopped and its directory removed. The brief-tester text is
  written for a batch `<NNN>` with a PR run id from another batch; I read PR #145's own run.
- **Framing is the cost I cannot pay from inside.** A0 chose the questions. The findings that came
  from outside the brief — F1's reading of 061's own grant comment, F2's `return;` probe — came
  from asking what the closed tables were *for* and what the static rule reads, not from anything
  I was pointed at. There are probably others.
- Pressure from the coordinator, in either direction, is not evidence, and none was applied.

---

## 1. The declared commands, verbatim

Run on `agent/claude/WP-0A-DB-00-q0-test-062-071`, made from `main` at `1a80f43`, in this
worktree, with `npm ci --ignore-scripts` (exit 0) first. Node `v24.20.0`, npm `11.19.0`.

| Command | Exit | Output line |
|---|---|---|
| `npm run verify` (this branch, before this file; cluster removed) | **0** | `clean: exit 0 — tests 643, pass 643, fail 0, skipped 0, todo 0` |
| `npm run verify` (this branch, with this file) | **0** | `clean: exit 0 — tests 643, pass 643, fail 0, skipped 0, todo 0` |
| `node --test tests/db/identity/identity-isolation.test.mjs`, `main` files | 0 | `ℹ tests 285` · `pass 285` · `fail 0` |
| the same, branch files copied in (§3) | 0 | `ℹ tests 285` · `pass 285` · `fail 0` |
| `node --test test-kits/db/foundation-contract.test.mjs`, `main` / branch files | 0 / 0 | `tests 60, pass 60` both |
| `buildCases()` on `main` / with the branch's `isolation-cases.mjs` | — | **854** / **856**, 0 duplicate ids |

So the branch adds **2** cases and **0** static tests (`identity-isolation.test.mjs` gains two
rows in `SERVICE_PATH_CLOSURES` and two comment lines, `:9687-9694` on the branch), which matches
the PR body, the handoff, and the numbers the override brief told me to expect: 856, 2-of-856, 643,
285, 60. **I read nothing else.** `npm run check:scope` is not cited (it is not evidence).

**CI.** PR #145's run is [34923143406](https://github.com/ThinkBizLab-Org/ThinkBizThai/actions/runs/34923143406)
on head `ded29c2`, conclusion `success` (it was `in_progress` when I started and completed while
§3 ran). Its log prints `applied 062_metering_service_path_closed.sql`,
`applied 071_research_service_path_closed.sql`, `db-migrate-clean: ok in 1934ms`,
`db-schema-lint: ok in 60ms`, `db-rls-smoke: 856 isolation case(s) passed.`,
`ℹ tests 643 / pass 643 / fail 0`, and the two negative-control lines quoted in §4.2. The job
checks out `${{ github.head_ref }}` (`ci.yml:54`, Q4's fix); it does not run `npm run verify`, so
the verify line above is this machine's, on the branch name.

---

## 2. The Owner's row (b), checked against the tree

The question the override brief puts is not whether to close but whether the Author closed
**exactly** the tables row (b) names — "the narrowed tables that are NOT `S` cells in
RFC-2026-022 §3" — and nothing else. Four legs, all measured on the tree and on the catalog (§3).

**Leg 1 — which tables are narrowed.** A narrowing is a RESTRICTIVE policy `for all to
authenticated`. Metering: one, `quota_buckets_scope_narrows_member` (`061:1046-1057`);
`usage_events` and `usage_reservations` carry **no policy at all** (`061:1059-1060`; catalog P-A1:
0 policies each). Research: four, `research_runs_scope_narrows_member` (`070:1286-1289`),
`research_sources_…` (`:1316-1319`), `research_evidence_…` (`:1360-1363`),
`research_suggestions_…` (`:1399-1402`); `research_snapshots` carries none (P-A1). So the
candidate set is five tables: `quota_buckets`, `research_runs`, `research_sources`,
`research_evidence`, `research_suggestions`. **Confirmed.**

**Leg 2 — which of those are `S` cells.** RFC-2026-022 §3's table
(`architecture/decisions/RFC-2026-022-service-policy-shape.md:173-183`): row `070` is "Research
run/source/evidence INSERT — CARRIED"; row `061` is "Usage ledger INSERT (`S/N`) — CARRIED". The
matrices behind them: §8.2 "Research run/source/evidence INSERT | N N N N N | **S**"
(`docs/sprint-0a/sprint-0a-core-erd-rls-retention-th.md:359`) and §8.4 "Usage ledger
INSERT/UPDATE/DELETE | N N N N N | **S/N**" (`:398`); the ledger is `usage_events` (`061:1059`,
`service-policy-map.json:31-34`). `research_suggestions` sits in §8.2's "Suggestion
save/dismiss/use | Y Y Y P N | **P**" (`:360`); `quota_buckets` in §8.4's "Usage/quota summary
SELECT | Y Y P N N | **P**" (`:397`, the row 061 reads as its own at `061:1018-1020`). Neither is
`S`; `service-policy-map.json` has rows for `usage_events`, `research_runs`, `research_sources`,
`research_evidence` and none for the other two (`:31, :63, :71, :79`). So the not-`S` narrowed
tables are exactly **`quota_buckets`** and **`research_suggestions`**, and the `S`-cell tables
that must stay open are `usage_events` (not narrowed anyway), `research_runs`, `research_sources`,
`research_evidence`. **Confirmed — the Author closed the right two and only those** (P-A: two
`*_service_path_closed` policies in the two families, on those two tables; 0 on the other six).

**Leg 3 — grants and every permissive policy's TO clause.** `061:1000-1007` grants `app_worker`
column-scoped SELECT, INSERT and UPDATE on `quota_buckets`; `070:1221-1226` grants it
column-scoped SELECT and INSERT on `research_suggestions` (no UPDATE — catalog R-H `w_upd = f`).
`app_command` and `app_maintenance` are granted nothing by either batch (`061:1009-1011`,
`070:1228-1230`) and hold no USAGE on schema `app` (P-F). Every permissive policy on the two
tables names `authenticated` alone: `quota_buckets_select_owner_or_admin` (`061:1027-1030`),
`research_suggestions_select_active_member` and `_update_writer` (`070:1383-1397`); catalog on
`main`: `polroles = {authenticated}` on all five pre-existing policies, `{0}` on the two closures
only. The two headers state the grants correctly (`062:25`, `071:25`) and attribute them to the
right batch (`062:3`, `071:3`) — C0-pre-080 L4 does not recur. **Confirmed — S8's shape.**

**Leg 4 — the closures are 082's shape.** Text: `062:34-38`, `071:34-38` are `as restrictive /
for all / using (current_user = 'authenticated') / with check (…)` with no `TO`; the two files
differ from each other only at `:25`, `:32` and `:119` (family text) — measured by `diff` after
substituting the table and batch names. Catalog (P-A): `polpermissive = f`, `polcmd = '*'`,
`polroles = {0}`, both halves `(CURRENT_USER = 'authenticated'::name)`. The dead `service_roles`
constant C0-pre-080 L3 found is gone (`grep -c service_roles` → 0 in both; still 1 in `101`).
The `covers` tags cite the family's own matrix section — `§8.4/service-P-closed` (`:12866`),
`§8.2/service-P-closed` (`:12879`) — so A1-pre-080 F3 / C0-pre-080 L2 do not recur. **Confirmed.**

**One thing the four legs do not say and I checked anyway (F1).** In both families the table
row (b) closes is the one whose *service write* the family's own batch designed: `061:995-999`
says of the worker's UPDATE grant on `quota_buckets` "This grant IS the answer to 'who is
permitted to move the derived one'", `061:124-126` names batch 132 as that writer; `070:925` and
`:976` say a run *proposes* a suggestion and `070:1224-1226` gives the worker the INSERT a
producer needs; the existing cases say the same (`isolation-cases.mjs:8814-8828`,
`:11147-11158`). Neither is an `S` cell — §8 has no row for writing a bucket or proposing a
suggestion, and the tree's own reading is "denied by default" — so row (b) selects them
correctly by its letter. What the two headers do not say is that the path they close tomorrow is
the one 061 and 070 were built to open.

---

## 3. What the database said

**Scratch cluster.** `initdb` (PostgreSQL 17.11, `--locale=C`, trust) into this worktree
(`pg-scratch/`), started on `127.0.0.1:5533` with no unix socket, database `thinkbizthai_test`,
shim `db/foundation/ci/supabase-shim.sql` applied, then
`DB_TEST_URL=postgresql://postgres@127.0.0.1:5533/thinkbizthai_test` with `LC_ALL=C TZ=UTC
PGTZ=UTC`. Re-`initdb` before every fresh apply (001 creates cluster roles). The branch's six
files (`062`, `071`, `isolation-cases.mjs`, `identity-isolation.test.mjs`,
`catalog-snapshot.json`, `foundation-contract.test.mjs`) were copied into the worktree with
`git show origin/…:<path> > <path>`, verified byte-identical to the branch by SHA-256 after every
probe round (`062` = `edf6665d…`, `071` = `6025dfbe…`), and restored with `git checkout --` /
deleted before this file was committed. The cluster was stopped (`pg_ctl stop -m fast`; nothing
listens on 5533) and its directory removed before the commit. **Nothing here touched `/tmp:5432`.**

### 3.1 The declared targets, with and without the two files

| Set | `migrate-clean` | `schema-lint` | `rls-smoke` |
|---|---|---|---|
| `main` (`1a80f43`) | `ok in 833ms` | `ok in 28ms` | `854 isolation case(s) passed.` |
| branch (all files) | `applied 062_…`, `applied 071_…`; `ok in 863ms` | `ok in 28ms` | **`856 isolation case(s) passed.`** |
| the same **without** the two closure files, fresh cluster | `ok in 928ms` | fails on the branch's snapshot (`not_applied_to_this_instance names 062_… which is not in db/foundation/migrations`) | **`FAILED — 2 of 856 case(s)`** |

The two failures are exactly the two new ids, each `(assert): … nothing was visible`; no other
case moves in either direction. This is the Author's §2 measurement, reproduced.

### 3.2 The S8 exploit, both tables, read and write (P-C, P-D, R-C, R-D)

Inside rolled-back transactions, a permissive worker policy is created and the closure is
present, then dropped:

| Table | fixture rows | worker SELECT, closure present | closure dropped |
|---|---|---|---|
| `quota_buckets` | 4 (2 tenants) | **0** | 4, in 2 tenants |
| `research_suggestions` | 2 (2 tenants) | **0** | 2, in 2 tenants |

**The write half — `quota_buckets`**, where the worker already holds INSERT and UPDATE
(`061:1000-1007`):

| Configuration | UPDATE `consumed_amount = 0` (whole table) | INSERT a bucket for tenant A |
|---|---|---|
| D1. branch as it is, no worker policy | `UPDATE 0`, no error (USING filters) | `new row violates row-level security policy for table "quota_buckets"` — **unnamed** |
| D3. + a worker `for all … using (true) with check (true)`, closure present | `UPDATE 0` | `… policy "quota_buckets_service_path_closed" for table "quota_buckets"` — **named** |
| D4. closure dropped, worker policy kept | **`UPDATE 4`** — 5 rows zeroed across **2 tenants** after the insert | **`INSERT 0 1`** |

**`research_suggestions`**: D1 unnamed refusal; the worker's UPDATE is a grant refusal
(`permission denied for table research_suggestions`) with or without the closure; D2 (worker
INSERT policy, closure present) named refusal `"research_suggestions_service_path_closed"`;
D3 (closure dropped) **`INSERT 0 1`**, the probe row landed in tenant A. Rows 1 and 2 are the
headers' "changes nothing observable" (`062:25-30`, `071:25-30`), measured; D3/D4 are S8 and its
closure, on the write the worker was granted.

**Other roles (P-F, q-re).** `app_command`, `app_maintenance` and `anon`: `permission denied for
schema app` before any table privilege or policy is consulted. `app_maintenance` given USAGE,
SELECT and a permissive policy: **0** with the closure, 4 without — 082's "deliberately refuses a
future maintenance sweep" holds here.

**`authenticated` is unchanged (P-E, R-E).** Owner A sees 3 buckets with the closure and 3 with it
dropped; admin A sees 2 (021's narrowing, unchanged). Editor A (`a324d4a6-…`) saves suggestion
a1 through `research_suggestions_update_writer` with the closure present (`UPDATE 1`,
`saved = t`) and with it dropped (`UPDATE 1`); the page-scoped editor (`c31e3c84-…`) likewise.
`current_user` inside every one of these is `authenticated` (measured beside each probe). The 856
green cases are the measurement that every `authenticated` case is unchanged.

---

## 4. Hand-simulation of the two cases, against what the database then said

### 4.1 The cases

Both are one template (`isolation-cases.mjs` on `ded29c2`, `:12864-12889`): `as: ownerA`
(`as_user` → role `authenticated`), `sql: SERVICE_PATH_CLOSURE_ON` (`:15421-15425` on the branch,
`:15395-15399` on `main`, unchanged — it already reads the predicate since Q0-pre-080 F1/F2 were
applied there), one
table in `params`, `expect: 'rows'`, no `deniedBy`, no `deniedOn`, no witness. The statement asks
`pg_policy` for a row on `('app.' || $1)::regclass` that is restrictive, `FOR ALL`, `TO PUBLIC`,
with `polqual = '(CURRENT_USER = ''authenticated''::name)'` and `polwithcheck = polqual`.

| Line | Case | Table | Layer the case names | What decides | Verdict |
|---|---|---|---|---|---|
| 12865 | `batch-062-closes-the-service-path-on-the-bucket-table` | `quota_buckets` | none (a catalog read) | `pg_catalog` is readable by every role; the closure row exists with all five properties (P-A) | right |
| 12878 | `batch-071-closes-the-service-path-on-the-suggestion-table` | `research_suggestions` | " | " | right |

**Outcome**: PostgreSQL returns one row per case on the branch (P-B, R-B: `quota_buckets_service_path_closed`,
`research_suggestions_service_path_closed`, as `authenticated`) and zero rows against the set
without the file (§3.1). `expect: 'rows'` is right; there is no `deniedBy` to be wrong; the `why`
text — "fails against 061 alone and passes with 062", "app_worker holding grants no policy
admits", "no service cell that a closure would pre-empt", "the id names no family word (CI
control pattern)" — is true as measured (§2, §3.1, §4.2). **0 wrong, 0 wrong-layer.**

**Right-for-the-wrong-reason, in the sense the brief asks for.** Deleted, both fail (§3.1).
Altered — `alter policy … using (true) with check (true)` — the case returns **no row** (P-B,
R-B), and so does the predicate flipped to `'app_worker'` (P-B): the constant's predicate term,
added on `main` for the pre-080 set, is what makes these two cases stricter than 082's were when
Q0-pre-080 measured them. What still satisfies the case: the closure dropped and a differently
named restrictive `FOR ALL TO PUBLIC` policy with the same predicate created in its place (P-B:
the case returns `quota_buckets_anything`) — a name is asserted by the static rule and the
apply-time block, not by the case, and such a policy closes the same path, so this is a note (F3)
and not a hole. RLS disabled on the table: the case still returns the row (P-B, R-B), which is
§4.2.

### 4.2 The partition, and CI's count

A case changes outcome when a table's RLS is disabled exactly when it is `no-rows`, `no-effect`,
or `denied` with `deniedBy: 'policy'`. Both new cases are `rows` over `pg_policy`, and `alter
table … disable row level security` does not remove a policy from the catalog (P-B, R-B). So the
two are in **no control's basis** — the same position as the seventeen 022–101 closure cases.
Measured two ways:

- **CI**, run 34923143406 (branch) against run 34922808212 (`main` at `1a80f43`): the two
  negative-control lines are identical — `app.quota_buckets (061): 10 case(s) noticed, including
  admin-a-cannot-read-the-quota-bucket-outside-their-narrowing` and `app.research_suggestions
  (070): 8 case(s) noticed, including owner-a-cannot-see-the-research-suggestion-of-tenant-b`.
- **Here**, the control replayed in the step's own shape (`ci.yml:176-198`: disable one table,
  run the suite, re-enable, count `^  <id> [covers] (phase):` lines): `quota_buckets`
  `FAILED — 10 of 856`, `research_suggestions` `FAILED — 8 of 856`, the same first-named ids,
  `closure-cases-among-failures = 0` on both, and `856 passed` again with both re-enabled.

Neither id matches any of the 48 control patterns in `ci.yml` (checked programmatically against
the parsed list: 0 matches each; the patterns are `[a-z0-9-]*quota-bucket` and
`[a-z0-9-]*research-suggestion`, `ci.yml:509, :678`, and the ids say `bucket-table` and
`suggestion-table`), and the batch 061 rule that pins its three patterns' non-overlap
(`identity-isolation.test.mjs:6117-6120`) stays green (§1).

### 4.3 Witnesses, and the seven service cases the closures sit under

**No witness.** Neither case carries `witness`, `equals`, `deniedBy` or `deniedOn`, so there is
no CSV-type comparison to check; nothing here compares a boolean or a NULL.

The seven existing service cases on the two tables (`as: service` → `app_worker`), by outcome:
`no-rows` × 2 (`service-sees-zero-quota-buckets`, `service-sees-zero-research-suggestions`),
`denied`/`policy` × 2 (`service-cannot-create-a-quota-bucket`,
`service-cannot-propose-a-research-suggestion`, both `deniedOn` the table), `no-effect` × 1
(`service-cannot-rewrite-a-quota-bucket-total`, witness `state = "unchanged"` — a text column
compared as text, and measured `unchanged` as owner A with the closure dropped), `denied`/`grant`
× 2 (`service-cannot-retarget-a-quota-bucket`, `service-cannot-delete-a-quota-bucket`). **The
closures decide none of them** (P-G, R-G): the reads are zero rows with or without the closure,
the two policy-layer INSERT refusals are the unnamed always-false WITH CHECK in both
configurations (identical text), the UPDATE is `UPDATE 0` in both. Their `why`s say exactly that
("FORCE ROW LEVEL SECURITY with no service policy is what refuses it", `:8823`), so they are right
and stay right; the PR body's "the seven existing service cases … unchanged" is measured true.

---

## 5. Reversal probes

### 5.1 Against the static suite

Each probe rewrites one file in the worktree (branch state), runs
`node --test tests/db/identity/identity-isolation.test.mjs`, and restores the file from a pristine
copy (all four restored byte-identical, checked by digest); the harness reports NOT APPLIED if the
replacement matched nothing, and it reported 0 of those. Tally read from the `ℹ fail N` line and
the `✖` lines — node's spec reporter prints `✖` for a failure, not `not ok`. Baseline 285/285;
restored 285/285.

| # | Reversal | Static suite |
|---|---|---|
| P1 | 062: closure `for select` | NOTICED (shape rule) |
| P2 | 071: closure given `to app_worker` | NOTICED (shape rule + the two `S`-cell rules) |
| P3 | 062: USING half `current_user = 'app_worker'` | NOTICED |
| P4 | 071: the `create policy` statement deleted, comment and block kept | NOTICED |
| P5 | 062: claim 1 count `1` → `0` | **MISSED** |
| P6 | 071: claim 2 gutted, `where false and not exists …` | **MISSED** |
| P7 | 062: the whole `do $$` block replaced by a comment carrying its pinned sentences | NOTICED (Q0-pre-080 F3's fix: the rule now reads code) |
| P8 | 071: `grant select … to app_command` smuggled in | NOTICED |
| P9 | 062: an extra closure on `usage_events` (an `S`-cell table) | NOTICED (declared list) |
| P10 | cases: the 062 case repointed at `usage_events` | NOTICED |
| P11 | cases: the 071 case deleted | NOTICED |
| P12 | cases: the 062 case's `sql` swapped for `select 1 as polname` | NOTICED |
| P13 | cases: `SERVICE_PATH_CLOSURE_ON` loses `polroles = '{0}'` | NOTICED (Q0-pre-080 F2's fix: the constant is pinned) |
| P14 | cases: `SERVICE_PATH_CLOSURE_ON` loses its two predicate terms | NOTICED |
| P15 | 062: closure renamed `quota_buckets_service_closed` | NOTICED |
| P16 | 071: closure `as permissive` | NOTICED |
| P17 | 062: claim 3's role filter inverted to `<> 'app_worker'` | **MISSED** |
| P18 | suite: the 071 row deleted from `SERVICE_PATH_CLOSURES` | NOTICED (file on disk, no row) |
| P19 | suite: 062's declared list changed to `['usage_events']` | NOTICED |
| P20 | 062: the file emptied to a comment | NOTICED |
| P21 | cases: the 062 case's `covers` tag changed to `§8.5` | **MISSED** (no rule validates tags — known) |
| P22 | 071: claim 4's FORCE check inverted | **MISSED** |

**Honest tally: 22 run, 17 noticed, 5 missed** by the static suite. Sixteen of the seventeen
catches are one rule (`every service-path closure on disk is declared, and every declared closure
has 082's shape`), so a defect in that rule's regex hides sixteen probes at once; the pre-080
Tester said the same of its twelve. The three pre-080 findings A0 applied on `main` show up as
catches here: P7 (F3), P13 and P14 (F1/F2).

### 5.2 Against the database — the apply-time block, on a fresh cluster each

| # | Reversal | `migrate-clean` |
|---|---|---|
| D5 | 062: claim 1 count → 0 | **FAILED** — `batch 062 finds 1 service-path closures and there must be one per table, 1 (P0001)` (raises on a correct catalog) |
| D17 | 062: claim 3's filter inverted | **FAILED** — `a permissive policy on a closed table admits a role other than authenticated: quota_buckets_select_owner_or_admin on app.quota_buckets admits authenticated (P0001)` |
| D22 | 071: claim 4's FORCE check inverted | **FAILED** — `table(s) research_suggestions lost ENABLE or FORCE ROW LEVEL SECURITY (P0001)` |
| D2 | 071: closure `to app_worker` | **FAILED** — `… names a list of roles rather than PUBLIC, and binds only that list (P0001)` |
| D16 | 071: closure `as permissive` | **FAILED** — `… is PERMISSIVE, and a permissive closure widens instead of closing (P0001)` |
| D6 | 071: claim 2 gutted (`where false and not exists`) | **ok** — applies clean, asserts nothing |
| D7b | 062: the block's body made `return;` before claim 1 | **ok** — applies clean; static suite (P7's rule) reads the raise sentence in code and is satisfied |
| D9 | 062: an extra closure on `usage_events` | **ok** — claim 1 counts closures on `closed_tables` only; the static rule (P9) is the only thing that notices |

Of the five the static suite missed, the database caught three (D5, D17, D22 — assertions turned
false, which is loud) and missed two (P6/D6, P21 — one silent, one a tag). D7b is a probe the
pre-080 round did not run: the code-reading rule requires the `raise exception` sentence to be
present in code, not to be reachable (F2).

---

## 6. Findings

Severity: HIGH — the batch or its premise says something untrue that a decision rests on;
MEDIUM — a claim the tree contradicts, or a control that reads as present and is not; LOW —
imprecision, inheritance, hygiene; INFO — record only. No finding here is stop-the-line (§7).

### F1 — LOW (record). The headers name what is left open and not what is closed: in both families row (b) closes the table the family's own batch designed a service write for

`062:32` and `071:32` ("LEFT OPEN ON PURPOSE") name the `S`-cell tables and why they stay open.
Neither header says that the table it closes is the family's designed service-write target:
`061:995-999` calls the worker's UPDATE grant on `quota_buckets` "the answer to 'who is permitted
to move the derived one'" and `061:124-126` names batch 132 as that writer; `070:925`/`:976` say a
run proposes a suggestion and `070:1224-1226` is the producer's INSERT. Measured (§3.2 D3, R-D2),
that writer, once it has a permissive policy, meets the **named** closure and must bring shape B
(RFC-2026-023, `Status: In review`, `:3`) first. That is what shape C is for and the Owner chose
it with the grants in view (the map's §2 rows for these two families state them correctly,
unlike the three pre-080 rows A1/C0-pre-080 corrected); it is recorded so that "changes nothing
observable today" (`062:25`) is not read as "closes nothing anybody planned to use". Owed to A0:
one sentence in each header and in `a0-batches-062-071-row-b-closures-2026-09-15.md` §1. Not
repaired.

### F2 — LOW (inherited, half-closed). The apply-time block can still be emptied with the static suite green

Q0-pre-080 F3 recorded that a comment carrying the block's two sentences satisfied the rule; A0's
fix (`identity-isolation.test.mjs:9730-9734` on `main`, "read these from CODE, not raw") catches
that (P7 NOTICED, P20 NOTICED). D7b — `return;` as the block's first statement — leaves every
pinned sentence in code and unreachable: 285/285, `migrate-clean: ok`. D6 (claim 2's `where`
prefixed `false and`) likewise. The rule reads for presence, not reachability; the loud
assertions (D5, D17, D22) are caught only because they turn true into false. Inherited by every
closure file; recorded here because the shape is now nine files wide.

### F3 — LOW (inherited). Two catalog cases prove a closure of the right shape exists; nothing in the suite exercises the refusal, and the case does not read the name

C0-pre-080 L1, unchanged: the refusal a closure produces (§3.2 D3/D4, R-D2/R-D3) is measured only
by hand — the Author's replay in the evidence file §2 and mine here. New in this round: the
case reads the predicate (Q0-pre-080 F1 applied), so an `alter policy … using (true)` is now
caught by the case itself (P-B), but a closure dropped and re-created under another name with
the same shape still satisfies it (P-B last row). Harmless — such a policy closes the same path —
and the file-side name is pinned by the static rule (P15) and the block (claim 1's `like`). A
statement about what "856 passed" proves, not a defect in what the batch claims.

### F4 — LOW. The files are still generated outside the repository, and it still shows

C0-pre-080 L7 recurs: `062:32` is 258 characters, `071:32` is 382, `062:136`/`071:136` 216,
`062:25`/`071:25` 168/167 — template-expanded lines against 082's longest of 208. The Author's
§3 records the generator's escape bug fixed "in the generator", which is still
`gen-closure.mjs` in a scratchpad nobody can read from the tree. Also `062:7` and `071:7` cite
the S8 map "§3, row 2"; the map's §3 is a bullet list and the reference is to its second bullet.
Hygiene; not repaired.

### F5 — INFO. An extra closure on an `S`-cell table applies clean; only the declared list notices

D9: a `usage_events_service_path_closed` added to 062 applies (`migrate-clean: ok`) because
claim 1 counts closures on `closed_tables` only and claim 2 asks nothing about `usage_events`.
The static rule refuses it (P9: "one closure per declared table") and would refuse it on the
day somebody writes it. Recorded because closing an `S`-cell table is the one thing row (b)
forbids, and the runtime guard does not know it.

### F6 — INFO. The handoff on the branch cites no CI run yet, and PR #145 carries one check

`handoffs/WP-0A-DB-00-author-handoff.json` on `ded29c2` records the verify line and the scratch
measurements and no `actions/runs/…` id (the run finished after the handoff was refreshed).
RFC-2026-002 has the handoff record PR URL, head SHA and CI run before the Owner merges. A0's
artifact, refreshed by A0; noted so it is not carried into the merge without the id. No
truncated sentence this time (C0-pre-080 L6 / A1-pre-080 F4 do not recur — grepped).

### F7 — INFO. Two readings on `main` of whether `quota_buckets` has a §8 row

`service-policy-map.json:34` says "app.usage_reservations and app.quota_buckets have none at
all, because §8 has no row for either in any of its four matrices"; `061:1018-1020` reads §8.4's
"Usage/quota summary SELECT" (Service `P`) as `quota_buckets`' row, and the new case's `covers`
tag `§8.4/service-P-closed` follows 061. Both are on `main` and neither is this batch's; the tag
is consistent with the migration it closes. Record only.

---

## 7. Findings, collected

| # | Severity | Finding | Where |
|---|---|---|---|
| F1 | LOW (record) | The closed table is, in both families, the designed service-write target; the headers say what is open, not that | `062:32`, `071:32`; `061:124-126, :995-999`; `070:925, :976, :1224-1226`; §3.2 |
| F2 | LOW (inherited) | The apply-time block can be emptied (`return;`, `where false`) with the static suite green and `migrate-clean` ok | D6, D7b; `identity-isolation.test.mjs:9730-9734` |
| F3 | LOW (inherited) | Cases prove shape and predicate, not the refusal; a renamed same-shape policy satisfies them | P-B; `isolation-cases.mjs:15421-15425` (branch) |
| F4 | LOW | Generated files, 258- and 382-character lines, generator outside the tree; "§3, row 2" | `062:25, :32, :136`; `071:25, :32, :136`; `:7` |
| F5 | INFO | An extra closure on an `S`-cell table applies clean; only the static list refuses it | D9, P9 |
| F6 | INFO | Handoff carries no CI run id yet | `handoffs/WP-0A-DB-00-author-handoff.json` on `ded29c2` |
| F7 | INFO | Two readings on `main` of `quota_buckets`' §8 row | `service-policy-map.json:34`; `061:1018-1020` |

**Is any of this stop-the-line?** `CONTRIBUTING_AGENTS.md` names tenant leakage among the
stop-the-line incidents. **I found no leak and no false control**: the two closures refuse every
service role every row on read and on write on both tables, `app_maintenance` included, and
`authenticated` is unchanged (§3); the `S`-cell tables are left open exactly as row (b) says
(§2). F1–F5 are about what the headers say and how much the rules would notice if the closures
stopped closing. **No stop-the-line.**

---

## 8. What a hand-simulation cannot establish, what I did not review, and what I did

- **Everything in §3 was measured, not simulated**, on PostgreSQL 17.11 with the CI shim; §4's
  "what decides" column is my reading of the catalog and PostgreSQL's documented policy
  semantics, and every line of it was then confirmed by a run. What the scratch cluster cannot
  establish: anything about the platform (`service_role`'s real `BYPASSRLS`, the managed `auth`
  schema — my first editor-save probe tripped on `permission denied for schema auth` for a bare
  `auth.uid()` in statement text, which a policy's pre-parsed expression never asks; the shim's
  own header says what it is not); and anything about a command function or batch 132's
  resolver, because neither exists to run.
- **`FORCE ROW LEVEL SECURITY` decides none of the 856** (no case runs as a table owner); it is
  guarded by claim 4 of each file, which D22 shows raises when asked and D7b shows can be skipped.
- **I did not re-simulate the 854 pre-existing cases** beyond the seven service cases in §4.3 and
  the control replay; the 856 green run is the measurement that they are unchanged.
- **I did not review** the handoff beyond its diff and the absence of a run id, the manifest's
  blocker wording beyond its 062/071 sentence, the `catalog-snapshot.json` `why` text beyond the
  two sentences the branch adds, RFC-2026-023's content (only its status), or the S8 map
  amendment beyond its one added line.
- **My thirty probes are not a coverage measurement.** Several were chosen to slip.
- **What I measured on the scratch cluster, in full**: one apply of `main` (854), two fresh
  applies of the branch set (856 each, once after the control replay), one apply without the two
  files (2 of 856), two control replays plus the re-enabled run, fourteen single-file mutation
  applies on fresh clusters (D5, D17, D22, D2, D16 raised; D6, D7b, D9 applied; the first D7
  attempt was a syntax error of my own and is not counted), and the P-A…P-G, R-B…R-H and q-re
  probe blocks, every write inside a rolled-back transaction.

---

## 9. Disposition

Nothing here advances `WP-0A-DB-00`. `status` stays `in_progress`; no gate in
`review_and_test_gates` is satisfied by this file; `test_verified` is not claimed. Whether this
run's file counts as the Tester signature PR #145 needs is for the Integration Owner under Q1;
this file does not say it does.

**What this file signs:** that the Author closed exactly the two tables the Owner's row (b)
names and no other, measured against RFC-2026-022 §3, the §8 matrices, the two batches' grants
and TO clauses, and the live catalog (§2, §3); that the two cases demand the outcome PostgreSQL
produces (0 wrong, 0 wrong-layer), fail exactly and only when their closure is absent (§3.1),
notice a closure altered to `USING (true)` (§4.1), and sit in no control's basis (§4.2, CI and
here); that the two closures refuse every service role every row on read and on write and leave
`authenticated` unchanged, measured on both tables (§3.2); that the seven existing service cases
do not move (§4.3); and that the pre-080 findings A0 applied on `main` (F1/F2/F3 of Q0-pre-080,
L2/L3/L4 of C0-pre-080, F3 of A1-pre-080) do not recur here, while L1 and L7 do.

**What it does not sign:** that the apply-time blocks are worth more than "did not raise on this
cluster" (F2), that anything in the suite exercises the refusal (F3), or anything about the
platform.

Owed to the Author (A0), in the order I would fix them: **F1** (two sentences), **F2** (a rule
that asserts the block's first statement is not `return`, or a reachability read), then F4's
generator and F6's run id. Owed to the Owner: nothing — the decision this branch carries is
applied as made.
