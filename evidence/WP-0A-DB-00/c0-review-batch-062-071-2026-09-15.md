# C0 contract review — the row (b) closure set: batches 062 and 071, PR #145 (Draft)

Run: `/claude/c0_contract_reviewer_062071`
Role: independent Reviewer, as named in `work-packages/WP-0A-DB-00.json`
`role_assignments.reviewer_agent_run_id`, run as a distinct run under the Owner's Q1 disposition.
This document **records findings**. It advances no package status, decides nothing that is the
Product Owner's to decide, and repairs nothing it found.
Date: 2026-09-15.
Subject: `origin/agent/claude/WP-0A-DB-00-closures-062-071`, head `ded29c2`, commits `f01d99e`,
`c503594`, `ded29c2` (all 2026-09-15 09:56 +07); PR #145, Draft, base `main`.
Base of this review: `main` = `1a80f43` (= `origin/main`, the merge of PR #144; `git merge-base`
with the branch is the same commit, and `git merge-tree --write-tree` produces `ff1ad3c` with no
conflict).

---

## 0. What I am, before anything else

I am a subagent spawned by `/claude/a0_atlas` — the run the manifest names as this package's
Author **and the author of the two files reviewed here** — and I run in the same vendor and model
family as it (Anthropic, Claude Fable 5.1). A0 wrote the three briefs I was handed, chose that I
would review this set, and chose what to point me at: RFC-2026-022 §3's table, the grants and
every permissive policy's TO clause on the two tables, the precedents 082/083/092/101 and
022/031/042, the three pre-080 role-run files, and the author's evidence file with its recorded
failed first apply. The Owner withdrew the cross-vendor condition on 2026-09-15 (Q1) and ordered
same-vendor role runs (Q6); under that decision a distinct run in the named role counts as that
role's signature. I am a distinct run. I am not A0, and where this document and A0's record
disagree, the tree decides: every claim below carries the command or the `file:line` a reader
needs to re-run it.

What the shared model does not weaken: a `pg_policy` row, a `has_any_column_privilege` answer, a
suite left green or turned red by a one-line mutation, an INSERT refused by a policy **by name**.
A shared model does not make `061_metering.sql:1002-1007` grant `app_worker` SELECT, INSERT and
UPDATE on `quota_buckets`; it does, and a live PostgreSQL 17.11 says so (§3/P2).

What it does weaken, and I cannot fix from inside: A0 chose the threads, and the brief I was
handed asserts the one thing this batch rests on that the tree cannot show me — that the Owner
chose row (b). **M1** is that finding, and it is the kind a same-vendor subagent of the Author is
least placed to make: I have only A0's word, in the brief and in five places on the branch, and I
say so rather than treat the brief as evidence. Pressure from the coordinator, in either direction,
is not evidence. §5 lists what I did not check.

Two corrections to the briefs, recorded here rather than silently followed (§1): the common brief
says no PostgreSQL is available on this machine, and that is false — everything in §3 that says
"measured" was measured on a scratch cluster of my own on `127.0.0.1:5532`, never on the user's
server at `/tmp:5432`, which I did not connect a session to, create anything on, or stop. And every
file I exported from the branch into this worktree was compared against the branch by
`git hash-object` = `git ls-tree` blob id before I relied on it (§6); the six blob ids matched.

---

## 1. Corrections to the briefs I was handed

1. **"No PostgreSQL is available on this machine" (brief-common) is false.** Homebrew
   `postgresql@17` 17.11 is at `/opt/homebrew/opt/postgresql@17/bin`. The subject brief says so
   and assigns me port 5532; every live claim in this file is from that scratch cluster (fresh
   `initdb` before each `migrate-clean`, `db/foundation/ci/supabase-shim.sql` applied, stopped and
   deleted before this file was committed).
2. **The subject brief's expected numbers are all as stated**, recorded as measurements: 854 on
   `main`, 856 on the branch, 2 of 856 failing with the two files removed, identity suite 285,
   contract suite 60, CI `verify` 643 (§3, §6).
3. **The brief-reviewer's "byte budget" no longer exists as a size cap** (C0-pre-080 §1/3; the
   driver feeds psql on stdin and the surviving rule is "no psql meta-command"). Measured anyway:
   9,547 and 9,704 bytes.
4. **The subject brief's "This PR is the Owner's decision, not a proposal" is a claim I could not
   verify against the tree**, and it is the brief's — A0's — claim. See M1. I reviewed the batch
   on the brief's framing (did the author close exactly the right tables and nothing else) and
   record separately that the framing itself has no Owner-voice record.

---

## 2. Findings

Severity: HIGH — the batch or its premise says something untrue that a decision rests on;
MEDIUM — a claim the tree contradicts, or a control that reads as present and is not, or an
authority the tree cannot show; LOW — imprecision, inheritance, hygiene. No finding here is
stop-the-line (§4).

### M1 — MEDIUM. The decision the batch implements — the Owner's row (b) — is recorded only in the Author's words

The batch says six times that the Owner chose row (b) of the S8 map's §3 on 2026-09-15:
`062:9-10` and `071:9-10` ("THE OWNER CHOSE ROW (b) ON 2026-09-15"), the map's new §3 note on the
branch ("**Disposed, 2026-09-15:** … chose **row (b)**"), the manifest blocker's bracketed tail
("THE OWNER DISPOSED BOTH … row (b) chosen"), the author's evidence file §0 ("The Owner, in
session, answered …"), and the handoff's `assumptions[0]`. Every one is A0's sentence. There is no
transcription: `grep -rn "row (b)" evidence/ architecture/ docs/` on `main` and on the branch
finds the map, the two headers and the role-run files, and nothing in the Owner's voice.

The repository's own standard for an Owner decision is set by its two disposition files:
`product-owner-disposition-batch-070.md:8` and
`product-owner-disposition-2026-09-15-six-questions.md:9` both say *"The transcription is the
record; the conversation is not"*, and the six-questions file quotes each answer verbatim with the
lettered options reproduced "so the mapping is checkable rather than plausible". Row (b) is a
lettered option — the map's §3 offers (a) wait, (b) close the non-S tables, (c) close all with the
amendment — and the letter chosen is recorded nowhere the Owner wrote it.

Why it matters more here than usual: under the six-questions file's closing instruction, A0
merges by delegation overnight, so the Owner may never see PR #145 before it is on `main`. The
merge is then the Owner's manual merge under RFC-2026-002 on the strength of a decision only the
Author attests. The premise the decision rests on is independently true against the tree (§3:
neither closed table is an `S` cell; the four `S`-cell tables are open), and the closure is inert
today (P2/P4/P6), so this is not stop-the-line; but a Reviewer cannot sign that "the author closed
the right tables" when the definition of "right" is the author's report of a conversation.
**Owed to A0, before or with the merge:** a transcription in the six-questions file's shape — the
question put, the three options, the Owner's words — as a disposition file or a dated addendum to
the existing one; and the Integration Owner should not treat the map's §3 note as that record.

### M2 — MEDIUM. The manifest blocker entry now leads with a sentence the branch itself makes false

`work-packages/WP-0A-DB-00.json` `open_blockers[163]` on the branch (the last entry, the only one
naming 062) still begins as it did on `main`: "THE THREE PRE-080 FAMILIES … ARE CLOSED IN 082's
SHAPE … **NOT CLOSED, ON PURPOSE: metering (061: quota_buckets) and research (070: four tables)**,
because a closure there refuses the CARRIED worker policies … This entry closes when the Owner
disposes the pull request that carries the three files and picks a row for the two families
left." The correction is a bracketed tail more than 1,300 characters in: "[2026-09-15, later: THE
OWNER DISPOSED BOTH … 062 (quota_buckets) and 071 (research_suggestions) close …]".

The manifest's own convention for an entry overtaken by events puts the closure first: four
entries begin `CLOSED <date> BY A0: … [ORIGINAL ENTRY FOLLOWS]` (`node -e` over the branch
manifest, §6). Here the stale statement leads and the truth trails, and the handoff describes the
edit as "the pre-080 blocker entry is narrowed to the S-cell tables"
(`open_risks_or_blockers[0]`), which it is not — nothing was narrowed; a paragraph was appended.
A reader of the first sentence — the Integration Owner reading blockers — is told on the branch
that `quota_buckets` is not closed while `062_metering_service_path_closed.sql` sits beside it.
**Owed to A0:** re-head the entry in the `CLOSED … [ORIGINAL ENTRY FOLLOWS]` shape, or split it:
the pre-080 part closed, the `S`-cell part as the one entry that remains.

### L1 — LOW. 071's "LEFT OPEN ON PURPOSE" inventory names three of the research family's four open tables

`071:32` lists `research_runs`, `research_sources` and `research_evidence` as left open and why.
The family has a fifth table, `app.research_snapshots`: `app_worker` holds SELECT, INSERT and
UPDATE on it (P2), it carries **no policy at all** (P1b — not narrowed, not even a permissive
one), and 070's own header says its service INSERT "is not an `S` cell either" (`070:357-360`).
Under row (b) — close only the *narrowed* tables that are not `S` cells — it is correctly not
closed, for the same reason `usage_reservations` is not: there is no narrowing making a false
claim. But `062:32` says exactly that about `usage_reservations` ("carry no narrowing") and `071`
says nothing about the snapshot, so the research header's account of what it leaves open and why
is incomplete in the one place a reader will look. The S8 map's research row (`:17`) has the same
omission.

### L2 — LOW. The closures are still generated by tooling outside the repository, and this batch recorded the cost (C0-pre-080 L7 recurs)

The evidence file §1 says the two files were "generated by the same tooling as 022/031/042", and
§3 records what that tooling did: it read 082's block as *text*, double-escaped the
`like '%\_service\_path\_closed' escape '\'` line, and the first apply of 062 failed with
`22025` — "the same slip 022/031/042 had, fixed there by hand and here fixed in the generator".
A fix "in the generator" is a fix in a file nobody can read from the tree (the map's §4:
"`gen-closure.mjs` (this session's scratchpad tooling, not in the repository)"). The files
themselves are right (§3/P0; the LIKE line at `062:65`/`071:65` is 082's), and the template shows:
`062:32` is 258 characters, `071:32` is 382 (082's longest line is 208). Pre-080 L7 asked for the
generator under `scripts/db/` or a header sentence saying the file is hand-maintained; neither
happened, and a third generated set now carries a recorded generator defect.

### L3 — LOW. The two catalog cases prove the closures exist; nothing in the suite exercises the refusal (C0-pre-080 L1, inherited from 082)

`batch-062-closes-the-service-path-on-the-bucket-table` (`isolation-cases.mjs` branch `:12865`)
and `batch-071-closes-the-service-path-on-the-suggestion-table` (`:12878`) run
`SERVICE_PATH_CLOSURE_ON` as `ownerA` and ask `pg_policy` for a restrictive, `FOR ALL`,
`TO PUBLIC` policy with the exact predicate in both halves (`:15421-15425` on the branch). No case
in the 856 makes a service role holding a permissive policy meet the closure. The refusal is
measured only by hand — the author's read probes (evidence §2: 0 vs 4, 0 vs 2) and mine (§3/P4-P6
for reads, P11 and P13b for **writes**, refused by the closure by name). "856 passed" proves the
closures are there, not what they do.

### L4 — LOW. Three imprecisions in citations and counts

- `062:7` and `071:7` cite "the S8 map … §3, row 2". §3 of the map is a bullet list; the table is
  §2, where metering is row 4 (`:16`) and research row 5 (`:17`). The second §3 bullet is the one
  meant ("Two need the Owner").
- The handoff's tests entry and the evidence §2 say "without the two files: FAILED — 2 of 856".
  True of `rls-smoke`; `schema-lint` also fails in that configuration (`FAILED — 3 problem(s)`,
  §3), because `catalog-snapshot.json` on the branch lists both files. The chained command the
  handoff cites (`migrate-clean && schema-lint && rls-smoke`) would stop before the 2 of 856 was
  printed.
- `062:25` and `071:25` say the closure "changes nothing observable, for 101's reason rather than
  082's" and cite "the 'grants and no policy' shape batch 010 introduced"; 101 is the precedent
  for the claim-3 form, which is right, but 061 and 070 make their own statement of the shape
  (`061:1082-1095`, `070:1064-1070`) and are the nearer citation.

### L5 — LOW (inherited from 082, all nine closure files). Assertion 2 finds the binding restrictive policy by `relname` alone

`062:104-112` and `071:104-112` (from `082:223-231`; also `101:136`): the `not exists` that looks
for a restrictive policy binding an admitted role joins `pg_policy` to `pg_class` on
`rc.relname = p.target` with no namespace term, while the outer query is filtered to `app`. A
same-named table in another schema carrying any restrictive `TO PUBLIC` policy would satisfy the
rule for a table in `app` that has none. No such table exists today (`catalog-snapshot.json`), so
the assertion is true for the right reason; it is one `join pg_namespace` short of asserting it.

---

## 3. Threads that check out — measured

Scratch cluster: PostgreSQL 17.11 (Homebrew, aarch64), `initdb --locale=C -E UTF8`, port 5532,
`listen_addresses=127.0.0.1`, no Unix socket, `supabase-shim.sql` applied; fresh cluster before
each of the six `migrate-clean` runs below, because 001 creates cluster roles. Files placed in
this worktree for the branch runs were produced by `git show <ref>:<path>` and their
`git hash-object` equals the branch's `git ls-tree` blob id for all six (§6). The branch was never
checked out.

**The premise, against the tree and the documents.**

- RFC-2026-022 §3's table (`architecture/decisions/RFC-2026-022-service-policy-shape.md:173-183`)
  names nine `S` cells. The metering one is "Usage ledger INSERT (`S/N`) | 061 | CARRIED"; the
  research one is "Research run/source/evidence INSERT | 070 | CARRIED". §8 of the RLS document
  agrees and is the source: `sprint-0a-core-erd-rls-retention-th.md:359` gives the run/source/
  evidence INSERT `S` and `:360` gives "Suggestion save/dismiss/use" Service **`P`**; `:397` gives
  "Usage/quota summary SELECT" Service **`P`** and `:398` the ledger `S/N`. Counting every `S` in
  the four matrices (`:341-402`) gives nine, and neither `quota_buckets` nor `research_suggestions`
  is one. 070 says the same of the suggestion in its own header (`070:357-360`: "no cell anywhere
  in §8's four matrices for CREATING a suggestion … the service's INSERT on the suggestion … is not
  an `S` cell either"), and `db/foundation/lint/service-policy-map.json:34` says "app.usage_reservations
  and app.quota_buckets have none at all, because §8 has no row for either". **Row (b) names the
  right two tables.**
- The `S`-cell tables are left open, by catalog: P1b lists every policy on `usage_events`,
  `usage_reservations`, `research_runs`, `research_sources`, `research_evidence`,
  `research_snapshots` — seven rows, all on the three narrowed research tables, all
  `to authenticated`, none `*_service_path_closed`; the two metering ledger tables and the snapshot
  carry no policy at all. P10: 20 `*_service_path_closed` policies on 20 tables schema-wide
  (18 on `main` + 2).
- Grants on the two tables (P2, live; text at `061:1002-1007`, `070:1222-1226`): `app_worker`
  SELECT/INSERT/UPDATE on `quota_buckets`, SELECT/INSERT on `research_suggestions`;
  `authenticated` SELECT on both and UPDATE on the suggestion (`070:1158`). No other service role
  and no `anon` holds anything on any of the eight metering/research tables. The headers'
  statements of the worker's grants (`062:25`, `071:25`) match the tree — the pre-080 map error
  (C0-pre-080 M1) does not recur.
- Every permissive policy on the two tables is `to authenticated` (P1: `quota_buckets_select_owner_or_admin`;
  `research_suggestions_select_active_member`, `_update_writer`), and the two narrowings are
  `as restrictive for all to authenticated` (`061:1047-1049`, `070:1400-1402`) — the S8 shape.
  `quota_buckets`' narrowing resolves through `app.member_scope_admits_business` (`061:1052`), so
  082's argument against naming `app_command` in it transfers; `research_suggestions`' narrowing
  is an `exists` on `research_runs` (`070:1403-1409`) evaluated as the caller, which for a service
  role sees zero runs — vacuous the other way, and equally not a fix.
- No `SECURITY DEFINER` function in `app` or `private` names either table (P7: empty). RFC-2026-022
  §7 is not in effect: `app_worker`'s only member is `postgres` (P8), which bypasses RLS; no
  service role has `rolbypassrls` or `rolsuper`. ENABLE and FORCE on both, owner `postgres` (P9).

**The closures, as the catalog holds them (P0).** Two rows, `polpermissive = f`, `polcmd = *`,
`polroles = {0}`, both halves `(CURRENT_USER = 'authenticated'::name)`.

**The author's probe claims, re-run** (evidence §2; three required, five done).

| claim | measured here |
|---|---|
| `main`: 854 cases | `db-migrate-clean: ok`, `db-schema-lint: ok`, **`854 isolation case(s) passed.`** |
| branch: 856 = 854 + 2 | `applied 062_…`, `applied 071_…`; lint ok; **`856 isolation case(s) passed.`** (three separate fresh runs, same number) |
| without the two files: 2 of 856 | `migrate-clean: ok`; `schema-lint: FAILED — 3 problem(s)` (L4); **`rls-smoke: FAILED — 2 of 856`**; the two failing ids are exactly the two new ones (`(assert): nothing was visible`); 2 failed-case lines, so no other case moved — in particular the seven existing service cases on the two tables (`service-sees-zero-quota-buckets`, `service-cannot-create/rewrite/retarget/delete-a-quota-bucket`, `service-sees-zero-research-suggestions`, `service-cannot-propose-a-research-suggestion`) pass with and without |
| `quota_buckets`: worker reads 0 with the closure, 4 without | P4: `probe_worker_reads … to app_worker using (true)`, `set role app_worker` → **0**; P5: closure dropped → **4** rows in **2** tenants |
| `research_suggestions`: 0 with, 2 without | P6: **0**; P6b: closure dropped → **2** rows in **2** tenants |

Extended to writes, which the author did not probe: P11 gives the worker a permissive INSERT
policy `with check (true)` on `quota_buckets` with the closure present → `new row violates
row-level security policy "quota_buckets_service_path_closed"` — refused by the closure by name.
P12 drops the closure: the same INSERT passes RLS and fails later, at NOT NULL (my probe's
workspace subselect returned nothing as the worker) — the S8 shape on a write. P13b/P14b, the same
on `research_suggestions` with literal ids: refused by
`"research_suggestions_service_path_closed"` by name with the closure; **lands** (`INSERT 0 1`,
3 rows, 1 probe row) without it. With a closure dropped, a permissive worker policy on either table
is a cross-tenant write today, exactly as A1-pre-080 measured for the three earlier families.

**The apply-time block fires (live).** With `062:85` mutated to `count_of <> 2`, `migrate-clean`
applied 20 files and stopped at 062: `batch 062 finds 1 service-path closures and there must be one
per table, 1 (P0001)`. 062 restored (sha256 equal to the exported copy).

**The static rule, mutated (branch tree in this worktree, `node --test`, baseline 285/285).**

| probe | file, mutation | result |
|---|---|---|
| M1 | 062: `for all` → `for all to app_worker` | 283/285 — the closure rule **and** 061's "no service policy" rule |
| M2 | 071: predicate → `current_user = 'app_worker'`, both halves | 284/285 |
| M3 | rules: delete the `062` row of `SERVICE_PATH_CLOSURES` | 284/285 (file with no row) |
| M4 | cases: the 071 case's `params` → `['research_runs']` | 284/285 |
| M5 | 062: `with check (true)` | 284/285 |
| M6 | 071: the rule-2 `raise exception` replaced by `null; -- …` (the sentence survives as a comment) | 284/285 — read from code, not comments (Q0-pre-080 F3 holds) |
| M7 | cases: the 062 case id's tail respelled `bucket-tabel` | **285/285 — not noticed**, correctly: the rule keys on prefix + table parameter (`:9742-9744` on the branch) |

Every mutation that changes what the closure *is* was noticed; the one that changes only a
spelling was not, and should not be. The four files' sha256 after restoration equal the exported
copies.

**Shape and rules.** `diff` of 062 against 071 with batch numbers, table and family names
normalised differs on exactly three lines (`:25`, `:32`, `:119` — the grant verbs and the
left-open list); `diff` of 062's apply-time block against 022's differs only in the claim-3
comment and the table array. Claims 1, 2 and 4 are 082's; claim 3 is 101's form
(`062:118-137` against `101:146-165`), the right one because the worker holds grants here.
`grep -c service_roles` → 0 in both (C0-pre-080 L3 applied). The `covers` tags are
`§8.4/service-P-closed` and `§8.2/service-P-closed` — the matrix sections the cells live in
(C0-pre-080 L2 applied). `SERVICE_PATH_CLOSURE_ON` asserts the predicate (Q0-pre-080 F2 applied;
`identity-isolation.test.mjs:9705-9710` on the branch pins it). No `grant`, `alter role`, `drop policy`,
`alter policy` or `member_scope_` in either file's code. `servicePolicyMapLint`'s closed field set
is untouched — `git diff main...<branch> -- db/foundation/lint/service-policy-map.json` is empty.
**Invariant 1** holds: `--stat` on `db/foundation/migrations/` shows two new files and nothing
else; the reason for a forward file (`062:20-23`) is 082's.

**§6 registry.** `062` and `071` are not reserved
(`sprint-0a-core-erd-rls-retention-th.md:264-265` list `061` and `070` and nothing between them and
`080`); the headers say so and cite 082/083/092/101 as the same case. The families' owners are
A0/A6 Metering and A2 Research; whether a closure on A2's family needs A2's countersignature is the
registry question C0-pre-080 recorded and did not decide, and I do not either.

**Ownership, by hand.** Twelve changed paths: ten under `writable_paths` (`db/foundation/**` ×3,
`evidence/WP-0A-DB-00/**` ×2, `handoffs/WP-0A-DB-00-*.json`, `test-kits/db/**`,
`tests/db/identity/**` ×2, the manifest), two declared as amendments
(`test-kits/branch-identity.test.mjs`, `test-kits/integrity-manifest.json`), matching
`amends_without_owning`. `catalog-snapshot.json`'s `why` grows by two sentences placing 062
between 061 and 070 and 071 between 070 and 080; `NOT_ON_THE_INSTANCE`
(`foundation-contract.test.mjs:330-333` on the branch) matches; the contract suite's tail rule
passes (60/60 on the branch tree). The `branch-identity.test.mjs` row is repointed from the
pre-080 branch to this one although `origin/agent/claude/WP-0A-DB-00-pre-080-closures` still
exists on the remote — the single-slot convention every increment has followed; noted, not found.

**Case ids and the CI control.** The two ids match none of the 48 control patterns in `ci.yml`
(checked by substring over the list at `ci.yml:200-818`); `quota-bucket` and
`research-suggestion` are patterns (`:509`, `:678`), and the ids say "bucket-table" and
"suggestion-table" for that reason. All 856 ids are unique on the branch.

**CI.** Run `34923143406`, head `ded29c2`, conclusion `success`: `applied 062_…`, `applied 071_…`,
`db-schema-lint: ok`, `db-rls-smoke: 856 isolation case(s) passed.`, bootstrap `tests 643, pass
643, fail 0`, and `the handoff for this branch describes this branch` passed — it ran on the
branch name, after the Q4 fix.

**What is left open, and whether the reasons hold.** `usage_events` (`S`, CARRIED, `:179`) and
`research_runs`/`_sources`/`_evidence` (`S`, CARRIED, `:176`): a closure would refuse the policy
RFC-2026-022 expects, and RFC-2026-023 §3.3 (`:58-71`) is where the amendment shape lives —
`In review`, not approved (`RFC-2026-023:3`), so "when that RFC is in effect" (`071:32`) is the
right tense. `usage_reservations` and `research_snapshots`: no policy of any kind (P1b), so no
narrowing makes a false claim — holds, with L1's omission.

**A consequence to state, not a finding.** These closures refuse a future `app_maintenance` sweep
and any `app_authz`-owned helper on the two tables until amended — 082's deliberate direction
(`082:85-87`), now true of the bucket a retention batch (160) will want to purge with its ledger.

---

## 4. Stop-the-line assessment

**No finding in this document is stop-the-line under `CONTRIBUTING_AGENTS.md`** (secret
exposure, tenant leakage, duplicate external side effects, lost jobs, migration divergence,
irreversible deletion, contract mismatch). The batch adds two RESTRICTIVE policies and two
apply-time blocks and nothing else; it widens no path (P1/P2 are the same set of privileges and
admissions before and after); it rewrites nothing merged; every measured refusal is a refusal,
on read and on write.

M1 is about the *record* of the decision, not its correctness: the two tables are the right two
under row (b) by every document in the tree, and closing them is inert today. I would put M1 in
front of the Integration Owner beside the PR, because RFC-2026-002 asks for the Owner's decision
to be on record and this one is not.

---

## 5. What I did not check

- **Whether the Owner chose row (b).** I could not; that is M1.
- **The metering and research narrowing predicates** for correctness against §7/§8.6 — they are
  061's and 070's, merged and reviewed before this batch.
- **The Tester's and Security's chairs.** L3 says why a Tester's own replay matters; A1's view of
  the `app_maintenance`/batch 160 consequence on `quota_buckets` is owed separately.
- **`node scripts/verify-branch-scope.mjs` on the branch** — it reads `HEAD`, the branch is not
  checked out anywhere, and ownership was enumerated by hand instead (§3). CI ran it on the branch
  name and is green.
- **`npm run verify` on the branch tree** — CI's 643 is cited instead; I ran the two static suites
  on the branch tree myself (285, 60) and `npm run verify` on this branch (§6).
- **The handoff plumbing** (`integrity-manifest.json` digests, `branch-identity.test.mjs`) beyond
  the suites passing on the branch tree and CI green on its head.
- **What neither A0 nor I thought of.** A0 chose the threads; M1 came from asking where the
  brief's authority lives in the tree, L1 from a catalog query the brief did not ask for. The rest
  of that category is not empty.

---

## 6. Commands run, verbatim

Worktree `/Users/bank/ThinkBizThai/.claude/worktrees/agent-abbe8d2f403d332d5`, branch
`agent/claude/WP-0A-DB-00-c0-review-062-071` made from `main` at `1a80f43`. Scratch cluster on
`127.0.0.1:5532` via helper scripts in the session scratchpad; the user's server on `/tmp:5432`
was not connected to (one `pg_isready` liveness ping at the end, no session), created on, or
stopped.

```
git rev-parse main origin/main origin/agent/claude/WP-0A-DB-00-closures-062-071 → 1a80f43, 1a80f43, ded29c2
git merge-base main origin/…-closures-062-071 → 1a80f43;  git merge-tree --write-tree → ff1ad3c
git log --format='%h %cI %s' main..origin/…-closures-062-071 → f01d99e, c503594, ded29c2 (2026-09-15 09:56 +07)
git diff main...origin/…-closures-062-071 --stat → 12 files, +444 −48; -- db/foundation/migrations/ → 2 new files only
git diff main...origin/…-closures-062-071 -- db/foundation/lint/service-policy-map.json | wc -l → 0
gh pr view 145 → OPEN, Draft, head ded29c2, base main, 0 reviews
gh run list --branch … → 34923143406 success on ded29c2; gh run view --log → applied 062/071, lint ok, 856 passed, tests 643 pass 643
git show origin/…:<path> > <path> (×6), git hash-object = git ls-tree blob id (×6, equal):
  062 95eb3c9…  071 ed0cf0d…  isolation-cases faba331…  identity test 729071f…  snapshot c28dca3…  contract test 3017bbb…
wc -c 062/071 → 9547 / 9704;  awk length >120 → 062: 258 max (:32), 071: 382 max (:32)
grep -c service_roles 062 071 → 0 / 0;  grep -n 'Disposed|row (b)|OWNER CHOSE' evidence/ db/… → A0's files and headers only
node (buildCases((s)=>s)) branch → 856 ids, unique; the two new ids match no ci.yml control pattern; covers/params/sql as recorded
diff <(sed-normalised 062) <(sed-normalised 071) → lines 25, 32, 119;  diff 022 block vs 062 block → claim-3 comment and table array

node --test tests/db/identity/identity-isolation.test.mjs   branch tree → 285/285 (mutations M1–M7 as in §3; restored, sha256 checked)
node --test test-kits/db/foundation-contract.test.mjs        branch tree → 60/60

initdb (C, UTF8) → pg_ctl start -p 5532 → create database thinkbizthai_test → supabase-shim.sql   (×6 fresh clusters)
DB_TEST_URL=postgresql://postgres@127.0.0.1:5532/thinkbizthai_test LC_ALL=C TZ=UTC PGTZ=UTC node scripts/db/run.mjs …
  main:            migrate-clean ok · schema-lint ok · rls-smoke: 854 isolation case(s) passed.
  branch files:    migrate-clean ok (applied 062, 071) · schema-lint ok · rls-smoke: 856 isolation case(s) passed.   (×3)
  062 count_of<>2: migrate-clean FAILED at 062 after 20 applied, P0001
  files removed:   migrate-clean ok · schema-lint FAILED — 3 problem(s) · rls-smoke: FAILED — 2 of 856 (the two new ids; 2 failed-case lines)
psql -f c0-062071-probes.sql (branch state, after rls-smoke) → P0–P10 as recorded in §3
psql -f c0-062071-insert-probe.sql → P11, P12;  psql -f c0-062071-write-probe2.sql (fresh branch cluster) → P13b, P14b
pg_ctl stop -m fast; rm -rf pg-scratch; pg_isready -p 5532 → no response
git checkout -- <4 files>; rm <2 migrations>; git status → clean before this file
git checkout -b agent/claude/WP-0A-DB-00-c0-review-062-071 main

npm run verify   (this branch, with this file in the tree)   → clean: exit 0 — tests 643, pass 643, fail 0, skipped 0, todo 0
```

`npm run verify` on this branch, with this file in the tree, printed exactly:
`clean: exit 0 — tests 643, pass 643, fail 0, skipped 0, todo 0`

Not run, and why: `npm run check:scope` (takes no arguments, exits 0 — not evidence);
`npm run refresh:handoff` (A0's artifact); anything against `/tmp:5432`.

---

## 7. Verdict

**From the Reviewer's chair, the two files close exactly the tables row (b) names and nothing
else, in 082's shape, and every claim they make about the tree is true** — `quota_buckets` and
`research_suggestions` are the one narrowed non-`S` table of each family by RFC-2026-022 §3 and by
§8 itself; the four `S`-cell tables carry no closure; the worker's grants are as the headers say
and admitted by no policy; the closures refuse a permissive worker policy on read and on write,
by name, and are inert today; the static rule and the apply-time block each turn red on every
mutation that changes what a closure is; the counts are 854 → 856 → 2 of 856 as claimed; CI is
green on the head, on the branch name.

**What this signs:** the Reviewer's check of PR #145 at `ded29c2` against the documents it cites
and against a live PostgreSQL 17.11. **What it does not sign:** the Tester's, Security's or
Integration Owner's chairs; the package's status; any gate; and — M1 — the fact that the Owner
chose row (b), which this batch asserts and the tree does not record. It asks A0 for two things
before or with the merge: put the Owner's words on record (M1) and re-head the blocker entry so
its first sentence is true of the tree it sits in (M2). The five LOW findings are owed as
hygiene and are not conditions.

Findings: 0 HIGH, 2 MEDIUM, 5 LOW. Stop-the-line: none.
