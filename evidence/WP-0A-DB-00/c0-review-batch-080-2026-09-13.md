# C0 contract review — batch 080 (`content.core`), merged as PR #112

Run: `/claude/c0_contract_reviewer`
Role: independent Reviewer, as named in `work-packages/WP-0A-DB-00.json`
`role_assignments.reviewer_agent_run_id`.
This document **records findings**. It advances no package status, signs nothing on any author's
behalf, and repairs nothing it found.
Date: 2026-09-13.
Subject: `49830ad` (merge of PR #112), branch commits `2cd1450`, `2941e28`, `3cd1617`.
Base of this review: `main` = `c5eb1b9`, confirmed with `git log --oneline -3`.

---

## 0. What I am, before anything else

**I am a subagent spawned by `/claude/a0_atlas`, the Author of the change reviewed here, and I run
in the same vendor and model family (Anthropic, `claude-opus-5`).** A0 wrote my task description,
chose the seven threads I was pointed at, told me which to look hardest at, and chose the range.

What that does **not** weaken:

- Everything below that is a *measurement against the tree* stands on its own. A `git grep -c`, a
  line number, an exit code, a mutation that leaves a suite green — a reader can re-run each one,
  and I have written the command or the `file:line` for every claim so they can. A shared model
  does not make `080_content.sql` contain the word `BYPASSRLS`; it contains it zero times, and
  `grep -c` says so on any machine.
- The findings that contradict A0's own record are, if anything, evidence against collusion rather
  than for it. Finding **H2** rejects a sentence A0 wrote in the migration, in the manifest's
  `open_blockers`, in the author handoff and in the session state record.
- **And the same applies in reverse, which is the harder half.** I wrote a HIGH finding (H3) that was
  wrong; it is **withdrawn in place** below rather than footnoted, along with its downstream
  inference. A reviewer whose errors are corrected in the same document as its findings is the only
  kind whose findings are worth anything.
- **Pressure to change a finding is not evidence, in either direction.** A coordinator falsified H3,
  then retracted the falsification and asked me to restore it at HIGH. I withdrew it on the first
  message because the measurement warranted it, and **declined to restore it on the second**, because
  by then I had a measurement of my own that contradicted the restatement. Deference to whoever spoke
  last is not independence; the H3 entry records which specific numbers moved me and which did not.

What it **does** weaken, and I cannot fix from inside:

- **Framing.** A0 chose the seven threads. I looked hard at those and glanced elsewhere. A defect
  A0 did not think to point me at is a defect I probably did not find. §5 lists what I did not
  check, and I cannot list what neither of us thought of. The one finding here that came from
  outside the brief (**M4**, the absent `set_updated_at` trigger) I found only because I was
  counting something else; that is the rate to expect.
- **Shared blind spots.** Where A0's reasoning is wrong for a reason characteristic of this model —
  a plausible-sounding claim about Postgres internals — I am the least likely reader to catch it.
  **H1** is precisely that shape: a confident, repeated, four-times-written claim about how RLS and
  SECURITY DEFINER ownership interact, which reads as correct and is the inverse of the approved
  decision it cites. I caught it only because the migration contradicts itself 950 lines later. The
  session record shows A0 had already half-seen it — it is written into the fan-out table as the
  thing `/claude/a1_bastion` should press on — and that run was cut before it started. There may be
  others of the same shape that read as correct to both of us.
- **Independence in the protocol's sense.** `CONTRIBUTING_AGENTS.md` §"Separation of duties"
  requires a distinct independent Reviewer. **This is a second reading, not a second opinion.** It
  does not satisfy `independence.prefer_cross_vendor_review`, does not lift
  `independence.cross_vendor_exception` in the manifest, and **must not be counted as the
  independent review this package owes.** The exception text in the manifest stands unchanged and
  the Integration Owner must carry it forward.

---

## 1. Corrections to the brief I was handed

A0 chose the seven threads below and I say so at each. I originally recorded two things I was told as
wrong. **One of them (§1.1) I got wrong myself and have withdrawn.** §1.2 stands.

### 1.1 WITHDRAWN — the brief's instruction was right and my objection to it was wrong

**This section originally claimed the green-suite instruction was unachievable. It is achievable; I
was wrong, and the correction is recorded at the H3 entry in §2 rather than hidden here.**

I was told:

> `ownership.branch` in the manifest is a SINGLE slot … To get a green suite, repoint
> `ownership.branch` to your branch AND update your row in that table, then
> `npm run regenerate:manifest`.

**That instruction is correct.** `/claude/a1_bastion` followed it, did not touch the Author's
handoff, and its CI concluded success (`gh run view 34751714594` → `"conclusion":"success"`, head
`de1e887`). I did follow it and measured `npm run check → exit 1 — tests 586, pass 584, fail 2`, and
I wrongly concluded from that single reading that no green configuration existed. The reading was an
artifact of taking it when my branch had **zero commits**, so `HEAD` was still the merge commit at
the tip of `main`; the full diagnosis is at the corrected H3 entry.

The only thing the brief is missing is an ordering constraint, recorded as `M6`: the repoint plumbing
must land in the **last** commit on the branch, because the handoff guard compares against `HEAD^`,
and once it is not there no commit can put it back without a force-push.
**This branch now follows the brief as written**; §7 records what its CI runs did and did not show.

### 1.2 The premise of thread 7 is sound, but today's measurement is an artifact and batch 080 is not at fault for it

I was told to check whether 080 wrote outside what it owns. Running the validator today:

```
node scripts/verify-branch-scope.mjs 49830ad~1 WP-0A-DB-00   → exit 73
WP-0A-DB-00 changed 3 path(s) it neither owns nor records as an amendment:
  .github/workflows/ci.yml
  evidence/VERIFICATION.md
  scripts/test-suite-contract.mjs
```

**This is not a batch 080 defect and I want it stated plainly before anyone quotes the exit code.**
At `49830ad`, the manifest declared all three:

```
git show 49830ad:work-packages/WP-0A-DB-00.json | jq -r '.ownership.amends_without_owning.paths'
  [".github/workflows/ci.yml", "evidence/VERIFICATION.md", "scripts/test-suite-contract.mjs",
   "test-kits/branch-identity.test.mjs", "test-kits/integrity-manifest.json"]
```

I enumerated the merge's changed paths against `writable_paths` at that commit: the set that is
neither owned nor declared is **empty**. Batch 080 was ownership-compliant when it merged. The exit
73 above exists only because the *later* session-handoff increment (`d6e08c7`) replaced
`amends_without_owning` with its own three-path list. That is finding **L1**.

`npm run validate:protocol` → **exit 0** (all three validators).

---

## 2. Findings

**Eleven findings: 2 HIGH, 6 MEDIUM, 3 LOW.** As first written this document had ten (3 HIGH, 4
MEDIUM, 3 LOW). **H3 was wrong and is withdrawn**; what replaces it is `M6` (a narrower and real
constraint) and `M5` (a separate defect the diagnosis turned up). That entry was corrected twice — a
coordinator falsified H3, then retracted the falsification and asked me to restore it, and I decline
on a measurement I can re-run. The whole sequence is recorded there. **H1 and H2 are unchanged.**

| id | sev | one line |
|---|---|---|
| H1 | HIGH | The migration, the manifest, the handoff and the state record all cite RFC-2026-017 §3 for the **inverse** of what RFC-2026-017 §3 decides. |
| H2 | HIGH | "Nothing in this repository can write a content version, variant or quality review" is **false against the tree**, and the fixture that falsifies it explains itself by a mechanism that does not exist. |
| ~~H3~~ | — | **WITHDRAWN.** Claimed no role run can open a green PR. This branch is the counter-example: five commits, repointed, handoff untouched, `clean: exit 0 — 586/586`. Not restored despite a later request to restore it. |
| M1 | MED | The `app_worker` departure silently retires the BYPASSRLS canary that batch 010 introduced and 070 states as the reason for the shape. Unstated in all four places the departure *is* discussed. |
| M2 | MED | The apply-time block **forbids** any policy naming `app_command` on these five tables — the very policy RFC-2026-017 §3 says the promised writer needs. |
| M3 | MED | Probe 18's defect class is **still open**. I re-opened it with a one-line mutation; suite stayed 232/232 green. |
| M4 | MED | No `set_updated_at` trigger, and `updated_at` handed to `authenticated` in the UPDATE grant. 080 is the only batch in the repository that does either. **Candidate seventh blocker.** |
| M5 | MED | **CI and the tree disagree on the same commit, and CI is the green one.** `actions/checkout` leaves a detached HEAD, so the handoff guard returns early and **never executes in CI**. Agrees with a1's S10, reached independently. |
| M6 | MED | The repoint plumbing must sit in the **last** commit or the tree goes red, and **nothing can move it back** — the only repair is a forbidden force-push. A role run gets one green-tree state and its next commit ends it. |
| L1 | LOW | `amends_without_owning` is a single mutable slot, so `check:scope` is not reproducible against history in **either** direction (§1.2). |
| L2 | LOW | A protected path (`.github/workflows/ci.yml`) was amended on the Author's own manifest declaration, with no Integration Owner; no validator enforces the protected-path rule. |
| L3 | LOW | "one untyped variant per platform" in a table comment should be "per (version, platform)". |

---

### H1 — HIGH. Four artifacts cite RFC-2026-017 §3 for the inverse of what it decides

**Thread 1 and 2. This is the most important finding in this document.**

`db/foundation/migrations/080_content.sql:111-115`:

> When a command function exists — RFC-2026-021 §10 records that none does — it will insert as its
> owner and **be exempt from these policies**, which is what RFC-2026-017 §3 arranges by keeping
> `app_command` **off** the table-owner role.

and again at `080_content.sql:895-899`:

> the act that CREATES one is a SECURITY DEFINER command function owned by `app_command`, **which is
> exempt from these policies by being the owner** rather than by holding a privilege
> (RFC-2026-017 §3).

Now `architecture/decisions/RFC-2026-017-service-path-identity.md:55-58`, **Approved 2026-09-05 by
the Product Owner**:

> `app_command` is deliberately **not** the table owner. A `SECURITY DEFINER` function executes as
> its owner, so a function owned by the table owner on a forced table **is subject to RLS and needs
> policies that name it** — which is the intended behaviour, and only holds if the owner is a role
> the policies can name.

and §4, "What this costs": *"Every service operation needs a policy. Writing the table is no longer
the whole job."*

**The cited decision says the opposite of the claim that cites it.** The migration says the function
is *exempt*; the RFC says it is *subject to RLS and needs a policy naming it*.

**The sentence also refutes itself.** Exemption from RLS, where it exists at all, comes from *being
the table owner*. The migration claims exemption is "arranged by keeping `app_command` **off** the
table-owner role". Those cannot both be true.

**And the migration contradicts itself, 950 lines apart.** `080_content.sql:770-773`:

> Without FORCE the table owner is exempt from every policy

— correct, and it entails that *with* FORCE (set at `:446-459` on all five tables) the owner is
**not** exempt. Then `080_content.sql:1060-1066`, the last assertion in the file:

> It matters more here than in most batches, because the writer this family is waiting for is a
> SECURITY DEFINER function owned by app_command: **if app_command also owned the table, that
> function would be exempt from the policies above by ownership** and the narrowings would bound
> nothing it does.

So the file asserts, as the reason for its final control, that ownership *would* confer exemption —
which is what `FORCE` is there to prevent, and which §1060's own sibling at `:770` denies.

**Where the error propagated.** It is not confined to comments:

- `work-packages/WP-0A-DB-00.json` `open_blockers`, the new blocker beginning "NOTHING IN THIS
  REPOSITORY CAN WRITE A CONTENT VERSION…": *"exempt by OWNERSHIP rather than by privilege
  (RFC-2026-017 §3)"*.
- `handoffs/WP-0A-DB-00-author-handoff.json` and
  `evidence/WP-0A-DB-00/session-2026-09-13-batch-080-landed-fanout-cut.md` §2 carry the same
  sentence.

**Why it matters, beyond being wrong.** The entire design of the three immutable tables is "no grant,
no policy, and a command function will be the writer later". If the mechanism is as RFC-2026-017 §3
states, then a command function **alone is not sufficient**: `app_command` holds no grant here
(asserted absent at `:790-808`) and no INSERT policy names it (asserted absent at `:817-826`, and
actively forbidden — see **M2**). The promised writer will need a *forward migration* adding both.
The batch prices its deferral as "waiting for a command function"; the true price is "waiting for a
command function **and** a forward migration that grants and polices it". That difference is not
recorded anywhere.

**Not repaired here.** Migration invariant 1 forbids rewriting a merged migration, so the comment
cannot be edited; the honest fixes are a forward-fix note and corrections to the manifest blocker,
the handoff and the state record, none of which is the Reviewer's to make.

**Note on provenance.** The session state record §3 already lists this as the question
`/claude/a1_bastion` was spawned to press —*"it may over-state the mechanism: FORCE binds the table
owner too, and `app_command` is not the table owner"*. That run was cut before it read anything. The
claim reached `main` unchallenged, and the Security/Privacy review this package owes is still owed.

---

### H2 — HIGH. "Nothing in this repository can write a content version, variant or quality review" is false against the tree

**Thread 2, stated by the brief as the claim to test literally.**

The claim appears in `080_content.sql:911-913` (the "cost is stated rather than discovered"
paragraph), in the manifest blocker, in the handoff, and in the state record §2.

**It is false.** `tests/db/identity/fixtures/080-content-fixture.sql` writes all three tables:

- `:171` `insert into app.content_versions` — 5 rows
- `:209` `insert into app.content_variants` — 3 rows
- `:231` `insert into app.quality_reviews` — 3 rows

These are not hypothetical and not rolled back. The fixture opens `begin;` at `:90` and `commit;` at
`:249`. It is loaded once, outside the case loop, at `scripts/db/rls-smoke.mjs:198-200`, from the
list at `tests/db/identity/run-isolation.mjs:115`. Only the isolation *cases* roll back
(`run-isolation.mjs:346`). So on every CI run, rows are committed into all three "unwritable" tables.

**The fixture's own explanation of why it can do this is wrong, and that is the part that matters.**
`080-content-fixture.sql:81-84`:

> THIS FILE LOADS ADMINISTRATIVELY, **as the table owner**, before any identity is assumed, and on
> three of these five tables **that is the only way there is**

Being the table owner is *not* what makes it work. `FORCE ROW LEVEL SECURITY` is set on all five
tables at `080_content.sql:446-459`, and the migration states its purpose at `:770-773`: FORCE binds
the owner. A non-superuser table owner writing into a forced table with **no INSERT policy** is
refused. The fixture succeeds because the connection role bypasses RLS altogether:
`.github/workflows/ci.yml:112-113` connects as `postgres` against `postgres:17`
(`.github/workflows/ci.yml:22-26`), where `postgres` is the **cluster superuser**, and
`scripts/db/psql-driver.mjs:150-153` issues no `set role`.

**Why the distinction is load-bearing and not pedantry.** The design's promised writer is a SECURITY
DEFINER function owned by `app_command`. Per `RFC-2026-017-service-path-identity.md:46-50`,
`app_command` holds **`BYPASSRLS`: no**, and it is not a superuser. So the fixture is *not* a
demonstration that the promised writer will work — it works by a mechanism `app_command` will never
have. Read together with **H1**, the two artifacts that appear to reassure a reader about the future
writer both rest on the same misunderstanding.

`scripts/db/run.mjs:1056-1059` already states the correct principle for a different role: *"a
superuser bypasses row level security whatever rolbypassrls says, so every policy written for this
role is inert."*

**What I checked and did not find**, so the claim's remaining scope is clear — there is no *other*
path in. No migration other than 080 names the three tables; no SECURITY DEFINER function inserts
into them (the four in the tree are `private.set_updated_at`, three read-only authz helpers at
`011_authorization_helpers.sql:237,261,286`, and an audit helper at `140_audit.sql:365`); no trigger
or rule on them; no `alter default privileges` anywhere in the repository; no
`grant … on all tables in schema`.

**The precise true statement** is the one the fixture half-reaches and the migration does not: *no
granted client or service path can write these tables; the only writer is a role that bypasses RLS
entirely, and the fixture uses one.* The blanket sentence in four artifacts is not that.

---

### M1 — MEDIUM. The `app_worker` departure retires a security canary, and that consequence is stated nowhere

**Thread 1. The brief asked whether the consequence is "fully stated and fully priced". Three
consequences are stated well. A fourth is not stated at all.**

Batch 080 grants `app_worker` nothing on all five tables and asserts the absence at
`080_content.sql:905-915`. The reasoning given (`:892-899`) is that a worker with write grants would
be a second path to an act belonging to a command function. **That reasoning is sound for INSERT,
UPDATE and DELETE. It does not reach SELECT — and SELECT is withheld too, and asserted absent at
`:910`.**

Batch 070 states why SELECT was there, at `070_research.sql:1166-1170`:

> `app_worker` HOLDS GRANTS AND NO POLICY, on all five, which is the shape batch 010 introduced and
> every batch since has kept, for the reason 010 gives: without a grant a service refusal is 42501
> either way and **proves only that somebody forgot a GRANT**; with the grant and no policy, an
> empty read can only have come from row level security, and **a service role that had quietly
> acquired BYPASSRLS would SUCCEED where the suite demands a refusal.**

That is a *detection* argument, not a *capability* argument, and a read grant creates no write path.
Under 080's shape, an `app_worker` that silently acquired `BYPASSRLS` would still be refused with
`42501` on all five content tables — because privilege is checked before RLS and `BYPASSRLS` does
not confer privilege — so the suite passes either way and **the regression is undetectable on these
five tables.** Batch 070's tables keep the canary; the content family does not.

Measured:

```
grep -c 'BYPASSRLS\|bypassrls' db/foundation/migrations/080_content.sql   → 0
grep -c 'BYPASSRLS\|bypassrls' db/foundation/migrations/070_research.sql  → 1
```

**Zero occurrences.** The departure is discussed at length in four places — the migration header, the
apply-time comment, the CI comment, the manifest blocker — and the loss of the canary is named in
none of them.

To be fair to what *is* stated: `.github/workflows/ci.yml` (the batch 080 block) is explicit and
correct that the `service-cannot-*` cases are **not** part of the negative-control basis, and says
why. `tests/db/identity/isolation-cases.mjs` labels every such case `deniedBy: 'grant'`
(279 occurrences repository-wide against 62 `deniedBy: 'policy'`). Both claims in the brief about
labelling are true. The unstated item is the BYPASSRLS canary specifically.

**Relatedly**, `RFC-2026-017-service-path-identity.md:98-105` §7 says the negative assertion — the
service identity denied *with an error, not an empty result* — "is owed by whoever writes the first
tenant table". A `42501` is literally an error, so 080's cases satisfy §7's letter; they cannot
satisfy its purpose, because a privilege error is produced identically whether or not the decision
has regressed. The state record's phrasing "none carries RFC-2026-017 §7" is the right conclusion; I
record that it is right for a sharper reason than given.

---

### M2 — MEDIUM. The apply-time block forbids the policy RFC-2026-017 §3 says the writer needs

**Thread 4.** `080_content.sql:943-953` raises if any policy on the five tables names
`anon`, `app_worker`, **`app_command`**, `app_maintenance` or `app_authz`:

```
raise exception 'batch 080 left a policy for a role that is not authenticated: %', offending;
```

The stated justification (`:935-942`) is that RFC-2026-022 carries service policies only for cells
the §8 matrix marks `S`, and content has no `S` cell.

That justification conflates two different identities. `app_worker` is the *service* path the `S`
cell governs. `app_command` is the *command function* path, which RFC-2026-017 §3 puts on an
entirely different footing and which §4 says "needs a policy". The assertion bans both alike.

The practical effect is bounded — the block runs only at 080's apply time, so a later forward
migration can still add the policy — but it encodes, as an enforced invariant with a hint explaining
why it is right, the inverse of the approved decision's requirement. Combined with **H1**, a reader
reconstructing the intended end state from this file will build the wrong one.

---

### M3 — MEDIUM. Probe 18's defect class is still open; I re-opened it and the suite stayed green

**Thread 4. The brief asked me to verify A0's self-criticism independently. A0's self-criticism is
accurate and honest — and understates the problem.**

`evidence/WP-0A-DB-00/a3-batch-080-probes-2026-09-12.md:92-106` records probe 18: the immutability
assertion was narrowed from three tables to one, nothing noticed, and the fix is a count. A0 labels
it *"a cheaper control than the defect deserves"* and states the limit. That is all true.

The guard, at `tests/db/identity/identity-isolation.test.mjs:8861-8870`, is two assertions: the
declaration `immutable_tables constant text[] :=` must be *present*, and `any (immutable_tables)`
must appear **exactly twice**.

**Neither pins the contents of the array.** So the identical defect is reachable by editing the
declaration instead of the call sites. I did it:

```
# db/foundation/migrations/080_content.sql:767
-    array['content_versions', 'content_variants', 'quality_reviews'];
+    array['content_variants'];

node --test tests/db/identity/identity-isolation.test.mjs
  → exit 0 — tests 232, pass 232, fail 0
```

Both guards still pass: the marker is present, and `any (immutable_tables)` still appears twice. The
apply-time assertion still runs, still passes on a correct database, and now checks **one of the
three tables §8.2 row 3 names** — the immutability of `content_versions` and `quality_reviews` is
asserted by nothing. That is probe 18's defect, after probe 18's fix, by a one-line edit.

*(I restored the file immediately; the tree was verified clean before committing. The full
`npm run check` under mutation is inconclusive for a different reason — `test-kits/integrity-manifest.json`
digests the migration, so byte-level drift exits 86 — but a digest pins bytes, not behaviour, and a
legitimate forward migration edit would carry a regenerated digest with it. The file-level guard
written to catch this specific class does not catch it.)*

The cheapest control that would: pin the three names, e.g. assert the declaration line matches
`/array\['content_versions', 'content_variants', 'quality_reviews'\]/`.

---

### M4 — MEDIUM. No `set_updated_at` trigger, and `updated_at` is inside the client UPDATE grant — the only batch in the repository to do either. This is my candidate seventh blocker

**Thread 6. This came from outside the brief.**

`app.content_ideas` and `app.content_items` are mutable and both carry
`updated_at timestamptz not null default now()` (`080_content.sql:205-206`, `:218-219`).

```
# create trigger, per migration
010:5  020:2  021:1  030:2  040:1  050:2  051:3  060:3  061:2  070:3
080:0   110:4  130:2  131:2  140:4
```

**080 is the only migration that creates mutable tenant tables and attaches no trigger at all.**
Batch 070 attaches three (`070_research.sql:1071-1081`).

The second half is sharper. Across the whole repository, `updated_at` appears inside an UPDATE grant
in exactly seven places — and in six of them the grantee is `app_worker`:

```
grep -n 'grant update' db/foundation/migrations/*.sql | grep 'updated_at'
060:624  … to app_worker;      070:1198 … to app_worker;     070:1214 … to app_worker;
061:993  … to app_worker;      061:1006 … to app_worker;
080:482  grant update (goal, topic, brief, status, updated_at, updated_by) … to authenticated;
080:496  grant update (title, updated_at, updated_by, deleted_at)          … to authenticated;
```

Batch 070 deliberately keeps `updated_at` out of its *client* grant (`070_research.sql:1145`:
`grant update (cancel_requested_at, updated_by) … to authenticated`) because the trigger owns the
column. Batch 080 has no trigger, so it hands the column to the client instead.

**Consequence:** on the two mutable content tables, `updated_at` is not maintained by the database
and any `authenticated` caller holding the row's UPDATE path may set it to an arbitrary value,
including backdating it. It is a provenance column that no longer records what it claims to record.
Nothing detects this: no lint rule, no apply-time assertion, and no test references
`private.set_updated_at` outside `db/foundation/lint/catalog-snapshot.json:180`.

Given the care this batch takes elsewhere about provenance columns — `created_by` is excluded from
both UPDATE grants "so an idea cannot change hands" (`:472-473`), and `client_request_id` is in the
apply-time unwritable list because "a key a client can rewrite after the fact identifies nothing"
(`:872-875`) — the omission reads as unintentional rather than decided. I found no sentence anywhere
in the batch that mentions it.

**On the six blockers that were added:** I diffed the lists and they are accurate and unusually
well-evidenced; **none was removed** (`prev=376 curr=382` lines of blocker text; `comm -23` empty).
Two carry the **H1** error into the manifest. This is the seventh I would add.

---

### ~~H3 — HIGH~~ → **WITHDRAWN**, and what replaces it: **M5** and **M6**. Corrected in place twice, 2026-09-13

**This entry has been wrong in two different directions and I am recording the whole sequence rather
than the tidy end state, because which claims died and how is the useful part.** Following
`3cd1617`'s precedent: corrected in place, originals restated, nothing quietly softened.

**Round 1 — what I originally claimed.** That an independent role run *cannot open a green PR at
all*: that repointing `ownership.branch` turns `npm run check` red at 584/586, and "the only remedy
rewrites A0's handoff to cite my work". I inferred a structural impossibility from one reading.

**Round 2 — the coordinator falsified it, and was right that it was wrong.** `/claude/a1_bastion`
repointed, did not touch the Author's handoff, and its CI concluded success (`34751714594`, head
`de1e887`; `gh pr diff 114 --name-only` contains no `handoffs/` path — both re-measured by me). My
584/586 reading had been taken with **zero commits on the branch**, when `HEAD` was still the merge
commit at the tip of `main`; for a merge whose parents are both already in the integration branch
`branchTipBefore` returns the *first* parent, and `d6e08c7` is not an ancestor of it, so
`driftBetween` returned `unrelated`. An artifact of measuring too early.

**Round 3 — the coordinator then retracted that, and asked me to restore H3 at HIGH in a sharper
form: "an independent role run's branch cannot hold a green tree past its first commit without
rewriting the Author's handoff." I decline, because I am a counter-example to it.** Measured on this
branch, on the real branch name (`git symbolic-ref -q HEAD` →
`refs/heads/agent/claude/WP-0A-DB-00-c0-review-080`, not detached), at **five** commits, repointed,
with `handoffs/WP-0A-DB-00-author-handoff.json` untouched:

```
npm run verify                              → clean: exit 0 — tests 586, pass 586, fail 0
node --test test-kits/handoff-conformance.test.mjs
  ✔ the handoff for this branch describes this branch     ← the very test that fails on a1
  ℹ tests 19  ℹ pass 19  ℹ fail 0
```

A branch at five commits, green on the tree, is not a branch that "cannot hold a green tree past its
first commit". **H3 stays withdrawn.** Restoring it because the coordinator now prefers it, against a
measurement I can re-run, would be the manufactured consensus the coordinator itself warned against
one paragraph later.

**What is actually true — one rule that explains every observation, including the coordinator's.**
The guard compares the handoff's cited revision against `branchTipBefore()` = `HEAD^`, and
`WRITTEN_AFTERWARDS` (`scripts/refresh-author-handoff.mjs:36-41`) exempts `^handoffs/`, `^evidence/`,
`OVERNIGHT-SUMMARY.md` and `test-kits/integrity-manifest.json` — but **not**
`test-kits/branch-identity.test.mjs` and **not** `work-packages/**`. So the branch is green exactly
when the repoint plumbing is **not yet in `HEAD^`**, i.e. when it is the **last** commit. Computed
with the guard's own exported functions:

```
driftBetween(d6e08c7, dac8b54)  → substantive=[]                          ← my HEAD^ today: GREEN
driftBetween(d6e08c7, f87bb1f)  → substantive=["test-kits/branch-identity.test.mjs",
                                               "work-packages/WP-0A-DB-00.json"]
driftBetween(d6e08c7, de1e887)  → substantive=[…same two…]                ← a1's HEAD^ at 78774e2
driftBetween(d6e08c7, 78774e2)  → substantive=[…same two…]
```

One rule accounts for a1's red (plumbing in commit 1 of 3), for my green (plumbing in commit 5 of 5),
and for the coordinator being wrong in both directions. The coordinator's "red from the second commit
onward" is right about a1 and wrong as a general rule: it is not the commit *count*, it is the
*position* of the plumbing.

**M6 — MEDIUM. It is a one-way door, and that is the part worth keeping.** Being green requires the
plumbing to sit in the last commit, and **nothing can move it back there.** `driftBetween` ranges
over `d6e08c7..HEAD^`, so once the plumbing is anywhere in that range, re-touching those files in a
later commit does not remove them from it. The only repair is rewriting history, and
`CONTRIBUTING_AGENTS.md` and my own brief forbid force-pushing. **So a role run gets exactly one
green-tree state, and its next commit ends it permanently.**

I am about to demonstrate this on myself, which is the cleanest evidence available: committing this
correction puts `f87bb1f` into `HEAD^`, and the line above predicts `substantive=["test-kits/branch-identity.test.mjs",
"work-packages/WP-0A-DB-00.json"]` — a red tree I cannot repair without a force-push. The measured
result is recorded in §7. A guard whose only remedy is forbidden is a guard that will be worked
around, and the available workaround is `npm run refresh:handoff` on somebody else's handoff.

**M5 — MEDIUM. CI and the tree disagree on the same commit, and CI is the green one.** This is the
finding the whole episode actually produced, and I reached it independently while diagnosing round 2,
before the coordinator raised it. `actions/checkout` leaves a detached HEAD — from a1's own run log,
`"You are in 'detached HEAD' state"`, `HEAD is now at 8e03a82 Merge de1e887… into c5eb1b9…` — and
`test-kits/handoff-conformance.test.mjs:233` opens with `git rev-parse --abbrev-ref HEAD`, which
returns the literal string `HEAD` when detached. No package declares `ownership.branch: "HEAD"`
(checked: zero across `work-packages/*.json`), so `resolved.code !== 0` and **the test returns early
on every CI run.** Reconstructing the exact merge commit CI built for a1
(`git commit-tree de1e887^{tree} -p c5eb1b9 -p de1e887`) shows the guard resolves the branch side
correctly and *would* object: `branchTipBefore → tip=de1e887`, `substantive=[…the same two…]`.

The coordinator has since measured the same divergence from the other end: a1's branch at `78774e2`
reads `NOT clean: exit 1 — tests 586, pass 584, fail 2` on the branch name, while CI run
`34752341453` on that identical commit concludes **success**. I have not re-run a1's tree myself —
I did not check out a sibling's branch — so I record those two numbers as the coordinator's
measurement, corroborated by my own computation above, which predicts exactly that red.

**Why it matters under the rules rather than as a curiosity.** RFC-2026-002 lets a Product Owner
merge on "a green required CI run" plus linked role evidence. A guard that is red on the tree and
green in CI means the required run can be green for a branch whose author handoff genuinely does not
describe it — the property the guard exists to enforce is enforced by nothing in the run that gates
the merge. `/claude/a1_bastion` has filed this as its S10; **I reached it independently and I agree
with it**, which is worth stating precisely because two same-vendor runs agreeing is weak evidence
and should be labelled as such rather than counted twice.

**The downstream inference stays withdrawn, and here I disagree with the coordinator's retraction.**
H3 concluded this was "a plausible mechanical reason the four role signatures have been absent on
every increment in this package's history". The coordinator now says that stands. **On my
measurements it does not.** I produced a repointed branch with a green tree and, at the previous
head, reached CI normally — without touching anyone else's artifact; a1 produced complete security
findings and a green CI run. Neither run was *prevented* from delivering evidence. What M6 describes
is friction after the fact — a local suite that goes red once you commit again — not a barrier to
producing or pushing the evidence in the first place. Attributing the absent signatures to a
mechanism, when two role runs in this very fan-out delivered their evidence, would be using a
tooling defect to excuse a governance gap. The state record §1 says the signatures were not produced;
that remains the explanation the evidence supports, and `CONTRIBUTING_AGENTS.md` and RFC-2026-002
still require them.

---

### L1 — LOW. `amends_without_owning` is a single mutable slot, so ownership history is not reproducible

See §1.2. `scripts/verify-branch-scope.mjs` reads the manifest at the working tree and compares it
against a historical range, so every past increment's declaration is overwritten by the next one.
The slot fails in **both** directions, and I measured each:

```
# too few: declarations the current manifest has lost
node scripts/verify-branch-scope.mjs 49830ad~1 WP-0A-DB-00   → exit 73
  WP-0A-DB-00 changed 3 path(s) it neither owns nor records as an amendment:
    .github/workflows/ci.yml   evidence/VERIFICATION.md   scripts/test-suite-contract.mjs

# too many: declarations this increment does not use
node scripts/verify-branch-scope.mjs c5eb1b9 WP-0A-DB-00     → exit 74
  WP-0A-DB-00 declares 3 amendment(s) that explain nothing this branch changed:
    OVERNIGHT-SUMMARY.md  test-kits/branch-identity.test.mjs  test-kits/integrity-manifest.json
```

Exit 73 is against batch 080 for paths it legitimately declared at merge; exit 74 is against my
branch for paths the *previous* increment declared. Neither reflects a real overreach. Anyone
auditing history with this tool will produce false positives against every increment but the most
recent, in one direction or the other.

### L2 — LOW. A protected path was amended on the Author's own declaration

`CONTRIBUTING_AGENTS.md` §"Ownership and change control": *"Root configuration, lockfiles, **CI**,
contract catalog, migration registry, composition root, and source-of-truth documents are protected.
Propose changes through the Integration Owner/RFC path."*

Batch 080 added 31 lines to `.github/workflows/ci.yml`. The content is good — the negative-control
block is well-documented and honest about what it does not cover (see **M1**). The route is the
issue: the change was authorised by the Author writing the path into its own manifest's
`amends_without_owning`, and the state record §1 records that the Integration Owner's signature was
absent when #112 merged. `npm run validate:protocol` exits 0, so **no validator enforces the
protected-path rule at all** — a manifest self-declaration is sufficient to edit CI. That gap is
worth naming independently of this batch.

### L3 — LOW. A table comment is imprecise where the assertion hint is exact

`080_content.sql:374-375` (the `comment on table` text): *"exactly one untyped variant per platform
is legal"*. The key is `(workspace_id, business_profile_id, content_version_id, platform,
variant_type)`, so the true statement is one untyped variant per **(version, platform)**. The
apply-time hint at `:971-973` says it correctly (*"two untyped variants of one version on one
platform"*), and so does the inline comment at `:359`. Only the durable artifact — the comment
stored in the database — is loose.

---

## 3. Threads that check out

### Thread 3 — `unique nulls not distinct` — CONFIRMED SOUND, for every case including the untyped one

`080_content.sql:362-364`:

```sql
constraint content_variants_logical_key
  unique nulls not distinct
    (workspace_id, business_profile_id, content_version_id, platform, variant_type),
```

I checked the nullability of every key column (`:331-338`): `workspace_id`, `business_profile_id`,
`content_version_id` and `platform` are all `not null`; `variant_type` is the only nullable member.
So `NULLS NOT DISTINCT` changes behaviour for exactly one column, which is the intended one, with no
side effect on the others.

- two rows, same version+platform, both `variant_type` null → collide → refused. **The untyped case
  now holds**, and under the Postgres default it would not have.
- two rows, same version+platform, `variant_type` `'a'` vs `'b'` → distinct → allowed, which is what
  "unique logical variant key ต่อ content version/platform" asks for.

The rule §4.6 asks for holds for every case. The apply-time check reads `indnullsnotdistinct` from
`pg_index` (`:959-974`), which is the flag that actually decides it. `UNIQUE NULLS NOT DISTINCT`
requires Postgres 15+; CI is `postgres:17`.

### Thread 4 — the assertions do assert what the header says, on all three tables, in both directions

Subject to **M3**, this thread is otherwise sound:

- **Grants direction**, `:790-808`: `immutable_tables` × `every_role`, using
  `has_any_column_privilege` for INSERT/UPDATE and `has_table_privilege` for DELETE. The
  column-scoped form is correct and is the trap 061 recorded from 060.
- **Policies direction**, `:817-826`: `polcmd in ('a','w','d')` over the same three tables, with `*`
  deliberately excluded so the restrictive FOR ALL narrowing is not caught.
- Both halves cover **all three** tables; the header's "both ways" is honest.
- The narrowing inspection `:995-1041` iterates **both** `polqual` and `polwithcheck` for all five
  policies, with a per-table content check on each half. This is the strongest control in the file
  and is the right response to 040's probe.

### Thread 5 — `SMOKE_COVERAGE[3]` — CONFIRMED SOUND, and A0 understated it

The claim was eight assertions rewritten, none deleted. Verified independently:

```
git grep -c "SMOKE_COVERAGE\[3\].covered, 'knowledge-half'" 49830ad~1 -- …identity-isolation.test.mjs → 8
git grep -c "SMOKE_COVERAGE\[3\].covered, true"             49830ad   -- …identity-isolation.test.mjs → 9
git diff 49830ad~1...49830ad -- …identity-isolation.test.mjs | grep -c '^-.*test('                     → 0
```

Eight before, nine after (the eight rewritten plus one new test of batch 080's own), **zero tests
deleted**. All eight remain `assert.equal` against a single literal — none was downgraded to a
truthiness, membership or count check. One neighbouring assertion was **strengthened**: a bare
`assert.equal(partials, 2)` became `assert.deepEqual(partials, ['8'])`, i.e. a count became a set
identity naming which key remains partial. The floors moved up by exactly the measured delta
(218→232 tests, 1607→1686 assertions) and no floor was lowered.

Nothing was weakened or deleted. One note only, not a finding: `assert.equal` is `==`, so
`assert.equal(x, true)` admits `1` where `assert.equal(x, 'knowledge-half')` admitted essentially
one string. The strict consumers at `identity-isolation.test.mjs:543` and
`scripts/db/rls-smoke.mjs:265` close this in practice.

### Thread 6 — the six blockers are accurate

Diffed in full; all six are supported by the tree, none was removed, and they are more candid than
most such lists (the `findings` CHECK blocker volunteers that jsonpath has no trim and `" "` passes).
Two of them carry **H1**'s error. My proposed seventh is **M4**.

### Thread 7 — ownership

Batch 080 was compliant at merge (§1.2). `npm run validate:protocol` → exit 0.

### The CI evidence cited in the state record is accurate

```
gh run view 34678716740 → {"conclusion":"success","headSha":"3cd1617…","status":"completed",
                            "workflowName":"Bootstrap validation"}
```

Matches the record's claim exactly, including the head SHA.

---

## 4. Stop-the-line assessment

**No finding in this document is stop-the-line under `CONTRIBUTING_AGENTS.md`.** That section
enumerates: *secret exposure, tenant leakage, duplicate external side effects, lost jobs, migration
divergence, irreversible deletion, or contract mismatch.* I checked each against my findings and
none matches.

The closest call is **H1**, and I want the reasoning visible rather than assumed. It could be read as
"contract mismatch": an approved RFC is cited for its own inverse, in a merged migration that
invariant 1 forbids editing. I do **not** call it stop-the-line, for two reasons: this package
declares `contracts_consumed: []` and `contracts_produced: []`, so no catalog contract is in scope;
and the defect is in comments and prose, with a forward fix available (a later migration granting and
policing `app_command`) rather than an irreversible state. **A different reviewer could reasonably
decide otherwise, and if the Product Owner reads "contract" to include an approved decision record,
H1 qualifies.** I record the disagreement rather than resolve it.

What I will say plainly: **H1 and H2 must be dispositioned before anything about batch 080 is called
`review_approved`.** Four artifacts currently tell a reader something untrue about how the promised
writer will work, and the batch's central deferral is priced on that understanding.

**The paragraph that stood here about H3 is withdrawn**, including its claim that H3 "partly
explains why the four independent signatures RFC-2026-002 requires have never been delivered". A
coordinator later asked me to reinstate that inference; I declined, and the reasoning is at the H3
entry. Two role runs in this fan-out delivered their evidence, so a mechanical barrier is not what
the evidence shows. The four signatures are owed, and nothing in this document excuses their absence.

**`M5` and `M6` are the control-gap findings that survive**, and neither is a defect in batch 080. The
sharper of the two is `M5`: a guard inside the suite RFC-2026-002 requires to be green **never
executes in the run that is required to be green**, so the required run can be green for a branch
whose author handoff does not describe it. Neither meets the stop-the-line list, and `M5` is the one
I would put in front of the Integration Owner first.

---

## 5. What I did not check

- **Any database reading.** This machine has no PostgreSQL. Nothing in this document is a
  measurement of a running instance. Every apply-time assertion in `080_content.sql` is verified
  here **by reading only**, and A0's own statement stands unimproved: *"the apply-time block is
  verified by review and by nothing else."* CI is the first Postgres, and CI proves only that the
  assertions do not raise — which a gutted assertion also does not (**M3**).
- **The 79 isolation cases, one by one.** I confirmed the `deniedBy` labelling and counted; I did not
  hand-simulate them. That is `/claude/q0_sentinel`'s assignment and it has not run.
- **The security review.** **H1** is squarely in `/claude/a1_bastion`'s territory and I have taken it
  only as far as reading the RFC against the migration. A Security/Privacy review is still owed and
  this document does not substitute for it.
- **The five RESTRICTIVE narrowing predicates as SQL.** I verified the apply-time block *inspects*
  both halves of all five; I did not evaluate whether each predicate is correct against §4 invariant
  3 for every membership shape.
- **The jsonpath `findings` CHECK**, beyond confirming the blocker's account of its limits is honest.
- **The 11 catalog symbols in the fixture**, the `fixture-catalog.json` changes, and whether the
  three "natural key" arguments hold.
- **Batches 110–140** except to confirm none touches the three immutable tables.
- **Whether the six blockers' *owners* are right** (A2 Research, A3 Content, Product, batch 090) — I
  checked the technical content, not the assignment.
- **And what neither A0 nor I thought of.** A0 chose these threads. One of my ten findings (**M4**)
  came from outside them, and I found it by accident while counting something else. I cannot
  enumerate the rest of that category; I can only report that it is not empty.

---

## 6. Commands run, verbatim

```
git log --oneline -3
  c5eb1b9 Merge pull request #113 …session-handoff-2026-09-13
  5af68af chore(protocol): the handoff cites the commit it describes
  d6e08c7 docs(evidence): where the session ended …

node --version → v24.20.0      npm --version → 11.19.0

npm run verify
  clean: exit 0 — tests 586, pass 586, fail 0, skipped 0, todo 0

npm run check
  exit 0 — ℹ tests 586  ℹ pass 586  ℹ fail 0  ℹ skipped 0  ℹ todo 0

npm run validate:protocol                                  → exit 0
node scripts/verify-branch-scope.mjs 49830ad~1 WP-0A-DB-00  → exit 73  (see L1)
node scripts/verify-branch-scope.mjs c5eb1b9   WP-0A-DB-00  → exit 74  (see L1)
node scripts/verify-branch-identity.mjs agent/claude/WP-0A-DB-00-c0-review-080 → exit 75  [1]
node scripts/verify-disposition-branch.mjs c5eb1b9          → exit 78  [1]
gh run view 34678716740                                    → success, head 3cd1617
```

Mutation probe for **M3** (file restored immediately; tree verified clean before commit):

```
sed -i '' "767s|.*|    array['content_variants'];|" db/foundation/migrations/080_content.sql
node --test tests/db/identity/identity-isolation.test.mjs   → exit 0 — tests 232, pass 232, fail 0
cp /tmp/080.bak db/foundation/migrations/080_content.sql
```

`[1]` These two were measured while the branch was deliberately **not** repointed, which was the
wrong call (withdrawn H3 / §1.1). They record what an *unclaimed* branch does and no longer describe
this branch: it is now repointed per the brief, so `verify-branch-identity.mjs` resolves it to
`WP-0A-DB-00` and the scope step is reached normally.

Branch: `agent/claude/WP-0A-DB-00-c0-review-080`, with `ownership.branch` repointed to it,
`BRANCH_OWNERSHIP` updated in both places and `test-kits/integrity-manifest.json` regenerated — the
three files `amends_without_owning` declares, plus this evidence file, which the package owns.

---

## 7. This document's own CI runs and tree readings, recorded after the fact

Draft PR: <https://github.com/ThinkBizLab-Org/ThinkBizThai/pull/115>.

**Runs `34751841373`** (head `8328651`) and **`34752106323`** (head `3580436`) both concluded
**failure**, each with one failing step — `Verify branch scope`, exit 75 — while
`Validate repository bootstrap` passed and the two database steps were skipped behind it. Those runs
were taken while the branch was deliberately not repointed, which was the wrong call.

**I originally wrote this section to say those runs "confirmed H3 exactly". They did not, and that
sentence is withdrawn.** They confirmed only that an *unclaimed* branch fails
`verify-branch-identity.mjs`, which was never in doubt and was never the interesting claim. Treating
my own failing run as evidence for my own finding was the error underneath the error.

The claim I made here that the skipped database steps disable the independent **Tester** also falls:
a1's run reached `Database foundation` and `Negative control` and both passed, so a repointed role
run does get a Postgres run. `/claude/q0_sentinel` is not blocked by this.

**The branch now follows the brief** — `ownership.branch` repointed, `BRANCH_OWNERSHIP` updated in
both places, `npm run regenerate:manifest` run, plumbing placed in the final commit per `M6`. At head
`f87bb1f`, on the branch name and not detached, the tree read:

```
npm run verify → clean: exit 0 — tests 586, pass 586, fail 0, skipped 0, todo 0
node --test test-kits/handoff-conformance.test.mjs
  ✔ the handoff for this branch describes this branch    ← passes here; fails on a1 at 78774e2
```

**`M6` demonstrated on this document, as predicted before it was measured.** The H3 correction is
committed *after* `f87bb1f`, which puts the plumbing into `HEAD^`. The prediction recorded at `M6`,
computed before committing, was
`driftBetween(d6e08c7, f87bb1f) → substantive=["test-kits/branch-identity.test.mjs",
"work-packages/WP-0A-DB-00.json"]`, i.e. a red tree with no repair available short of a force-push
this branch will not perform.

**Measured at head `7b9644e`, after that commit, on the branch name — the prediction holds exactly:**

```
npm run verify → NOT clean: exit 1 — tests 586, pass 584, fail 2, skipped 0, todo 0

✖ the handoff for this branch describes this branch
  AssertionError: WP-0A-DB-00's handoff cites head d6e08c7, after which 2 substantive path(s) changed:
    test-kits/branch-identity.test.mjs
    work-packages/WP-0A-DB-00.json
  The range is true and does not describe the branch. Run `npm run refresh:handoff`.
✖ the handoff ratchet fails when an author handoff claims another role approved something
```

The same two tests, and the same two paths, that the coordinator measured on a1. Note the last line
of the guard's own message: **the remedy it prints is `npm run refresh:handoff`** — rewriting the
Author's handoff, the one repair this branch refuses to make. A guard whose printed remedy is the
forbidden act is a guard that teaches the workaround, and that is the whole of `M6` in one line of
tool output.

This reading is recorded in a commit after the one it describes, which is the same reason the guard
compares `HEAD^` at all, and the reason `3cd1617` exists as a separate commit in this package.

So this branch ends in the state `M6` describes: **green CI, red tree, and no way back to green
without rewriting history.** That is the finding, performed rather than asserted. It is also why `M5`
matters: CI will not show it.

This section is written after the fact, following `3cd1617`'s precedent — evidence corrected in
place, with the run cited, when a measurement decides something the document asserts. Twice over
here, and both times against my own claims rather than someone else's.
