# Batches 022, 031, 042 — the three pre-080 families the S8 map reads as closable, closed in 082's shape (Draft for the Owner)

Run: `/claude/a0_atlas` (Author, A0 Integration). Date: 2026-09-15, after the third pass. Branch
`agent/claude/WP-0A-DB-00-pre-080-closures`. Written from
[`a0-pre-080-families-s8-map-2026-09-15.md`](a0-pre-080-families-s8-map-2026-09-15.md) §3 row 1 —
business/page, industry, knowledge: no `S` cell in RFC-2026-022 §3, `app_worker` holds grants no
policy admits, the narrowings are `for all to authenticated` and make the claim 080's did. **This
pull request is a Draft for the Owner's disposition**, because the two 2026-09-15 state records put
the pre-080 question to the Owner; it is built so that saying yes costs a merge and nothing else.

## 1. What the three files are

| File | Closes | Tables | Left open on purpose |
|---|---|---|---|
| `022_business_service_path_closed.sql` | 020, 021 | `business_profiles`, `business_profile_versions`, `page_context_profiles`, `page_context_profile_versions` | `workspace_member_scopes` (021's helper table, an `app_authz` question the map does not ask) |
| `031_industry_service_path_closed.sql` | 030 | `industry_assignments` | `industry_packs`, `industry_pack_versions` — global, no narrowing, no S8 claim |
| `042_knowledge_service_path_closed.sql` | 040 (041 adds no table) | `knowledge_items`, `knowledge_item_versions` | — |

Each is 082's shape exactly — RESTRICTIVE, FOR ALL, no TO, `current_user = 'authenticated'` in both
halves — held to it by `SERVICE_PATH_CLOSURES` (three rows added), with 082's claims 1, 2 and 4 and
**101's claim 3** (every family here grants `app_worker` verbs, so the premise is "no permissive
policy admits any role but authenticated", not "no grant"). One catalog case per table, seven in
all, found by the rule through the table parameter; the ids carry no family word because
`business`, `page`, `scope`, `industry-assignment`, `knowledge-item` and `knowledge-version` are all
CI control patterns.

**Not closed**: metering (061) and research (070). A closure there refuses the CARRIED worker
policies RFC-2026-022 §3 expects; the map's §3 rows (a)/(b)/(c) are the Owner's to pick.

## 2. Measured, scratch PostgreSQL 17.11 (fresh cluster each run)

| Run | Result |
|---|---|
| full set with the three files | `applied 022_…`, `031_…`, `042_…`; `schema-lint: ok`; **`854 isolation case(s) passed.`** = 847 + 7 |
| the same set **without** the three files | **`FAILED — 7 of 854`**, exactly the seven new cases ("nothing was visible") — no other case moves in either direction, so no existing service case depended on the path being open |
| static suites | identity 285/285; contract 60/60 |

**The S8 exploit, replayed on `knowledge_items`** (as 082's probes did on the content tables):
with the fixtures loaded (5 rows as `postgres`), `create policy probe_worker_reads on
app.knowledge_items for select to app_worker using (true)`:

| | `set role app_worker; select count(*)` |
|---|---|
| probe policy **and** `knowledge_items_service_path_closed` | **0 rows** — the closure ANDs against the probe |
| probe policy, closure dropped | **5 rows** — 040's narrowing `to authenticated` binds nothing for the worker |

That is the shape: a narrowing that claims to bound every writer and applies to one role. Before
this batch, the one thing between a service role and every tenant's rows on these seven tables was
that nobody had written the policy — and since the worker holds INSERT on all seven and UPDATE on
four (the map's §2 said SELECT; corrected after A1-pre-080 F1 and C0-pre-080 M1), a permissive
policy would have been a cross-tenant write, not only a read. A1 measured it: 5 rows in 2 tenants.

## 2a. Role runs on this branch (2026-09-15)

| Run | Verdict | Applied here |
|---|---|---|
| A1 Security `a1-security-batch-pre-080-2026-09-15.md` | no stop-the-line; 0 HIGH, 0 MEDIUM, 3 LOW, 3 INFO; premise confirmed against RFC-2026-022 §3, §8.1/§8.2 and all 40 policies' TO clauses; four exploit replays refused with the closures | F1 map corrected; F3 covers cells corrected (§8.1, §8.2; industry cites §8.5 and says why); F4 the truncated handoff sentence — a shell ate a backtick span in the plumbing script, restored |
| C0 Reviewer `c0-review-batch-pre-080-2026-09-15.md` | no stop-the-line; 0 HIGH, 1 MEDIUM, 7 LOW; ten static mutations noticed; the apply-time count block refused a mutated file live | M1 = A1 F1; L2 = F3; L3 dead `service_roles` constant removed from all three; L4 022's header attributions (narrowings 021's, grants 020's); L5 `workspace_member_scopes` sentence rewritten; L6 = F4; L1 and L7 stand (inherited from 082 / generator line length) |
| Q0 Tester `q0-test-batch-pre-080-2026-09-15.md` | no stop-the-line; 0 HIGH, 3 MEDIUM, 2 LOW; premise confirmed on all three legs; 854 / 7-of-854 / 285 / 60 / 643 as expected; 17 static probes, 12 noticed, 5 missed, the database caught 2 of the 5 | **F1** `SERVICE_PATH_CLOSURE_ON` now asks for the predicate too (`pg_get_expr` of both halves, as 17.11 renders it) -- all 19 closure cases, 082 to 042; measured: `alter policy … using (true) with check (true)` on `knowledge_items` goes **1 of 854 red** where before it stayed green. **F2** the query text is pinned by the static rule. **F3** the rule reads the apply-time sentences from code, not comments, and requires the raising do-block and the count check; Q0's P7 (block replaced by its description) now fails the rule by name. **Not caught, said plainly:** P6 (`false and` prefixed to claim 2's where) still passes the static suite -- an assertion made silent by an edit inside its predicate is a reviewer's to see; the live count block and the seven cases are the controls behind it. F4 = A1 F1. F5 stands. |

## 3. What this does not decide

Whether the three merge. The Owner's Q3 answered the shape for 080's family and A0 applied it to
081/090/100 in the same pull requests as those batches; the pre-080 families were merged before Q3
existed, and both state records list them as the Owner's call. Role runs (Reviewer, Tester,
Security) are launched on this branch as for 111, so that the package the Owner disposes is complete.
