# Q0-081 F2 — what actually refuses the sibling-target case, measured, and two whys corrected

Run: `/claude/a0_atlas` (Author). Date: 2026-09-15. Finding: [`q0-test-batch-081-2026-09-15.md`](q0-test-batch-081-2026-09-15.md) F2 (MEDIUM): the case `pinned-editor-a-cannot-see-the-content-target-of-a-sibling-target-item` said a narrowing that asked only the Business question "would leak this one". It would not: the narrowing's subquery runs as the page editor, and `app.content_items`' own narrowing hides the sibling item from that identity, so the `exists()` is false whatever 081's `case … end` asks.

## 1. Measured on the scratch PostgreSQL 17.11

Q0's probe **P1** replayed: `content_targets_scope_narrowing` re-created with the Page question deleted from **both** halves (`app.member_scope_admits_business(i.workspace_id, i.business_profile_id)` in place of the `case`), then the suite — **`844 isolation case(s) passed.`** No case noticed, the sibling-target read case and the write-half case (F3, #136) included. 081's Page branch is observable by no case while the item's policy answers first; it is held by the static rule and the apply-time text check, and nothing else.

## 2. The change

Two `why`s in `tests/db/identity/isolation-cases.mjs`, corrected with the old sentence quoted: the read case above, and the F3 write case (`…-cannot-aim-a-content-target-at-a-sibling-target-item`), which this session had written yesterday-evening attributing the refusal to "the Page branch" — also false by the same measurement, and corrected in the same change rather than left because it was mine.

Nothing else changes; 081's apply-time hint (`081:679-690`) makes the same claim and cannot be edited.

## 3. What follows

A case that exercises 081's Page branch itself would need a content item the page editor CAN see whose target should nonetheless be refused — which does not exist, because a target inherits its item's reach by design. That is the property, not a gap: the item's policy is the one that matters, and 081's branch is defence in depth held by text.
