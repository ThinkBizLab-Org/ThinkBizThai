# Q0-081 F3 closed — the write half of §8.6 case 4 on content_targets, and what the probe behind it actually reaches

Run: `/claude/a0_atlas` (Author). Date: 2026-09-15. Finding: [`q0-test-batch-081-2026-09-15.md`](q0-test-batch-081-2026-09-15.md) F3 (MEDIUM): no case had the page-scoped editor aim a target at the sibling-page item, so the narrowing's WITH CHECK Page branch was exercised by nothing.

## 1. The change

One case in `tests/db/identity/isolation-cases.mjs`: `pinned-editor-a-cannot-aim-a-content-target-at-a-sibling-target-item` — `user_page_editor_a` (page scope on `page_a1`) inserts a target on `content_item_a1_sibling_page` with `content_target_destination_a2`, the pair Q0 named as free of the logical key; `denied`, `deniedBy: 'policy'`, on `content_targets`. Nothing else changes.

## 2. Measured on the scratch PostgreSQL 17.11 (fresh cluster)

| | Result |
|---|---|
| `rls-smoke` | **`843 isolation case(s) passed.`** = 842 + 1 |
| **Q0's probe P10 replayed as spelled** — the `i.id = content_targets.content_item_id` term cut from the **WITH CHECK half only** | the suite stays green, the new case included. Diagnosed by hand: `member_scope_admits_business(A, business_a1)` is true for the page editor and the mutated subquery finds two items, so the WITH CHECK half admits the row — and the insert is still refused by `content_targets_scope_narrowing`, because the statement RETURNS the row and PostgreSQL checks a returned row against the **USING** half, which still names the item. Q0's hand-simulation of P10 did not account for RETURNING. |
| the term cut from **both halves** | `FAILED — 2 of 843`: the existing read case `pinned-editor-a-cannot-see-the-content-target-of-a-sibling-target-item` ("1 row(s) were visible") **and the new case** ("1 row(s) came back. The operation was permitted"). |

So the write half now exists and is the write-side case that fails when the narrowing loses its item term; and P10 as Q0 spelled it is a narrower hole than the record says — a WITH CHECK-only cut is caught by the USING half on any case that returns the row, which every insert case in this suite does. The case's `why` records exactly that, and the F3 entry in Q0's file stands as written with this note beside it.

## 3. Not done

Q0-081 F2 (right for the wrong reason, three places), F4 (two apply-time assertions guttable with every rule green) and the rest of the 081 findings stand in their files.
