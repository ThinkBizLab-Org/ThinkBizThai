# Batches 062 and 071 — the Owner's row (b): the one narrowed non-S table per family, closed in 082's shape

Run: `/claude/a0_atlas` (Author, A0 Integration). Date: 2026-09-15, after PR #144 merged (`1a80f43`).
Branch `agent/claude/WP-0A-DB-00-closures-062-071`. The Owner, in session, answered the S8 map's
§3 question for metering and research with **row (b)** — close only the narrowed tables that are
not `S` cells in RFC-2026-022 §3 — and this is that choice, one file per family.

## 1. What the two files are

| File | Closes | Table | Left open by name, and why |
|---|---|---|---|
| `062_metering_service_path_closed.sql` | 061 | `quota_buckets` | `usage_events` is the family's `S` cell (usage ledger INSERT, CARRIED); `usage_reservations` carries no narrowing |
| `071_research_service_path_closed.sql` | 070 | `research_suggestions` | `research_runs`, `research_sources`, `research_evidence` are `S` cells (run/source/evidence INSERT, CARRIED); their closures come with RFC-2026-023 §3.3's amendment in the same file |

082's exact shape, 101's premise claim (061 grants the worker SELECT/INSERT/UPDATE on
`quota_buckets`, 070 grants it SELECT/INSERT on `research_suggestions`, column-scoped, no policy).
Generated by the same tooling as 022/031/042 with the dead `service_roles` constant gone at source
(C0-pre-080 L3). One catalog case per table; ids avoid `quota-bucket` and `research-suggestion`
(CI control patterns). `SERVICE_PATH_CLOSURES` has two more rows.

## 2. Measured, scratch PostgreSQL 17.11 (fresh cluster each run)

| Run | Result |
|---|---|
| full set with the two files | `applied 062_…`, `applied 071_…`; `schema-lint: ok`; **`856 isolation case(s) passed.`** = 854 + 2 |
| the same set **without** them (`migrate-clean` then `rls-smoke`; `schema-lint` refuses this configuration with 3 problems because the snapshot declares both files — C0-062-071 L4) | **`FAILED — 2 of 856`**, exactly the two new cases; the seven existing service cases on the two tables (`service-sees-zero-quota-buckets`, `service-cannot-create-a-quota-bucket`, … `service-cannot-propose-a-research-suggestion`) do not move in either direction |
| static suites | identity 285/285; contract 60/60 |

**The S8 exploit, both tables** — fixtures loaded, `create policy probe_worker_reads … for select to app_worker using (true)`:

| | with the closure | closure dropped |
|---|---|---|
| `quota_buckets` (4 rows as `postgres`) | worker reads **0** | worker reads **4** |
| `research_suggestions` (2 rows) | worker reads **0** | worker reads **2** |

## 3. Recorded because it went wrong first

The first apply of 062 failed with `invalid escape string (22025)`: the generator reads 082's
generator as *text*, so the `like '%\_service\_path\_closed' escape '\'` line came out
double-escaped — the same slip 022/031/042 had, fixed there by hand and here fixed in the generator.
The first probe on `quota_buckets` ran against that half-applied cluster and read 0/0; it was
replayed on a clean apply (0/4) and only the replay is in §2.

## 3a. Role runs on this branch (2026-09-15)

| Run | Verdict | Applied here |
|---|---|---|
| C0 Reviewer `c0-review-batch-062-071-2026-09-15.md` | no stop-the-line; 0 HIGH, 2 MEDIUM, 5 LOW; 854/856/2-of-856, read and write probes both tables, apply-time block fired on a mutated count, 6 of 7 static mutations noticed | **M1** the Owner's words are now transcribed verbatim (`product-owner-disposition-2026-09-15-pr-144-and-row-b.md`); **M2** the blocker entry re-headed CLOSED … [ORIGINAL ENTRY FOLLOWS] with the S-cell remainder first; **L1** `research_snapshots` named in 071's left-open list; **L4** citations corrected (map §2 rows 4/5; 061/070's own statement of the shape; the negative control's `schema-lint` caveat, this row); L2, L3, L5 stand (inherited) |
| A1 Security `a1-security-batch-062-071-2026-09-15.md` | no stop-the-line; 0 HIGH, 0 MEDIUM, 2 LOW, 4 INFO; both tables confirmed non-S in RFC-2026-022 §3, §8.2/§8.4 and the policy map; closures refuse worker and app_command by name, dropped they read/write across both tenants; residual left open measured (usage_events 2, research_runs 5, research_snapshots 2) | **F1** = C0 L1 (`research_snapshots` named); **F2** both headers now say what row (b) means for `app_worker`'s own verbs — no §8 cell, no RFC arm, so the closure is the end of that path until a further RFC, not a wait for shape B; F3 = C0 L4; F4–F6 stand |
| Q0 Tester `q0-test-batch-062-071-2026-09-15.md` | no stop-the-line; 0 HIGH, 0 MEDIUM, 4 LOW, 3 INFO; 856 / 2-of-856; exploit 0 vs 4 and 0 vs 2 on read, `UPDATE 4` across 2 tenants and `INSERT 0 1` on write with a closure dropped; CI 34923143406 negative-control lines identical to main's; static reversal 22 run / 17 noticed / 5 missed | **F1** (the closed table is each family's designed service-write target) is what A1 F2's paragraph in both headers now states; **F6** the handoff cites the CI run; F2/F3/F4 inherited and stand; F5, F7 stand |

## 4. What remains of the pre-080 question

Nothing that A0 can take: the `S`-cell tables of metering and research are owed to the batch that
lands RFC-2026-023's amendment beside their closures, once that RFC is disposed. The pre-080
blocker entry says so; the map's §3 carries the disposition.
