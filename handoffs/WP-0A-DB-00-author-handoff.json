{
  "schema_version": "1.0.0",
  "protocol_version": "1.0.0",
  "work_package_id": "WP-0A-DB-00",
  "agent_run_id": "/claude/a0_atlas",
  "final_status": "author_complete",
  "base_revision": "101438a214fc0821a9ee83dbfc344e534141a948",
  "head_revision_or_patch_checksum": "2cd145042d4a5da003ea04f5e2836f4c2a0b5129",
  "task_ids": [],
  "files_added": [
    "db/foundation/migrations/080_content.sql",
    "evidence/WP-0A-DB-00/a3-batch-080-probes-2026-09-12.md",
    "tests/db/identity/fixtures/080-content-fixture.sql"
  ],
  "files_modified": [
    ".github/workflows/ci.yml",
    "db/foundation/README.md",
    "db/foundation/lint/catalog-snapshot.json",
    "db/foundation/seeds/fixture-catalog.json",
    "handoffs/WP-0A-DB-00-author-handoff.json",
    "scripts/test-suite-contract.mjs",
    "test-kits/branch-identity.test.mjs",
    "test-kits/db/foundation-contract.test.mjs",
    "test-kits/integrity-manifest.json",
    "tests/db/identity/identity-isolation.test.mjs",
    "tests/db/identity/isolation-cases.mjs",
    "tests/db/identity/run-isolation.mjs",
    "work-packages/WP-0A-DB-00.json"
  ],
  "files_deleted": [],
  "decisions_consumed": [],
  "contracts_consumed": [],
  "contracts_produced_or_changed": [],
  "assumptions": [
    "BATCH 080 LANDS THE `content.core` FAMILY: five tables, ten policies, and three of the five immutable in the strongest form this schema has. §6's registry gives the batch five words -- ideas/items/versions/variants/quality -- and §5's inventory names six objects; the sixth, content_targets, belongs to batch 081 by the same registry, and batch 021's rule (creating a table no registry row gives you is reserving somebody else's work) is followed rather than re-argued.",
    "THE MIGRATION WAS ALREADY DRAFTED ON `main` AND WAS NOT FINISHED, WHICH IS WHY THIS INCREMENT IS LARGER THAN ITS SQL DIFF. The working tree carried 080_content.sql and a catalog-snapshot line and nothing else: no fixture, no isolation case, no static rule, no CI control entry, no evidence, and a failing test (`the digest gap between the tree and the instance is exactly what the snapshot declares`), because the snapshot declared 080 not-applied and NOT_ON_THE_INSTANCE did not. TWO CLAIMS IN THE DRAFTED HEADER WERE FALSE WHEN THIS INCREMENT PICKED IT UP -- \"the migration asserts the count\" and \"the second half asserted below\" -- and the file contained no apply-time assertion at all. Both are now true: the batch has a `do $$` block, and the variant key is declared NULLS NOT DISTINCT rather than described as though it were.",
    "app_worker IS GRANTED NOTHING, WHICH IS THE ONE PLACE THIS BATCH DEPARTS FROM 070's SHAPE AND IS DECLARED RATHER THAN QUIET. Batch 070 grants its worker select/insert/update and lets row level security refuse it, which is batch 010's \"grants and no policy\". Content does not: §8.2 row 3 forbids updating a version at all, the act that CREATES one is a SECURITY DEFINER function owned by app_command (RFC-2026-017 §3), and granting app_worker a write would build a second path to the same act -- the shape RFC-2026-018 was superseded for proposing. The consequence is that every service case here is a PRIVILEGE refusal, none carries RFC-2026-017 §7, and none is part of the CI negative control's basis.",
    "SMOKE_COVERAGE[3] MOVES FROM `knowledge-half` TO `true`, AND IT IS THE ONLY VALUE IN THAT MAP THAT HAS EVER MOVED. §12.6/3 is \"user_approver_a cannot edit content/knowledge\"; batch 040 paid knowledge and five batches in a row recorded that they were not content. This batch creates the two tables in the family a client may write and refuses the approver on both, with the READ beside them so the refusals are about the role rather than about visibility. Eight assertions in earlier batches pinned the old value and are rewritten in the same change, each saying what its own batch did and did not pay; none is deleted."
  ],
  "acceptance_results": [
    {
      "criterion": "Batch 080 creates the five tables §6's registry names, and no table it does not",
      "result": "met",
      "evidence": "db/foundation/migrations/080_content.sql creates app.content_ideas, app.content_items, app.content_versions, app.content_variants and app.quality_reviews. `every batch 080 table carries RLS, FORCE, a primary key and an owner comment` asserts the set against tablesCreatedByMigrations() and asserts that app.content_targets and app.generation_runs are NOT created."
    },
    {
      "criterion": "§8.2 row 3 is implemented as absent grants AND absent policies, asserted both ways",
      "result": "met",
      "evidence": "No INSERT/UPDATE/DELETE grant and no INSERT/UPDATE/DELETE policy exists on the three immutable tables; `the three immutable tables hold no write grant and no write policy, asserted both ways` holds the migration text, and the apply-time block holds the LIVE ACLs and the LIVE policy catalog. Nine isolation cases assert the refusal from the owner, which is what makes them about the operation rather than the caller."
    },
    {
      "criterion": "The half-implemented `Y` cell is implemented in the half it has and named in the half it does not",
      "result": "met",
      "evidence": "`status`, `current_version_id` and `approval_state` are outside every UPDATE grant; `title`, `updated_at`, `updated_by` and `deleted_at` are in it. `owner-a-cannot-approve-a-content-item` is a grant-layer refusal and `editor-a-can-rename-the-content-item-of-a1` passes. The missing half is in open_blockers and cites the Product Owner's 2026-09-10 disposition of the same shape on batch 070 rather than re-asking it."
    },
    {
      "criterion": "The member scope is falsifiable at every depth the family has",
      "result": "met",
      "evidence": "The item asks both questions; the version resolves through the item; the variant and the quality review resolve through the version and then the item. The fixture loads a version, a variant and a quality review under the SIBLING-PAGE item so the second link has a negative: `pinned-editor-a-cannot-see-the-content-variant-of-a-sibling-target-item` and its quality-review twin fail if that link is cut, and a reversal probe confirmed the first link's rule bites."
    },
    {
      "criterion": "Every decision this batch could not make is recorded rather than made",
      "result": "met",
      "evidence": "Six new open_blockers: three unenumerated columns left without a CHECK, the `id`-only foreign key into app.research_suggestions, the three things the findings CHECK cannot hold, the absence of any writer for the three immutable tables, the half-implemented `Y` cell, and app.generation_runs still assigned to nobody."
    },
    {
      "criterion": "The batch is falsifiable, and the probe round says where it is not",
      "result": "met",
      "evidence": "evidence/WP-0A-DB-00/a3-batch-080-probes-2026-09-12.md: seventeen reversal probes, fifteen noticed on the first run and two not -- an inline foreign key on generation_run_id that the rule's regex could not see, and an apply-time assertion narrowed to one of its three tables. Both rules were changed. Marked `partially met` while no case had met a database; CI run 34678457021 on head 2941e28 then applied the migration (db-migrate-clean ok in 1345ms) and ran the suite (629 isolation cases passed), and the five new negative-control entries each made it fail as required. What the probe round could not reach is unchanged and is recorded in the evidence file: a static rule reads the SHAPE of an apply-time assertion, and a block that checks nothing does not raise either."
    }
  ],
  "tests": [
    {
      "command": "npm run check",
      "exit_code": 0,
      "result": "see evidence/VERIFICATION.md; 232 tests in tests/db/identity/identity-isolation.test.mjs, up from 218"
    },
    {
      "command": "node --test tests/db/identity/identity-isolation.test.mjs",
      "exit_code": 0,
      "result": "232 pass, 0 fail"
    },
    {
      "command": "node --test test-kits/db/foundation-contract.test.mjs",
      "exit_code": 0,
      "result": "56 pass, 0 fail"
    },
    {
      "command": "node scripts/verify-test-coverage-floor.mjs",
      "exit_code": 0,
      "result": "floors raised to 232 declared tests and 1686 assertions; no floor lowered"
    },
    {
      "command": "reversal probes (a throwaway script in a scratch directory, not a repository command)",
      "exit_code": 0,
      "result": "seventeen probes, fifteen noticed on the first run; the two misses and the rule each one changed are recorded in evidence/WP-0A-DB-00/a3-batch-080-probes-2026-09-12.md. The script is deliberately not committed: it edits tracked files in place, and a tool that does that living in the repository is a footgun with a README."
    },
    {
      "command": "CI run 34678457021 on head 2941e28 — LC_ALL=C TZ=UTC make db-migrate-clean, then make db-rls-smoke, then the per-family negative control",
      "exit_code": 0,
      "result": "db-migrate-clean ok in 1345ms (080_content.sql applied to postgres:17, so the file parses and the apply-time block did not raise); db-rls-smoke: 629 isolation case(s) passed; all five new control entries bit, noticing 7, 12, 4, 2 and 3 cases respectively"
    }
  ],
  "security_privacy_cost_impact": "SYNTHETIC ONLY, and the fixture carries no captured material: every string is this repository's own, every id is uuid5 of its symbol, and the two Thai messages are invented for the fixture. THREE THINGS ARE WORTH NAMING AS SECURITY CONSEQUENCES RATHER THAN LEFT IN THE BLOCKERS. (1) app.content_ideas references app.research_suggestions on `id` alone, so a caller holding another tenant's suggestion id could write a dangling cross-tenant CITATION -- reading the suggestion is still refused by row level security, so this is a provenance weakness and not a disclosure. (2) generation_run_id may name a run that never existed, which the Product Owner's disposition records as the cost of option ก. (3) The quality-review findings CHECK accepts a whitespace-only message_th, so a rule set could record a finding a person cannot read. No secret, credential, token or customer content is stored, logged, fixtured or handed off.",
  "migration_and_data_impact": "One new migration, db/foundation/migrations/080_content.sql, NOT applied to any database: no instance holds it, and db/foundation/lint/catalog-snapshot.json declares it not-applied with the reason (its tables reference app.business_profiles and app.page_context_profiles over composite keys batch 020 creates, its policies call four helpers 011 and 021 create, and app.content_ideas references app.research_suggestions from 070). It sorts between 070 and 110 and is INSERTED into the middle of the declaration rather than appended, because pendingDeclarationLint requires a TAIL. No existing migration is altered.",
  "compatibility_impact": "None to anything that runs. The one change visible to another package is SMOKE_COVERAGE[3] moving to `true`, which eight assertions in earlier batches pinned and which are updated in the same change.",
  "known_limitations": [
    "EVERY ISOLATION CASE HAS NOW MET A DATABASE, AND THIS ENTRY IS CORRECTED RATHER THAN DELETED. It read \"not one isolation case has met a database\", which was true of this host -- no psql, no pg_ctl, no docker, and RFC-2026-001 forbids adding a dependency -- and false the moment CI ran. CI run 34678457021 on head 2941e28: 629 isolation cases passed against a postgres:17 container and the five new negative-control entries each made the suite fail as required. What remains true is the reason the entry existed: nothing on the author's machine can verify a policy, so every claim in this package about BEHAVIOUR rests on a CI run somebody can open rather than on a local pass.",
    "THE SQL HAS NOW BEEN PARSED BY POSTGRES, and the same correction applies. `unique nulls not distinct`, the jsonpath CHECK and the `do $$` block were first parsed by CI run 34678457021, where `db-migrate-clean` reported ok in 1345ms.",
    "AN APPLY-TIME ASSERTION CAN BE GUTTED WITHOUT THE STATIC SUITE NOTICING, which a probe demonstrated and a count now partly covers. A rule that reads the SHAPE of a plpgsql block cannot read its meaning, and CI applying the migration proves only that the block does not RAISE -- which a block that checks nothing also does not. Review twelve's finding, one level further in.",
    "THE WITH CHECK HALVES OF THE THREE CHILD NARROWINGS ARE INERT TODAY. They bound writes, and no role may write those tables; they are written for the day a grant appears, and nothing in this suite exercises them.",
    "THE THREE UNENUMERATED COLUMNS ARE FREE TEXT. `content_ideas.status`, `content_items.approval_state` and `content_variants.variant_type` accept any string, including one that contradicts a vocabulary a later batch chooses. A later CHECK on an IMMUTABLE table is a forward migration with a data question attached, which is why the columns exist now and their vocabularies do not."
  ],
  "open_risks_or_blockers": [
    "NO INDEPENDENT ROLE HAS SIGNED ANYTHING ON THIS PACKAGE, on this increment as on every increment in its history. Reviewer /claude/c0_contract_reviewer, Tester /claude/q0_sentinel, Security /claude/a1_bastion and Integration Owner /claude/r0_steward evidence is absent. This handoff is the AUTHOR's and is none of those four signatures.",
    "THE THREE IMMUTABLE TABLES HAVE NO WRITER AND CANNOT GET ONE HERE. §8.2 row 3 is `N` for every role including the service; the writer is a SECURITY DEFINER command function owned by app_command, and RFC-2026-021 §10 records that none exists. Until it does, nothing in this repository can record a content version, a variant or a quality verdict.",
    "app.generation_runs REMAINS UNASSIGNED IN §6's REGISTRY, and this batch now holds two columns that reference it with nothing enforcing them.",
    "app.research_suggestions EXPOSES NO COMPOSITE SCOPE KEY, so the idea's citation is enforced on `id` alone. The fix is a unique constraint owed to batch 070's owner, and the forward migration that tightens the key is owed to A3 Content after it lands.",
    "THREE COLUMNS §4.6 NAMES CARRY NO VOCABULARY, and `content_items.approval_state` is the one a reader is most likely to fill in from §4.7's approval_requests.status -- a different column on a table batch 090 owns."
  ],
  "rollback_or_forward_fix": "ROLLBACK: revert this commit through a reviewed revert PR. The migration has never been applied to any database, so there is no deployed schema to roll back; the fixture, the cases and the rules go with it, the catalog-snapshot declaration returns to a tail ending at 070, and SMOKE_COVERAGE[3] returns to `knowledge-half` along with the eight assertions that pin it. FORWARD FIX is the route for anything found after a merge: migration invariant 1 forbids rewriting an integrated migration, and every control this batch adds is a column list, a policy predicate or an absent grant that a later batch can tighten -- except on the three immutable tables, where a column added later is the thing this batch says is worse than a column with no CHECK.",
  "recommended_next_work_packages": [
    "Batch 081 (content_targets), which §6's registry assigns to A3 Content and which this batch deliberately did not create. It inherits the deferred Social FK and will meet the same generation-run gap.",
    "The unique constraint on app.research_suggestions that would let app.content_ideas reference it over the scope path -- batch 070's owner's to make, and small.",
    "An owner for app.generation_runs in §6's registry, which needs A0 and probably an RFC. Two batches now carry columns that reference it.",
    "The independent role runs, on this and on every other open increment in this package."
  ],
  "reviewer_instructions": [
    "START WITH THE TWO FALSE CLAIMS THIS INCREMENT INHERITED. The drafted header said the migration asserts the variant count and asserts immutability 'below', and it asserted nothing: `git show` the first commit of this branch and check that the apply-time block and the NULLS NOT DISTINCT declaration are what made those sentences true, rather than the sentences having been softened.",
    "READ THE PROBE EVIDENCE BEFORE THE RULES IT DESCRIBES. Two probes were not noticed; both changed a rule. The second one (an apply-time assertion narrowed to one of its three tables) is covered by a COUNT, which is weaker than the defect deserves and is labelled as such.",
    "PRESS ON app_worker HOLDING NOTHING. It is this batch's one departure from 070's shape, it is argued from RFC-2026-017 §3 and RFC-2026-018's supersession, and if it is wrong the consequence is that the service cases here assert something weaker than they appear to.",
    "CHECK THE COVERAGE FLIP AGAINST ITS OWN CASES. SMOKE_COVERAGE[3] moves to `true` on three refusals and one read; if the read (`approver-a-sees-the-content-item-of-a1`) does not do what its `why` says, the flip is a claim about visibility rather than about a role.",
    "NOTHING HERE IS A TEST RESULT ABOUT A DATABASE. Every isolation case is static until CI runs it."
  ],
  "superseded_by": null
}
