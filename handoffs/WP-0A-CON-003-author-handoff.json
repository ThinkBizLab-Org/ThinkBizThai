{
  "schema_version": "1.0.0",
  "protocol_version": "1.0.0",
  "work_package_id": "WP-0A-CON-003",
  "agent_run_id": "/claude/a0_atlas",
  "final_status": "author_complete",
  "base_revision": "463cf974d93e1bbd93a20e354d21ecc47b0008d7",
  "head_revision_or_patch_checksum": "417b8a905c58710d13e529a4b93691c94e15065d",
  "task_ids": [],
  "files_added": [],
  "files_modified": [
    "handoffs/WP-0A-CON-003-author-handoff.json",
    "test-kits/branch-identity.test.mjs",
    "test-kits/integrity-manifest.json",
    "work-packages/WP-0A-CON-003.json"
  ],
  "files_deleted": [],
  "decisions_consumed": [
    "DEC-021",
    "DEC-022",
    "DEC-023",
    "DEC-025"
  ],
  "contracts_consumed": [
    "CTR-TEN-001",
    "CTR-ERR-001"
  ],
  "contracts_produced_or_changed": [
    "None. This increment produces and changes no contract. CTR-MOD-001 and CTR-FLG-001 stand exactly as this package materialized them and were since promoted to Candidate under RFC-2026-010; no schema, fixture, manifest or index file under contract-catalog/ is touched here."
  ],
  "assumptions": [
    "Gate G0 remains Specification Baseline Complete / External Verification Pending; every change here is reversible and synthetic-only.",
    "RFC-2026-002 reserves the merge for the Product Owner. This package was never merged and main was never touched.",
    "Cross-vendor review is NOT satisfied. Every run against this package is Anthropic claude-opus-5, carried as the recorded exception rather than as an equivalent.",
    "The revisions above are the branch's CURRENT base and head. This stack has been rebased repeatedly; an earlier version of this handoff cited SHAs that no longer exist in any branch, which independent review caught. Re-derive them if the stack is rebased again."
  ],
  "acceptance_results": [
    {
      "criterion": "Every fixture is validated against its own shipped schema.json by the subset validator, and every protected constraint is killed by at least one declared fixture — a constraint no fixture can kill is documentation.",
      "result": "pass",
      "evidence": "evidence/WP-0A-CON-003/author-self-check.md"
    },
    {
      "criterion": "Every rule in both schemas carries an x-source naming the baseline task it comes from; no rule is present without one.",
      "result": "pass",
      "evidence": "evidence/WP-0A-CON-003/author-self-check.md"
    },
    {
      "criterion": "MR-004 deny-by-default is expressed in the contract: a module cannot be `ready` unless activation readiness explicitly succeeded, and a `blocked` module must record what was missing.",
      "result": "pass",
      "evidence": "evidence/WP-0A-CON-003/author-self-check.md"
    },
    {
      "criterion": "FP-002 is expressed in the contract: a kill switch is a platform-scope deny, and disabling write preserves historical read.",
      "result": "pass",
      "evidence": "evidence/WP-0A-CON-003/author-self-check.md"
    },
    {
      "criterion": "A secret handle is an opaque reference; no literal credential appears in any fixture.",
      "result": "pass",
      "evidence": "evidence/WP-0A-CON-003/author-self-check.md"
    },
    {
      "criterion": "Both manifests declare in untestable_by_fixture the properties a single document cannot demonstrate, rather than reading as if they were materialized.",
      "result": "pass",
      "evidence": "evidence/WP-0A-CON-003/author-self-check.md"
    },
    {
      "criterion": "The index still reports 4 Candidate and 10 Draft: materializing a Draft contract does not promote it.",
      "result": "pass",
      "evidence": "evidence/WP-0A-CON-003/author-self-check.md"
    },
    {
      "criterion": "npm run check passes on pinned Node 24.20.0 / npm 11.19.0 with skipped and todo both zero.",
      "result": "pass",
      "evidence": "evidence/WP-0A-CON-003/author-self-check.md"
    },
    {
      "criterion": "Every rule states whether it is sourced verbatim from a named task or is a declared inference, and every citation names the correct document section.",
      "result": "pass",
      "evidence": "evidence/WP-0A-CON-003/author-self-check.md"
    },
    {
      "criterion": "Each obligation has its own counterexample: a fixture that violates two rules at once tests neither independently.",
      "result": "pass",
      "evidence": "evidence/WP-0A-CON-003/author-self-check.md"
    }
  ],
  "tests": [
    {
      "command": "npm run check",
      "exit_code": 0,
      "result": "see evidence/VERIFICATION.md, which is machine-written and compared byte-for-byte against a live run",
      "toolchain": "node v24.20.0 / npm 11.19.0"
    }
  ],
  "security_privacy_cost_impact": "data classification synthetic-only; secrets required false; network policy deny-unless-declared. No provider call, credential, migration or customer-data effect. Cost impact none: no metered resource is touched.",
  "migration_and_data_impact": "None. No migration, production schema, provider call, credential, fixture or customer-data effect. The increment edits one work-package manifest, this handoff, one row of the branch-to-package mapping and the digest that covers it. Gate G0 forbids every one of the effects listed above and this increment has none of them.",
  "compatibility_impact": "None. This increment changes no contract-catalog file: no schema, no example fixture, no contract manifest and not the index, so there are no contract changes and no consumer surface moves. What changed is a record ABOUT contracts -- four of the ten open_blockers on this package described a state the tree no longer has -- together with this package branch declaration and the mapping row that pins it.",
  "known_limitations": [
    "RFC-2026-004 was recorded here as Proposed and awaiting disposition before the RFC-2026-002 manual merge. That is no longer the state of the tree: its Status line in architecture/decisions/ now reads Approved 2026-09-02, the $ref integrity guard is live, and no freeze level moved. What remains is narrower and unchanged in kind: RFC-2026-004 records that its own Limitations and Rollback stand unchanged despite the disposition, so this package still inherits them; and RFC-2026-002, itself only provisional manual control rather than native branch protection, still reserves every merge into main for the Product Owner, so nothing dispositioned here lets this package merge itself.",
    "CTR-MOD-001 (owner A0) declares secret-handle syntax chartered to CTR-SEC-001 (owner A0+A1) with no RFC. Independent security review graded this High. A1 is a required authority and is not in this session; the conflict must be resolved by the two owners, not by this package.",
    "This said secret_handles and the repository secret scanner \"compose into zero coverage\" -- independent security review had probed four handle-shaped payloads and found the only one the scanner catches is the one the schema already rejects. A1 re-measured it as disposition condition C4 (commit e7b8c4b) against 15 credential bodies the pattern admits, and scripts/scan-repository-secrets.mjs detected 12. The zero-coverage claim was false in the alarming direction, which is as wrong as understating a surface. What remains is narrower and differently shaped, and is now carried verbatim in CTR-SEC-001 x-opacity-limitation: the residual is 5 of 5 lowercase-canonical forms -- bare 32-hex, a UUID, an HMAC hex digest, a passphrase and a dotted-label password -- and the real limitation is one of REACH rather than of coverage, because the scanner walks repository files at commit time and has none over a handle in a log, an event or a job, which is the surface PT-010 is actually about. The pattern is still NOT a control and must not be cited as one; A1 must specify a handle ISSUANCE format before freeze.",
    "This said not one of the nine catalog contracts had a fixture that killed its root required list, that CON-003 enforced it for the two contracts it owns, and that the other seven were escalated to their packages. The escalation was taken up and closed catalog-wide by WP-0A-CON-008 (merged as 963990f, PR #44). It remeasured across all fourteen contracts -- 8 of 101 root required entries isolated by a negative fixture, eight contracts isolating none -- and, rather than land 101 fixtures that can rot the same way, asserts the property generatively: test-kits/contracts/catalog-registry.test.mjs drops each declared root required key from a valid example and requires the schema to reject it by name, with a floor of 101 keys exercised so a key cannot leave a required list quietly. What remains is narrower: that guard reads schema.required at the ROOT and nowhere else. A required list nested inside a subschema is outside it, and is defended only where test-kits/contracts/schema-mutation-coverage.test.mjs names one by hand -- ctr-ntf-001 deep_link.required is the single nested entry -- so nested requiredness across the catalog is still unmeasured.",
    "The ENFORCEMENT half of FP-002 — that a narrower scope cannot override a live platform kill switch — cannot be expressed in a single decision document and is not materialized. It needs an evaluator that does not exist. The ORDER half is now enforced.",
    "MR-002 duplicate-capability resolution and MR-003 circular-dependency blocking are registry properties across multiple manifests, and FP-003 stable allocation is an evaluator property over repeated inputs. Declared in untestable_by_fixture and NOT claimed as materialized.",
    "The dependency range grammar (MR-003), the drain and shutdown protocol timing (MR-005), the bucket allocation algorithm (FP-003), the circuit-state machine (FP-004) and the admin APIs (MR-006, FP-006) are deliberately not inferred and remain open before freeze.",
    "This said the package depends on WP-0A-CON-002 and WP-0A-A0-002, both unmerged. Both reached main long ago -- A0-002 at 41e47bf (PR #3) and CON-002 at 1eb31c2 (PR #4) -- as did the other two declared dependencies, WP-0A-A0-001 and WP-0A-CON-001, and this package now sits on top of all four. What remains is the inheritance, which merging a branch does not discharge: both manifests still read status in_review and still record open blockers -- 13 on CON-002 and 8 on A0-002 -- and this package still inherits every one of them, including that the test-integrity guard is a tripwire with no self-anchor (A0-002) and that CTR-PAG-001 accepts a forgeable cursor with no page-size bound (CON-002).",
    "prefer_cross_vendor_review is not satisfied; see independence.cross_vendor_exception. Recorded as an exception, not waived.",
    "Gate G0 remains Specification Baseline Complete / External Verification Pending; this package is restricted to reversible, synthetic-only work."
  ],
  "open_risks_or_blockers": [
    "RFC-2026-004 was recorded here as Proposed and awaiting disposition before the RFC-2026-002 manual merge. That is no longer the state of the tree: its Status line in architecture/decisions/ now reads Approved 2026-09-02, the $ref integrity guard is live, and no freeze level moved. What remains is narrower and unchanged in kind: RFC-2026-004 records that its own Limitations and Rollback stand unchanged despite the disposition, so this package still inherits them; and RFC-2026-002, itself only provisional manual control rather than native branch protection, still reserves every merge into main for the Product Owner, so nothing dispositioned here lets this package merge itself.",
    "CTR-MOD-001 (owner A0) declares secret-handle syntax chartered to CTR-SEC-001 (owner A0+A1) with no RFC. Independent security review graded this High. A1 is a required authority and is not in this session; the conflict must be resolved by the two owners, not by this package.",
    "This said secret_handles and the repository secret scanner \"compose into zero coverage\" -- independent security review had probed four handle-shaped payloads and found the only one the scanner catches is the one the schema already rejects. A1 re-measured it as disposition condition C4 (commit e7b8c4b) against 15 credential bodies the pattern admits, and scripts/scan-repository-secrets.mjs detected 12. The zero-coverage claim was false in the alarming direction, which is as wrong as understating a surface. What remains is narrower and differently shaped, and is now carried verbatim in CTR-SEC-001 x-opacity-limitation: the residual is 5 of 5 lowercase-canonical forms -- bare 32-hex, a UUID, an HMAC hex digest, a passphrase and a dotted-label password -- and the real limitation is one of REACH rather than of coverage, because the scanner walks repository files at commit time and has none over a handle in a log, an event or a job, which is the surface PT-010 is actually about. The pattern is still NOT a control and must not be cited as one; A1 must specify a handle ISSUANCE format before freeze.",
    "This said not one of the nine catalog contracts had a fixture that killed its root required list, that CON-003 enforced it for the two contracts it owns, and that the other seven were escalated to their packages. The escalation was taken up and closed catalog-wide by WP-0A-CON-008 (merged as 963990f, PR #44). It remeasured across all fourteen contracts -- 8 of 101 root required entries isolated by a negative fixture, eight contracts isolating none -- and, rather than land 101 fixtures that can rot the same way, asserts the property generatively: test-kits/contracts/catalog-registry.test.mjs drops each declared root required key from a valid example and requires the schema to reject it by name, with a floor of 101 keys exercised so a key cannot leave a required list quietly. What remains is narrower: that guard reads schema.required at the ROOT and nowhere else. A required list nested inside a subschema is outside it, and is defended only where test-kits/contracts/schema-mutation-coverage.test.mjs names one by hand -- ctr-ntf-001 deep_link.required is the single nested entry -- so nested requiredness across the catalog is still unmeasured.",
    "The ENFORCEMENT half of FP-002 — that a narrower scope cannot override a live platform kill switch — cannot be expressed in a single decision document and is not materialized. It needs an evaluator that does not exist. The ORDER half is now enforced.",
    "MR-002 duplicate-capability resolution and MR-003 circular-dependency blocking are registry properties across multiple manifests, and FP-003 stable allocation is an evaluator property over repeated inputs. Declared in untestable_by_fixture and NOT claimed as materialized.",
    "The dependency range grammar (MR-003), the drain and shutdown protocol timing (MR-005), the bucket allocation algorithm (FP-003), the circuit-state machine (FP-004) and the admin APIs (MR-006, FP-006) are deliberately not inferred and remain open before freeze.",
    "This said the package depends on WP-0A-CON-002 and WP-0A-A0-002, both unmerged. Both reached main long ago -- A0-002 at 41e47bf (PR #3) and CON-002 at 1eb31c2 (PR #4) -- as did the other two declared dependencies, WP-0A-A0-001 and WP-0A-CON-001, and this package now sits on top of all four. What remains is the inheritance, which merging a branch does not discharge: both manifests still read status in_review and still record open blockers -- 13 on CON-002 and 8 on A0-002 -- and this package still inherits every one of them, including that the test-integrity guard is a tripwire with no self-anchor (A0-002) and that CTR-PAG-001 accepts a forgeable cursor with no page-size bound (CON-002).",
    "prefer_cross_vendor_review is not satisfied; see independence.cross_vendor_exception. Recorded as an exception, not waived.",
    "Gate G0 remains Specification Baseline Complete / External Verification Pending; this package is restricted to reversible, synthetic-only work."
  ],
  "rollback_or_forward_fix": "Revert through a reviewed revert PR. This increment covers four paths -- work-packages/WP-0A-CON-003.json, handoffs/WP-0A-CON-003-author-handoff.json, test-kits/branch-identity.test.mjs and test-kits/integrity-manifest.json -- so a revert restores the four previous blocker texts, the previous ownership.branch and the previous mapping row, and nothing else. No contract, schema, fixture, migration or persisted state is touched, so there is no data, provider or credential effect either way. A forward fix is cheaper than a revert here: a rewritten blocker that turns out to be wrong is corrected by rewriting it again, because the text IS the artifact.",
  "recommended_next_work_packages": [
    "WP-0A-CON-005"
  ],
  "reviewer_instructions": [
    "This increment rewrites four of the ten open_blockers on WP-0A-CON-003, moves ownership.branch, and changes nothing else about the package. Verify each rewrite the way it was produced: against the tree, not against this document.",
    "Blocker 1: read the Status line of architecture/decisions/RFC-2026-004-catalog-reference-integrity.md. Blocker 3: read commit e7b8c4b and the x-opacity-limitation annotation in contract-catalog/shared-kernel/ctr-sec-001/schema.json, which carry the measurement that replaced the zero-coverage claim. Blocker 4: read commit 963990f and the generated root-required check at the end of test-kits/contracts/catalog-registry.test.mjs. Blocker 8: run git log main --oneline --grep=WP-0A-CON-002 and the same for WP-0A-A0-002, then read those two manifests for the blockers this package still inherits from them.",
    "Six blockers were checked and deliberately left verbatim, and the check matters as much as the rewrites. Blocker 2 stands because the handle-syntax ownership conflict is still open with no RFC and is still recorded as open in CTR-SEC-001 own schema annotation. Blockers 5 and 6 stand because the FP-002 enforcement half, MR-002, MR-003 and FP-003 are still declared in untestable_by_fixture on both manifests and no evaluator or registry harness exists anywhere in the tree. Blocker 7 stands because none of its five deliberately-uninferred items appears in either schema: the dependency range is still an unconstrained non-empty string and bucket.allocated is still a bare boolean. Blockers 9 and 10 stand unchanged.",
    "Blocker 2 is left exactly as written, including its clause that A1 is not in this session. That clause is a statement about run staffing, not about the repository, so it is not something this branch can verify or falsify from the tree; what IS in the tree is the conflict itself, unresolved and un-RFC-ed, which is why the blocker stands as it is rather than being softened.",
    "The author is /claude/a0_atlas. Independent contract, security, test and integration verification remain open and must be distinct runs; cross-vendor review is not satisfied and is carried as a recorded exception."
  ],
  "superseded_by": null
}
