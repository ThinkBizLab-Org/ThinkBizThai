{
  "schema_version": "1.0.0",
  "protocol_version": "1.0.0",
  "work_package_id": "WP-0A-DB-00",
  "title": "DB-00 schema foundation and test harness",
  "gate": "G0",
  "priority": "P0",
  "status": "in_progress",
  "required_skill_profiles": {
    "author": [
      "architecture-contracts"
    ],
    "reviewer": [
      "architecture-contracts"
    ],
    "tester": [
      "independent-qa"
    ],
    "integration_owner": [
      "integration-release"
    ],
    "conditional_reviewers": [
      "security-privacy"
    ]
  },
  "role_assignments": {
    "author_agent_run_id": "/claude/a0_atlas",
    "reviewer_agent_run_id": "/claude/c0_contract_reviewer",
    "tester_agent_run_id": "/claude/q0_sentinel",
    "integration_owner_agent_run_id": "/claude/r0_steward",
    "security_reviewer_agent_run_id": "/claude/a1_bastion",
    "product_reviewer_agent_run_id": null,
    "_run_id_disambiguation": "/claude/r0_steward (Anthropic) is THIS package's Integration Owner and must record its own capability declaration before this package leaves backlog. /root/r0_steward (OpenAI Codex) is a DIFFERENT run: it is the Integration Owner of WP-0A-A0-001 and WP-0A-CON-001 and is named in open_blockers solely because it must countersign the amendments made to those two packages and record the WP-0A-CON-001 npm run check addendum. Neither run substitutes for the other. Independent testing flagged the near-identical names as ambiguous; both are intended."
  },
  "independence": {
    "no_self_approval": true,
    "require_distinct_author_reviewer": true,
    "require_distinct_author_tester": true,
    "prefer_cross_vendor_review": true,
    "cross_vendor_exception": "prefer_cross_vendor_review is NOT satisfied: every assigned run is Anthropic claude-opus-5. The Decision Register (§ cross-vendor review) permits this only as an explicit exception - 'if there is no cross-vendor capacity, use an independent agent per run and record the exception'. Each of the five roles is a separate agent run with its own capability declaration and no run reviewed, tested, or integrated its own work. The Integration Owner must carry this exception forward; it is not waived and it is not equivalent to cross-vendor review."
  },
  "accountable_owners": [
    "A0"
  ],
  "required_human_authorities": [
    "Product Owner and A0 to decide the service path (supabase-js with the service-role key, or a direct driver with per-request SET LOCAL role), due before G1."
  ],
  "purpose": "The first application code in this repository. RFC-2026-015 closed DATA-DEC-01 (`app`) and DATA-DEC-02 (a wrapper command contract, no tool named), which is exactly what the data package says gates the DB-00 merge and the start of DB-00 coding. This increment lands the batch 000 migration, the deterministic command contract, and the half of the schema lint decidable from the repository. The half needing a live Postgres is wired to REFUSE: this host has no psql, pg_ctl or docker and the repository forbids adding a dependency, so a harness that printed a pass would be the largest false clean run this repository has produced -- on the one surface where a false pass means tenant data.",
  "scope": {
    "include": [
      "Batch 000: the app and private schemas, the approved extension set, and the updated_at helper",
      "The Makefile command contract with every target the data package names, and the wrapper behind it",
      "The schema lint rules decidable from migration text, including the ENABLE-vs-FORCE rule the specified lint misses",
      "Behaviour tests: each lint rule shown to reject before it is trusted, and every live target shown to refuse without a database"
    ],
    "exclude": [
      "Any tenant table, and therefore any RLS policy. WP-0A-A0-007 carries a Proposed decision on the policy shape every later batch would inherit; migration invariant 1 forbids rewriting a merged migration, so writing the shape now would commit batches 010..180 to it.",
      "Choosing a migration runner. DATA-DEC-02 fixes the contract, not the tool.",
      "The live half of the harness: seed replay, RLS smoke and the acceptance tests. They need a provisioned test database."
    ]
  },
  "decisions_consumed": [
    "DATA-DEC-01",
    "DATA-DEC-02",
    "DATA-DEC-03"
  ],
  "contracts_consumed": [],
  "contracts_produced": [],
  "dependencies": {
    "required_work_packages": [
      "WP-0A-A0-006"
    ],
    "approved_fixtures": []
  },
  "ownership": {
    "branch": "agent/claude/WP-0A-DB-00-reservations",
    "writable_paths": [
      "Makefile",
      "db/foundation/**",
      "db/foundation/ci/**",
      "db/foundation/migrations/010_identity.sql",
      "docs/handoffs/db-00/**",
      "evidence/WP-0A-DB-00/**",
      "handoffs/WP-0A-DB-00-*.json",
      "scripts/db/**",
      "test-kits/db/**",
      "work-packages/WP-0A-DB-00.json"
    ],
    "read_only_paths": [
      "README.md",
      "docs/**",
      "CONTRIBUTING_AGENTS.md",
      "AGENTS.md",
      "CLAUDE.md",
      "contract-catalog/**",
      "package.json",
      "package-lock.json"
    ],
    "forbidden_paths": [
      "*.pem",
      "*.key",
      "*.p12",
      "*.pfx",
      ".env",
      ".env.*"
    ],
    "amends_without_owning": {
      "paths": [
        "evidence/VERIFICATION.md",
        "scripts/test-suite-contract.mjs",
        "test-kits/branch-identity.test.mjs",
        "test-kits/integrity-manifest.json",
        "tests/db/identity/fixtures/010-identity-fixture.sql",
        "tests/db/identity/identity-isolation.test.mjs",
        "tests/db/identity/isolation-cases.mjs",
        "tests/db/identity/run-isolation.mjs"
      ],
      "rationale": "The four files under tests/db/identity are A1's, carried by this package since the batch 010 increment. Three of the five reservations A1 raised are IN those files -- the fixture's public.digest() calls, the runner not asserting deniedBy, and the assertion that pinned the very call that cannot run on the platform -- so repairing them means editing them. Each edit is the repair A1's own record asks for, and its countersignature is cited at every site.\\n\\nThe branch-to-package mapping is pinned whole in a test, so renaming this package's branch moves one row there. The integrity manifest records the digests of the protected files this branch edits.\n\nTwo registries move whenever a test file changes and were missing from this declaration until CI refused the branch: scripts/test-suite-contract.mjs carries the per-file test and assertion floors and the name digests, and evidence/VERIFICATION.md is the machine-written record every commit regenerates. Adding a test raises a floor there; no floor is lowered. This is the same under-declaration this repository has caught on nearly every branch, and the reason is always the same -- the list is maintained by hand against a diff that changes with every commit."
    }
  },
  "inputs": {
    "files": [
      "docs/sprint-0a/sprint-0a-core-erd-rls-retention-th.md",
      "architecture/decisions/RFC-2026-015-db00-data-foundation-decisions.md"
    ],
    "fixtures": []
  },
  "outputs": {
    "files": [
      "db/foundation/migrations/000_foundation.sql",
      "Makefile",
      "scripts/db/run.mjs",
      "test-kits/db/foundation-contract.test.mjs",
      "work-packages/WP-0A-DB-00.json"
    ],
    "contracts": []
  },
  "acceptance_criteria": [
    "Batch 000 creates the app and private schemas with DATA-DEC-01's approved name, grants nothing to a client role, and touches no Supabase-managed schema.",
    "Every target the data package names is reachable through make, sets LC_ALL=C and TZ=UTC, and prints a stable summary line whether it passes or fails.",
    "Every target needing a database exits non-zero without one and names DB_TEST_URL. None reports a pass it cannot earn.",
    "A connection string never reaches stdout or stderr.",
    "Each schema lint rule is shown to REJECT a violating input, not merely to pass batch 000.",
    "The lint rejects ENABLE ROW LEVEL SECURITY without FORCE, which the lint rule the data package specifies would pass.",
    "npm run check stays green with no test skipped and no declared floor lowered."
  ],
  "required_tests": [
    "Each lint rule rejects a violating input",
    "Every live target refuses without a database",
    "db-verify fails as a whole and names what is missing",
    "A connection string is redacted",
    "npm run check"
  ],
  "deterministic_commands": {
    "setup": [],
    "verify": [
      "npm run check"
    ],
    "package_evidence": [
      "node --test test-kits/db/foundation-contract.test.mjs",
      "LC_ALL=C TZ=UTC make db-verify"
    ]
  },
  "security_privacy": {
    "data_classification": "synthetic-only",
    "secrets_required": false,
    "network_policy": "deny-unless-declared"
  },
  "rollback_or_forward_fix": "Revert through a reviewed revert PR. The migration has never been applied to any database -- no instance is provisioned -- so there is no deployed schema to roll back, and the command contract and lint are inert without one.",
  "open_blockers": [
    "The live half runs in CI and nowhere else. A Postgres service container gives db-migrate-clean, db-schema-lint and db-rls-smoke a real database on every pull request; all five migration batches apply and the schema lint passes. This DEVELOPMENT HOST still has no psql, pg_ctl or docker, so the targets refuse locally and every live change is proven by a CI round, not from a shell. db-migrate-upgrade and db-seed-replay remain unproven for a different reason: neither has a fixture to run against, and both say so rather than reporting a pass.",
    "open_blockers[1] asked for the co-owner's signature on the role topology. A1 wrote its own record at evidence/WP-0A-DB-00/a1-countersignature-role-topology.md. READ THE VERDICT THERE: this manifest is written by A0 and does not speak for another role, so it does not restate it here.\n\nWhat A0 can state, because it is a property of that document rather than a judgement about it: A1 re-measured the provisioned instance itself through ten read-only catalog queries rather than accepting A0's record, three of its findings contradict a record A0 wrote, and it declares in its own §0 that it is a subagent run in A0's vendor and model family assessing files it authored. It therefore does not satisfy independence.prefer_cross_vendor_review and does not lift the cross_vendor_exception this package already carries -- a second reading, not a second opinion.\n\nThe reservations that record raises are carried as the five blockers below, and they are open.",
    "RESERVATION 1 (A1 §5.1). RFC-2026-016 §4 retired 'force RLS where compatible' and made an EXEMPTION REGISTER the control that replaces it. No such register exists anywhere in the repository. The phrase was retired for being unfalsifiable; its replacement is unfalsifiable by absence. Owner: A0, with RFC-2026-016's author.",
    "RESERVATION 2 (A1 §5.2) — REPAIRED, and the repair is what is now asserted. scripts/db/run.mjs asserts no service role is SUPERUSER as well as no service role holds BYPASSRLS, refuses a snapshot whose service_roles carry no superuser field at all (an unmeasured property must not read as a passing one), and pins the instance's superuser set the way it already pinned the bypass set. Measured 2026-09-06 on xtvtflkntpqfvflvdbwk: all three service roles rolsuper false; supabase_admin is the only rolsuper on the instance.",
    "RESERVATION 3 (A1 §5.3/§5.4). The record does not describe the tree in two places. 010_identity.sql:14 cites 'batch 004 corrects the record' and no 004_*.sql exists, so batch 000's pgcrypto claim is still wrong on both counts (pgcrypto is in `extensions`; gen_random_uuid is in pg_catalog). 000_foundation.sql:12 still calls an approved RFC 'Proposed'.",
    "RESERVATION 4 (A1 §5.5) — REPAIRED at the call sites AND at the cause. The fixture and the invitation case now take the digest with pg_catalog's sha256(convert_to(...)), which resolves with no extension and no schema qualification; measured 2026-09-06 that it produces byte-identical output to extensions.digest(...,'sha256'), so no fixture identity moved. The cause was that the CI shim was MORE PERMISSIVE than the platform: batch 000's `create extension ... with schema public` is a no-op on the instance and succeeded in the container, so public.digest existed only in CI. The shim now installs pgcrypto in `extensions` where the platform has it, and the test that used to pin `public.digest(` now refuses any schema-qualified pgcrypto call.",
    "RESERVATION 5 (A1 §5.6) — REPAIRED. expectDeniedBy holds a case to the layer it declares, and the runner applies it to every case carrying deniedBy. A policy refusal always names the policy; anything else saying permission denied is the privilege layer; anything that classifies as neither fails rather than guessing. Silence stays silence: a case declaring no layer is not checked.",
    "Tenant isolation is proven against the CI container and measured once against a provisioned Supabase instance, and the two prove different things. In CI, 28 isolation cases pass on every run and a NEGATIVE CONTROL disables row level security on app.workspaces and requires the suite to fail -- it reports 10 of 28, so the suite is not blind. What CI cannot establish is the platform: its roles come from db/foundation/ci/supabase-shim.sql and were created by us, so a green run means the policies still do what they did, never that this holds on Supabase. The BYPASSRLS measurement behind DATA-DEC-03 came from the provisioned instance and has no CI equivalent. Batches 020 and above own the assertions SMOKE_COVERAGE names as uncovered.",
    "Gate G0 remains Specification Baseline Complete / External Verification Pending."
  ],
  "review_and_test_gates": [
    "author_complete",
    "review_approved",
    "security_approved",
    "test_verified",
    "integration_verified"
  ]
}
