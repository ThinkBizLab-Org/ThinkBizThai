{
  "schema_version": "1.0.0",
  "protocol_version": "1.0.0",
  "work_package_id": "WP-0A-A0-008",
  "title": "The service path identity, measured rather than assumed",
  "gate": "G0",
  "priority": "P0",
  "status": "in_progress",
  "required_skill_profiles": {
    "author": [
      "architecture-contracts"
    ],
    "reviewer": [
      "architecture-contracts"
    ],
    "tester": [
      "independent-qa"
    ],
    "integration_owner": [
      "integration-release"
    ],
    "conditional_reviewers": [
      "security-privacy"
    ]
  },
  "role_assignments": {
    "author_agent_run_id": "/claude/a0_atlas",
    "reviewer_agent_run_id": "/claude/c0_contract_reviewer",
    "tester_agent_run_id": "/claude/q0_sentinel",
    "integration_owner_agent_run_id": "/claude/r0_steward",
    "security_reviewer_agent_run_id": "/claude/a1_bastion",
    "product_reviewer_agent_run_id": null,
    "_run_id_disambiguation": "/claude/r0_steward (Anthropic) is THIS package's Integration Owner and must record its own capability declaration before this package leaves backlog. /root/r0_steward (OpenAI Codex) is a DIFFERENT run: it is the Integration Owner of WP-0A-A0-001 and WP-0A-CON-001 and is named in open_blockers solely because it must countersign the amendments made to those two packages and record the WP-0A-CON-001 npm run check addendum. Neither run substitutes for the other. Independent testing flagged the near-identical names as ambiguous; both are intended."
  },
  "independence": {
    "no_self_approval": true,
    "require_distinct_author_reviewer": true,
    "require_distinct_author_tester": true,
    "prefer_cross_vendor_review": true,
    "cross_vendor_exception": "prefer_cross_vendor_review is NOT satisfied: every assigned run is Anthropic claude-opus-5. The Decision Register (§ cross-vendor review) permits this only as an explicit exception - 'if there is no cross-vendor capacity, use an independent agent per run and record the exception'. Each of the five roles is a separate agent run with its own capability declaration and no run reviewed, tested, or integrated its own work. The Integration Owner must carry this exception forward; it is not waived and it is not equivalent to cross-vendor review."
  },
  "accountable_owners": [
    "A0"
  ],
  "required_human_authorities": [
    "Product Owner disposition of RFC-2026-017, which closes the half of DATA-DEC-03 RFC-2026-016 left open.",
    "A1 to countersign the role topology before the roles are created, since DATA-DEC-03 is owned A0+A1 and this is a security surface."
  ],
  "purpose": "RFC-2026-016 closed the direction of DATA-DEC-03 and refused to claim the control, because the service path was undecided and on Supabase the service-role key bypasses RLS. The instance was then measured: service_role bypasses, and so does postgres, which is the role in every direct connection string -- so both obvious options bypass. postgres holds rolcreaterole, so a topology that does not bypass is implementable. This package records the decision that follows, which closes the half of DATA-DEC-03 that was left open.",
  "scope": {
    "include": [
      "RFC-2026-017 recording the service-path decision and the role topology it implies",
      "The measurement behind it, taken from the provisioned instance rather than vendor documentation",
      "The costs of the decision, recorded so they are not discovered later as surprises"
    ],
    "exclude": [
      "Creating the roles. That is a migration, and it belongs to the batch A0 owns, not to a decision record.",
      "Writing any tenant table or policy. Batch 010 is A1 Identity's.",
      "Claiming the isolation is proven. No tenant table exists, so no cross-tenant assertion has run.",
      "Disposing this RFC. That is the Product Owner's act on a disposition branch."
    ]
  },
  "decisions_consumed": [
    "DATA-DEC-03"
  ],
  "contracts_consumed": [],
  "contracts_produced": [],
  "dependencies": {
    "required_work_packages": [
      "WP-0A-A0-007"
    ],
    "approved_fixtures": []
  },
  "ownership": {
    "branch": "agent/claude/WP-0A-A0-008-service-path",
    "writable_paths": [
      "architecture/decisions/RFC-2026-017-service-path-identity.md",
      "work-packages/WP-0A-A0-008.json",
      "evidence/WP-0A-A0-008/**",
      "handoffs/WP-0A-A0-008-*.json"
    ],
    "read_only_paths": [
      "README.md",
      "docs/**",
      "CONTRIBUTING_AGENTS.md",
      "AGENTS.md",
      "CLAUDE.md",
      "contract-catalog/**",
      "package.json",
      "package-lock.json"
    ],
    "forbidden_paths": [
      "*.pem",
      "*.key",
      "*.p12",
      "*.pfx",
      "db/**",
      "migrations/**"
    ],
    "amends_without_owning": {
      "paths": [],
      "rationale": "THIS INCREMENT AMENDS NOTHING OUTSIDE THIS PACKAGE. It corrects three sentences in this package's own open_blockers and removes one that is spent, touching only the manifest and the handoff this package owns. `paths` is emptied rather than left carrying the earlier increment's four (evidence/VERIFICATION.md, scripts/test-suite-contract.mjs, test-kits/branch-identity.test.mjs, test-kits/integrity-manifest.json), because verify-branch-scope reads the declaration in both directions and exits 74 on a declared pattern matching nothing in the diff. `recorded_on` is removed with them: it named work-packages/WP-0A-A0-002.json as the register where the amendment was recorded, that register's `ownership.amended_by` has never held an entry for this package, and with `paths` empty there is no amendment for it to point at. Those four paths remain in git history."
    }
  },
  "inputs": {
    "files": [
      "architecture/decisions/RFC-2026-016-rls-service-policy-and-platform.md",
      "docs/sprint-0a/sprint-0a-core-erd-rls-retention-th.md",
      "db/foundation/lint/catalog-snapshot.json"
    ],
    "fixtures": []
  },
  "outputs": {
    "files": [
      "architecture/decisions/RFC-2026-017-service-path-identity.md",
      "work-packages/WP-0A-A0-008.json"
    ],
    "contracts": []
  },
  "acceptance_criteria": [
    "The RFC states the measurement it rests on, names the instance it was taken from, and does not cite vendor documentation in its place.",
    "It records that postgres bypasses RLS as well as service_role, so that a reader does not conclude a direct driver is by itself a fix.",
    "The role topology names every role, its purpose, and that none holds BYPASSRLS.",
    "The costs are recorded in the RFC, not left to be discovered during implementation.",
    "The RFC does NOT claim tenant isolation is proven, and names the assertion that would prove it as still owed.",
    "The RFC is committed with status Proposed."
  ],
  "required_tests": [
    "npm run check",
    "The work-package and ownership validators accept this manifest"
  ],
  "deterministic_commands": {
    "setup": [],
    "verify": [
      "npm run check"
    ],
    "package_evidence": [
      "node scripts/validate-work-packages.mjs",
      "node scripts/validate-work-package-ownership.mjs"
    ]
  },
  "security_privacy": {
    "data_classification": "synthetic-only",
    "secrets_required": false,
    "network_policy": "deny-unless-declared"
  },
  "rollback_or_forward_fix": "Revert through a reviewed revert PR. RFC-2026-017 is a Proposed decision record: no role is created, no policy written, no migration applied, and nothing on the provisioned instance is touched by it.",
  "open_blockers": [
    "WHAT RFC-2026-017 §7 ASKS FOR NOW EXISTS AND RUNS, AND WHETHER THAT DISCHARGES §7 IS NOT THIS PACKAGE'S TO DECIDE. §7 asks for \"the service identity attempting an operation the matrix marks `N` for service and being denied with an ERROR, not an empty result\", and says it \"does not exist yet\". Measured at 2e18f75, which is this branch's base after PRs #104, #105 and #106 merged: 65 case blocks in tests/db/identity/isolation-cases.mjs declare `covers: ['RFC-2026-017§7']` and 28 of them declare `expect: 'denied'`; tests/db/identity/identity-isolation.test.mjs carries a test named \"the service denial is attributable to row level security and not to a forgotten grant\"; and private.as_service() in db/foundation/test-helpers/auth-context.sql reaches app_worker through `set_config('role','app_worker',true)`, which was measured to switch current_role and to land on a role whose rolbypassrls is false. So the assertion exists, runs, and runs as a role that does not bypass. THIS BLOCKER IS LEFT OPEN DELIBERATELY: closing it would be this package judging its own §7 condition met, and an independent reviewer should decide whether 28 denial cases over 45 tables are what §7 meant.",
    "WHAT IS GENUINELY UNPROVEN IS THE PRODUCTION SERVICE PATH, NOT THE POLICY LAYER. The tests above become app_worker from a superuser connection, which works because postgres may SET ROLE to anything. RFC-2026-022 §5/8 measures that the only MEMBER of app_worker is postgres, and RFC-2026-019 §4/3 leaves app_worker's connection method undecided until a background worker exists. So nothing outside a test helper can be app_worker today, and that is the gap -- owed to the RFC that creates a worker rather than to whoever writes the first tenant table, which is what this blocker used to say.",
    "Gate G0 remains Specification Baseline Complete / External Verification Pending; a decision record is reversible, synthetic-only work whether it is Proposed or disposed. The tail of this sentence used to read \"a Proposed decision record is reversible, synthetic-only work\", and the 2026-09-05 disposition made that clause false while leaving the restriction it justifies true."
  ],
  "review_and_test_gates": [
    "author_complete",
    "review_approved",
    "security_approved",
    "test_verified",
    "integration_verified"
  ]
}
